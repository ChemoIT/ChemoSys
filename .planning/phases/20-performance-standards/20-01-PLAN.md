---
phase: 20-performance-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - c:/Sharon_ClaudeCode/CLAUDE.md
  - .planning/performance-standard.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md contains a new IRON RULE section titled 'Performance Standard' that mandates Suspense + Skeleton + loading indicators for every new page"
    - "A performance-standard.md document exists in .planning/ with complete code examples extracted from the fuel page reference implementation"
    - "Any Claude Code agent reading CLAUDE.md knows exactly what patterns to apply when building a new page"
  artifacts:
    - path: "c:/Sharon_ClaudeCode/CLAUDE.md"
      provides: "IRON RULE: Performance Standard section"
      contains: "כלל ברזל: ביצועים"
    - path: ".planning/performance-standard.md"
      provides: "Complete performance standard documentation with code examples"
      contains: "FuelPageSkeleton"
  key_links:
    - from: "c:/Sharon_ClaudeCode/CLAUDE.md"
      to: ".planning/performance-standard.md"
      via: "IRON RULE references the detailed doc for code examples"
      pattern: "performance-standard\\.md"
---

<objective>
Add a new IRON RULE to CLAUDE.md defining the mandatory performance pattern for all new pages, and create a comprehensive performance-standard.md reference document with full code examples from the fuel page.

Purpose: Ensure every future page built by Claude Code automatically includes Suspense boundaries, Skeleton loading states, and loading indicators — no more blank screens or silent loading.

Output: Updated CLAUDE.md + new .planning/performance-standard.md
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference implementation (the fuel page patterns)
@src/app/(app)/app/fleet/fuel/page.tsx
@src/components/app/fleet/fuel/FuelPageSkeleton.tsx
@src/components/app/fleet/fuel/FuelRecordsPage.tsx
@src/components/ui/skeleton.tsx
@src/actions/fleet/fuel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IRON RULE "ביצועים" to CLAUDE.md</name>
  <files>c:/Sharon_ClaudeCode/CLAUDE.md</files>
  <action>
Add a new IRON RULE section to CLAUDE.md, placed AFTER the existing "כלל ברזל: ChemoSystem Responsive Design" section (which is the last IRON RULE in the MEMORY.md pattern). Use the exact same format as other IRON RULEs in CLAUDE.md (## כלל ברזל: ...).

The new section title: `## כלל ברזל: ביצועים — Performance Standard`

Content must include these mandatory rules:

1. **כל דף חדש חייב `<Suspense>` עם `fallback` של Skeleton component** — אסור לשלוח async data fetch ללא Suspense boundary
2. **כל דף חדש חייב Skeleton component ייעודי** — Skeleton חייב לשקף את layout האמיתי של הדף (header, filters, table/cards)
3. **כל Skeleton חייב animated progress bar** — shimmer bar בחלק העליון (`animate-[shimmer_1.5s_ease-in-out_infinite]`)
4. **כל filter/search/pagination חייב loading indicator** — `useTransition` + `isPending` → spinner + "מעדכן נתונים..." text
5. **Server Actions שמחזירות data לרשימות חייבות `React.cache()`** — למניעת queries כפולים ב-render pass יחיד
6. **אגרגציות חייבות להיות RPC בצד DB** — לא loops ב-JS על אלפי שורות (`get_fuel_stats` = הרפרנס)
7. **מסמך הנחיות מלא:** `performance-standard.md` ב-`.planning/` — חובה לקרוא לפני כתיבת דף חדש

Add a "עקרון מנחה" quote at the end:
> "המשתמש חייב לראות feedback ויזואלי תוך אפס שניות — אף דף לא נטען לתוך מסך ריק."

Also add a reference to the detailed doc:
> **מסמך מלא עם דוגמאות קוד:** `.planning/performance-standard.md`
  </action>
  <verify>Read CLAUDE.md and confirm the new IRON RULE section exists with all 7 rules listed.</verify>
  <done>CLAUDE.md contains "כלל ברזל: ביצועים" section with 7 mandatory rules, guiding principle quote, and reference to performance-standard.md</done>
</task>

<task type="auto">
  <name>Task 2: Create performance-standard.md reference document</name>
  <files>.planning/performance-standard.md</files>
  <action>
Create `.planning/performance-standard.md` — a comprehensive reference document that any Claude agent can read before building a new page. Structure:

**Header:**
```
# Performance Standard — תבנית ביצועים חובה
> רפרנס: דף דלק (`/app/fleet/fuel`) — Session #40
```

**Sections:**

### 1. Page Structure Pattern (Suspense + Skeleton)
Show the EXACT code from `src/app/(app)/app/fleet/fuel/page.tsx` as the reference:
- Outer page component: `await verifyAppUser()` + `<Suspense fallback={<Skeleton />}>`
- Inner async component: `async function FuelContent()` with `Promise.all()` for parallel data fetching
- Explain: page.tsx renders instantly → Skeleton shows → data loads → real content replaces Skeleton

### 2. Skeleton Component Pattern
Show the FULL code of `FuelPageSkeleton.tsx` as the reference.
Document the required elements:
- **Animated shimmer bar** at top (the `w-1/3 bg-sky-500/70 animate-[shimmer]` pattern)
- **Layout matching**: Skeleton must mirror the real page layout (header area → filter bar → table/card grid)
- **shadcn `<Skeleton>`** component usage for individual placeholder blocks
- **Inline `@keyframes shimmer`** style block
- Naming convention: `{PageName}Skeleton` (e.g., `VehicleListSkeleton`, `DriverCardSkeleton`)

### 3. Loading Indicator Pattern (Filter/Search/Pagination)
Show the pattern from `FuelRecordsPage.tsx`:
- `useTransition()` hook → `[isPending, startTransition]`
- Wrap filter changes in `startTransition(() => { ... })`
- Render: `{isPending && <div>spinner + "מעדכן נתונים..."</div>}`
- Use `Loader2` from `lucide-react` for the spinner icon

### 4. React.cache() Pattern
Show the pattern from `fuel.ts`:
- `import { cache } from 'react'`
- `export const getProjectsForFuelFilter = cache(async () => { ... })`
- Explain: prevents duplicate DB calls when multiple server components call the same function in one render pass
- When to use: any Server Action called from multiple server components in the same request

### 5. DB Aggregation Pattern (RPC)
Show the `get_fuel_stats` RPC concept:
- Stats calculated with `SUM()` / `COUNT()` in DB, not JS loops
- Use Supabase `.rpc('function_name', { params })` to call
- Explain: prevents fetching thousands of rows to client just to sum them

### 6. Checklist — Before Shipping a New Page
A checklist that Claude can verify before marking a page task as done:
- [ ] `page.tsx` has `<Suspense>` wrapping async content
- [ ] Skeleton component exists and matches real layout
- [ ] Skeleton has animated shimmer bar at top
- [ ] Filter/search/pagination uses `useTransition` + loading indicator
- [ ] Server Actions returning shared data use `React.cache()`
- [ ] Aggregations run in DB (RPC), not JS
- [ ] No blank screen during initial load
- [ ] No silent filter changes — user sees "מעדכן נתונים..."

Write all explanatory text in Hebrew. Code blocks remain in English.
  </action>
  <verify>Read .planning/performance-standard.md and confirm all 6 sections exist with code examples.</verify>
  <done>performance-standard.md exists in .planning/ with 6 sections: page structure, skeleton pattern, loading indicator, React.cache, DB aggregation, and pre-ship checklist — all with code examples from the fuel page reference</done>
</task>

</tasks>

<verification>
1. `grep "כלל ברזל: ביצועים" c:/Sharon_ClaudeCode/CLAUDE.md` returns a match
2. `cat .planning/performance-standard.md | head -5` shows the title and reference line
3. `grep "Suspense" .planning/performance-standard.md` returns matches (code examples present)
4. `grep "FuelPageSkeleton" .planning/performance-standard.md` returns matches (reference impl cited)
5. `grep "performance-standard.md" c:/Sharon_ClaudeCode/CLAUDE.md` returns a match (cross-reference exists)
</verification>

<success_criteria>
- CLAUDE.md has a new IRON RULE section that any Claude agent will see and follow
- performance-standard.md provides complete, copy-pasteable code patterns
- Both files cross-reference each other
- Requirements RULE-01 and RULE-02 are fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/20-performance-standards/20-01-SUMMARY.md`
</output>
