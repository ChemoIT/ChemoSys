---
phase: 04-projects
plan: "04"
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "All Phase 4 UI and data features work end-to-end after migration is applied"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 4 implementation: migration applied, project CRUD working, map rendering, export functioning. This is a human-verify checkpoint where Sharon tests the full project management flow.

Purpose: Confirm all 12 requirements (PROJ-01 through PROJ-11 + EXPORT-01) work correctly before marking Phase 4 complete.
Output: User confirmation or issue list for gap closure.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-projects/04-01-SUMMARY.md
@.planning/phases/04-projects/04-02-SUMMARY.md
@.planning/phases/04-projects/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration 00014 in Supabase SQL editor</name>
  <files>supabase/migrations/00014_projects_rebuild.sql</files>
  <action>
The migration must be applied manually in the Supabase SQL editor because this project uses Supabase hosted (not local CLI).

Instructions:
1. Open the Supabase Dashboard for the ChemoSystem project
2. Go to SQL Editor
3. Open the file `supabase/migrations/00014_projects_rebuild.sql`
4. Copy the FULL content and paste it into the SQL editor
5. Click "Run" -- all statements should succeed
6. Verify in Table Editor:
   - `projects` table exists with all new columns (open_date, status with view_only, client_name, supervision_*, cvc_is_employee, radius, etc.)
   - `attendance_clocks` table exists with project_id FK and clock_id
7. Go to Storage and create a new bucket called `client-logos` with Public access ON

Resume signal: Type "migration applied" when done, or describe any errors.
  </action>
  <verify>projects table and attendance_clocks table visible in Supabase Table Editor with correct columns</verify>
  <done>Migration 00014 applied successfully, client-logos bucket created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full Phase 4 verification</name>
  <files>src/app/(admin)/admin/projects/page.tsx</files>
  <action>
Complete project management has been built: CRUD with all fields, employee-linked managers, conditional CVC, attendance clocks, client logo upload, supervision contact, map location with radius, 3-state status filter, export to Excel/CSV.

**Start dev server** (if not running): `npm run dev` then open http://localhost:3000

**A. Navigation (1 min)**
1. Verify "פרויקטים" appears in the sidebar and is clickable
2. Click it -- projects page loads with empty table and "פרויקט חדש" button

**B. Create Project -- Basic Fields (2 min) -- PROJ-01, PROJ-05**
3. Click "פרויקט חדש"
4. Fill name: "פרויקט בדיקה ראשון"
5. Verify project_number field is disabled and shows "ייווצר אוטומטית"
6. Set open_date to today
7. Set expense_number: "EXP-001"
8. Set project_type to "פרויקט"
9. Set status to "פעיל"
10. Add description: "פרויקט לבדיקת המערכת"

**C. Managers -- PROJ-04**
11. In מנהל פרויקט section: search for an employee by name, select one
12. Verify email and phone fields auto-populate from employee data
13. Check the "שלח הודעות" checkbox
14. Repeat for מנהל עבודה (select a different employee)

**D. CVC -- PROJ-10**
15. Verify CVC starts in "employee mode" by default
16. Switch to "רישום חופשי" mode
17. Enter invalid phone "1234" -- should show validation error
18. Enter valid phone "0501234567" -- error should clear
19. Switch back to employee mode, select an employee

**E. Client -- PROJ-08**
20. Enter client name: "חברת מזמין בדיקה"
21. Upload a small image file as client logo
22. Verify the image preview appears

**F. Supervision -- PROJ-09**
23. Fill supervision_company: "חברת פיקוח בדיקה"
24. Fill contact name, email, phone
25. Check "אישור שליחת הודעות" and "צירוף לדוחות"

**G. Attendance Clocks -- PROJ-07**
26. Click "הוסף שעון" -- a new empty clock input appears
27. Enter clock_id: "CLK-001"
28. Add another clock: "CLK-002"
29. Remove one clock (X button) -- verify it disappears

**H. Map Location -- PROJ-11**
30. Scroll to the map section -- verify OpenStreetMap renders (tiles load, Israel visible)
31. Click on the map -- verify coordinates appear in the latitude/longitude fields
32. Change radius to 200 -- verify the blue circle on the map changes size

**I. Save and Verify**
33. Click "שמירה" -- dialog should close
34. Verify the new project appears in the table
35. Verify project_number is auto-generated (PR26XXXXXX format)
36. Verify status badge is green "פעיל"
37. Verify PM and SM names show in the table

**J. Edit Project -- PROJ-02**
38. Click edit on the created project
39. Verify all fields are populated with the saved data
40. Change the status to "לצפייה בלבד"
41. Save -- verify yellow badge appears in table

**K. Status Filter -- PROJ-06**
42. Verify the active count badge shows correct number
43. Filter by "לצפייה בלבד" -- only the edited project shows
44. Filter by "פעיל" -- the edited project should NOT show
45. Filter by "הכל" -- all projects show

**L. Soft Delete -- PROJ-03**
46. Click delete on a project
47. Confirm in the dialog
48. Verify the project disappears from the list

**M. Export -- EXPORT-01**
49. Click "ייצוא Excel" -- an .xlsx file should download
50. Open the file -- verify it contains project data with Hebrew worksheet name
51. Click "ייצוא CSV" -- a .csv file should download
52. Navigate to employees page and test export there too (verifies universal export)

**N. Audit Log**
53. In Supabase Dashboard, check audit_log table for INSERT/UPDATE/DELETE entries with entity_type='projects'

Resume signal: Type "approved" if all checks pass, or list specific issues found.
  </action>
  <verify>All 53 verification steps pass with correct behavior</verify>
  <done>All 12 Phase 4 requirements verified: PROJ-01 through PROJ-11 + EXPORT-01</done>
</task>

</tasks>

<verification>
All 12 Phase 4 requirements verified by human testing:
- PROJ-01: Create with all fields
- PROJ-02: Edit existing project
- PROJ-03: Soft delete
- PROJ-04: PM and SM from employees with auto-pull
- PROJ-05: Auto-generated PR26XXXXXX
- PROJ-06: 3-state filter with active count
- PROJ-07: Attendance clocks (add/remove)
- PROJ-08: Client name + logo upload
- PROJ-09: Supervision contact with notification flags
- PROJ-10: CVC employee/manual toggle with phone validation
- PROJ-11: Map with coordinates + radius circle
- EXPORT-01: Excel/CSV export from any admin table
</verification>

<success_criteria>
User confirms all verification steps pass, or issues are logged for gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/04-projects/04-04-SUMMARY.md`
</output>
