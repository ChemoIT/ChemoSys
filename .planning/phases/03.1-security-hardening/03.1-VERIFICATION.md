---
phase: 03.1-security-hardening
verified: 2026-03-03T15:13:56Z
status: human_needed
score: 13/14 must-haves verified
human_verification:
  - test: Confirm migration 00013 was applied in Supabase SQL editor
    expected: pg_policies returns 4 policies on user_permissions - delete_admin, insert_admin, select, update_admin
    why_human: Cannot query Supabase production DB programmatically. SUMMARY states Sharon confirmed but unverifiable from code.
  - test: Confirm NEXT_SERVER_ACTIONS_ENCRYPTION_KEY is set in .env.local
    expected: File .env.local has key with real 44-char base64 value, not the placeholder
    why_human: .env.local is gitignored and inaccessible via filesystem tools.
---

# Phase 03.1: Security Hardening Verification Report

Phase Goal: Apply security hardening across the full Next.js 16 + Supabase admin system before deployment: upgrade patched Next.js version (CVE fix), add security headers + CSP, add rate limiting on login, tighten RLS on user_permissions, fix minor security gaps (server-only guard, PII logs, encryption key).
Verified: 2026-03-03T15:13:56Z
Status: human_needed - 13/14 automated checks pass; 2 items require human confirmation
Re-verification: No - initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Next.js version is patched against CVE-2025-55183 and CVE-2025-67779 | VERIFIED | package.json line 33: next 16.1.6. npm install next@latest confirmed latest stable - CVE fix in 16.0.x line carried forward |
| 2 | Every HTTP response includes X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, and Content-Security-Policy | VERIFIED | next.config.ts lines 41-55: all 5 headers in async headers() with source /(.*) |
| 3 | HSTS header is only sent in production (not in development) | VERIFIED | next.config.ts lines 49-52: HSTS in isDev ternary spread - absent in dev, present in prod |
| 4 | CSP allows Supabase API calls (connect-src) and Storage images (img-src) | VERIFIED | next.config.ts lines 20-21: connect-src and img-src both reference supabaseUrl from NEXT_PUBLIC_SUPABASE_URL |
| 5 | CSP does NOT reference fonts.googleapis.com or fonts.gstatic.com | VERIFIED | next.config.ts line 19: font-src self data - no CDN domains; line 13 comment confirms Heebo served locally |
| 6 | dal.ts cannot be accidentally imported from client components | VERIFIED | src/lib/dal.ts line 1: import server-only - first import before all other code |
| 7 | Login endpoint blocks IP after 5 failed attempts within 60 seconds | VERIFIED | src/actions/auth.ts lines 24-53: loginAttempts Map, RATE_LIMIT_MAX=5, RATE_LIMIT_WINDOW_MS=60000; checkLoginRateLimit() first in login() |
| 8 | Rate-limited users see Hebrew error message | VERIFIED | src/actions/auth.ts line 70: returns Hebrew text for too many attempts |
| 9 | Rate limiting resets after 60 seconds | VERIFIED | src/actions/auth.ts lines 41-43: window expiry check resets Map entry to count=1 |
| 10 | importEmployeesAction error log does not leak employee PII | VERIFIED | src/actions/employees.ts line 747: err instanceof Error ? err.message : Unknown error - raw err not logged |
| 11 | NEXT_SERVER_ACTIONS_ENCRYPTION_KEY is documented for deployment | VERIFIED | .env.local.example in git commit eefb1f1: key var with generation instructions |
| 12 | user_permissions INSERT/UPDATE/DELETE require caller is_admin=true | VERIFIED | 00013 migration lines 21-56: 3 policies with EXISTS subquery checking is_admin=true |
| 13 | user_permissions SELECT policy remains permissive for authenticated users | VERIFIED | Migration 00013 does NOT drop SELECT policy; comment line 58 confirms preservation |
| 14 | Migration 00013 applied in Supabase - non-admin cannot self-grant permissions | UNCERTAIN - HUMAN NEEDED | SUMMARY states applied and confirmed by user. Cannot verify from codebase. |

Score: 13/14 truths verified (14th requires human confirmation)

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| package.json | Patched Next.js version | VERIFIED | next 16.1.6 present. Unstaged diff: caret added (pinned 16.1.6 -> floating ^16.1.6) - minor finding |
| next.config.ts | Security headers and CSP | VERIFIED | 72 lines; async headers() with 5 headers + CSP; isDev guards for HSTS and unsafe-eval |
| src/lib/dal.ts | server-only guard as first import | VERIFIED | import server-only at line 1 |
| src/actions/auth.ts | Login with rate limiting | VERIFIED | checkLoginRateLimit, loginAttempts Map, getClientIp, Hebrew error - all present and wired |
| src/actions/employees.ts | Sanitized error logging | VERIFIED | Line 747: err instanceof Error check prevents PII dump |
| .env.local.example | Documented env vars including encryption key | VERIFIED | Tracked in git commit eefb1f1; contains NEXT_SERVER_ACTIONS_ENCRYPTION_KEY with instructions |
| .env.local | Actual encryption key set on disk | UNCERTAIN | Gitignored. Cannot access via tools. Cannot confirm key was written to disk. |
| supabase/migrations/00013_tighten_user_permissions_rls.sql | Admin-only write policies | VERIFIED | 3 admin policies with is_admin=true EXISTS subquery |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| next.config.ts | NEXT_PUBLIC_SUPABASE_URL | process.env in CSP connect-src and img-src | WIRED | Line 7: supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL; used in CSP lines 20-21 |
| src/actions/auth.ts | headers() from next/headers | import for IP extraction | WIRED | Line 12: import headers; used at line 66: await headers() |
| src/actions/auth.ts | loginAttempts Map | module-level singleton in Node.js runtime | WIRED | Line 24: const loginAttempts = new Map(); used in checkLoginRateLimit() lines 38-53 |
| supabase/migrations/00013 | users table | EXISTS subquery checking is_admin | WIRED | Lines 25-29, 38-43, 50-55: EXISTS SELECT 1 FROM users WHERE auth_user_id = auth.uid() AND is_admin = true AND deleted_at IS NULL |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| next.config.ts | 67 | TODO: fill allowedOrigins before production go-live | Info | Intentional placeholder. Must fill before deployment. Not a blocker for dev. |
| package.json | unstaged | Floating ^16.1.6 instead of pinned 16.1.6 | Warning | May auto-upgrade on next npm install. Not a security gap but reduces reproducibility. Change is unstaged. |

No blocker anti-patterns found.

---

## Human Verification Required

### 1. Confirm RLS Migration 00013 Applied in Supabase

Test: Open Supabase Dashboard - SQL Editor and run:

```sql
SELECT policyname, cmd, permissive, qual, with_check
FROM pg_policies
WHERE tablename = 'user_permissions'
ORDER BY policyname;
```

Expected: Exactly 4 rows:
- user_permissions_delete_admin - DELETE - is_admin = true in qual
- user_permissions_insert_admin - INSERT - is_admin = true in with_check
- user_permissions_select - SELECT - permissive (from migration 00002, unchanged)
- user_permissions_update_admin - UPDATE - is_admin = true in with_check

Why human: Cannot query production Supabase DB from the codebase. SUMMARY states Sharon confirmed migration ran successfully. This is the critical Broken Access Control fix.

### 2. Confirm NEXT_SERVER_ACTIONS_ENCRYPTION_KEY Is Set in .env.local

Test: In the project root terminal, run:

```bash
grep NEXT_SERVER_ACTIONS_ENCRYPTION_KEY .env.local
```

Expected: One line with a real 44-character base64 value (not the placeholder text).

Why human: .env.local is gitignored and inaccessible via automated tools. If missing, Server Actions may show Server Action expired errors after redeployment.

---

## Gaps Summary

No hard gaps blocking the phase goal. All code-verifiable security controls are in place:

- CVE patch: Next.js 16.1.6 confirmed latest stable at time of execution (npm install next@latest)
- Security headers: All 5 present on all routes; HSTS production-only via isDev guard
- CSP: Supabase connect-src/img-src scoped correctly; Tailwind unsafe-inline; no external font CDN
- server-only guard: First line of dal.ts - prevents client bundle inclusion of session verification logic
- Rate limiting: In-memory Map, 5 attempts/60s, Hebrew error message, fires before Supabase Auth call
- PII logging: Sanitized in employees.ts importEmployeesAction catch block (line 747)
- RLS migration: SQL file complete, correct, committed (commit 95c7bcd)

Two items require human confirmation:
1. Whether migration 00013 is live in Supabase (the DB-level Broken Access Control fix)
2. Whether .env.local has the generated encryption key on disk

Both reported as done in SUMMARY.md. If Sharon confirms both, the phase goal is fully achieved.

---

_Verified: 2026-03-03T15:13:56Z_
_Verifier: Claude (gsd-verifier) - claude-sonnet-4-6_