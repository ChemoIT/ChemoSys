---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/supabase/server.ts
  - src/lib/supabase/browser.ts
  - proxy.ts
  - .env.local.example
  - public/logo-he.png
  - public/logo-icon.png
autonomous: true
user_setup:
  - service: supabase
    why: "Database and authentication backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> anon/public (or publishable)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> service_role (secret)"
    dashboard_config:
      - task: "Create a new Supabase project for ChemoSys"
        location: "https://supabase.com/dashboard -> New Project"

must_haves:
  truths:
    - "Next.js app starts with `npm run dev` and renders a Hebrew RTL page"
    - "Heebo font loads for Hebrew text with no CLS flash"
    - "Brand colors (turquoise #4ECDC4, dark #1B3A4B, bg #F5F7FA) are available as Tailwind theme tokens"
    - "Supabase server client factory compiles without errors using async cookies"
    - "proxy.ts file exists at project root and exports an async proxy function"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with dir=rtl, lang=he, Heebo font"
      contains: "dir=\"rtl\""
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS config with brand theme"
      contains: "@theme"
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client factory"
      contains: "await cookies()"
    - path: "src/lib/supabase/browser.ts"
      provides: "Browser-side Supabase client factory"
      contains: "createBrowserClient"
    - path: "proxy.ts"
      provides: "Auth guard proxy for Next.js 16"
      contains: "export async function proxy"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "src/lib/supabase/server.ts"
      to: "next/headers"
      via: "async cookies import"
      pattern: "await cookies\\(\\)"
---

<objective>
Scaffold the ChemoSys Next.js project with RTL Hebrew UI foundation, brand theming, and Supabase client infrastructure.

Purpose: This is the foundation every other plan depends on. The scaffold must be correct from the first commit: RTL direction on HTML, Heebo font loaded with Hebrew subset, brand colors in Tailwind theme, and Supabase client factories ready for auth and data access.

Output: A running Next.js 16 app with correct RTL rendering, brand theme, shadcn/ui components installed, and Supabase client utilities ready to use.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with all dependencies</name>
  <files>
    package.json
    .env.local.example
    public/logo-he.png
    public/logo-icon.png
  </files>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` from the project root (C:/Sharon_ClaudeCode/Apps/ChemoSystem). Accept defaults. This installs Next.js 16, React 19.2, Tailwind v4, TypeScript.

    2. Install core dependencies:
    ```bash
    npm install @supabase/supabase-js @supabase/ssr zod react-hook-form @hookform/resolvers lucide-react @tanstack/react-table
    ```

    3. Initialize shadcn/ui:
    ```bash
    npx shadcn@latest init
    ```
    When prompted: style=default, base color=slate, CSS variables=yes.

    4. Add shadcn/ui components needed for Phase 1:
    ```bash
    npx shadcn@latest add button input label table tabs dialog dropdown-menu select badge toast card skeleton separator form sheet
    ```
    Note: `sheet` is needed for mobile sidebar drawer.

    5. Create `.env.local.example` with:
    ```
    NEXT_PUBLIC_SUPABASE_URL=your-project-url
    NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
    ```
    NOTE: Do NOT use NEXT_PUBLIC_ prefix for service role key. Also create `.env.local` by copying the example (it should already be in .gitignore from create-next-app).

    6. Copy logo files from project root into public/:
    - `Heb-ChemoIT.png` -> `public/logo-he.png` (full Hebrew logo for login page — AUTH-04)
    - `CA.png` -> `public/logo-icon.png` (icon for sidebar)

    7. Add `.env.local` to `.gitignore` if not already there (create-next-app usually includes it).
  </action>
  <verify>
    - `npm run dev` starts without errors
    - `package.json` contains all listed dependencies
    - `.env.local.example` exists with correct variable names
    - `public/logo-he.png` and `public/logo-icon.png` exist
    - shadcn/ui components exist in `src/components/ui/`
  </verify>
  <done>Next.js 16 project scaffolded with all dependencies installed, shadcn/ui initialized with Phase 1 components, logo files in public/, env template created.</done>
</task>

<task type="auto">
  <name>Task 2: Configure RTL root layout, brand theme, and Supabase clients</name>
  <files>
    src/app/layout.tsx
    src/app/globals.css
    src/app/page.tsx
    src/lib/supabase/server.ts
    src/lib/supabase/browser.ts
    proxy.ts
  </files>
  <action>
    1. **Root Layout** (`src/app/layout.tsx`):
    - Import Heebo from `next/font/google` with `subsets: ['hebrew', 'latin']`, `display: 'swap'`, `variable: '--font-heebo'`, `weight: ['300', '400', '500', '600', '700', '800']`
    - Set `<html lang="he" dir="rtl" className={heebo.variable}>`
    - Set `<body className="font-[var(--font-heebo)] bg-background text-foreground antialiased">`
    - Import `./globals.css`
    - Metadata: title "ChemoSys - חמו אהרון", description "מערכת ניהול פנימית"

    2. **Global CSS** (`src/app/globals.css`):
    - Keep the Tailwind v4 imports that shadcn/ui init created (`@import "tailwindcss"` and `@import "tw-animate-css"`)
    - Inside the existing `@theme inline` block, add/ensure:
      ```
      --color-brand-primary: #4ECDC4;
      --color-brand-dark: #1B3A4B;
      --color-brand-bg: #F5F7FA;
      --color-brand-card: #FFFFFF;
      --color-brand-success: #27AE60;
      --color-brand-warning: #F2994A;
      --color-brand-danger: #EB5757;
      --color-sidebar-bg: #1B3A4B;
      --color-sidebar-text: #E8EAED;
      --color-sidebar-hover: #2A4F63;
      --color-sidebar-active: #4ECDC4;
      --font-sans: var(--font-heebo);
      ```
    - Keep shadcn/ui's existing CSS variable definitions for --background, --foreground, etc. but update:
      - `--background: #F5F7FA` (brand bg)
      - `--primary: #4ECDC4` (brand primary/turquoise)
      - `--primary-foreground: #FFFFFF`
    - IMPORTANT: Work WITH shadcn/ui's existing globals.css structure, do not replace it. Add brand tokens alongside.

    3. **Landing Page** (`src/app/page.tsx`):
    - Simple redirect to `/login` using `redirect()` from `next/navigation`. This is a server component — just call redirect at the top.

    4. **Supabase Server Client** (`src/lib/supabase/server.ts`):
    - MUST use `await cookies()` (Next.js 16 requirement)
    - Use `createServerClient` from `@supabase/ssr`
    - Use `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` env vars
    - Implement `getAll`/`setAll` cookie pattern exactly as in research Pattern 1
    - Export: `async function createClient()`

    5. **Supabase Browser Client** (`src/lib/supabase/browser.ts`):
    - Use `createBrowserClient` from `@supabase/ssr`
    - Same env vars
    - Export: `function createClient()` (synchronous, returns singleton)

    6. **Proxy** (`proxy.ts` at project root, NOT in src/):
    - File must be named `proxy.ts` (Next.js 16 — NOT middleware.ts)
    - Export `async function proxy(request: NextRequest)` — function MUST be named `proxy`
    - Create Supabase client with cookie read/write via request/response
    - Call `supabase.auth.getUser()` (NOT getClaims — getUser refreshes tokens in proxy context)
    - Redirect unauthenticated users from `/admin/*` to `/login`
    - Redirect authenticated users from `/login` to `/admin/companies`
    - Export `config.matcher` to exclude static files: `['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)']`

    IMPORTANT RTL RULES for all code in this project:
    - Use ONLY logical Tailwind utilities: `ps-`, `pe-`, `ms-`, `me-`, `start-`, `end-`
    - NEVER use physical: `pl-`, `pr-`, `ml-`, `mr-`, `left-`, `right-`
  </action>
  <verify>
    - `npm run build` compiles without TypeScript errors
    - `src/app/layout.tsx` contains `dir="rtl"` and `lang="he"` and Heebo font
    - `src/app/globals.css` contains brand color tokens
    - `src/lib/supabase/server.ts` contains `await cookies()`
    - `proxy.ts` exists at project root with `export async function proxy`
    - Visiting `http://localhost:3000` redirects to `/login`
  </verify>
  <done>Root layout renders Hebrew RTL with Heebo font, brand colors are available as Tailwind theme tokens, Supabase server and browser clients are ready to use, proxy.ts guards admin routes.</done>
</task>

</tasks>

<verification>
- `npm run dev` shows the app running
- `npm run build` succeeds with zero errors
- Browser dev tools confirm `<html lang="he" dir="rtl">` in DOM
- Network tab shows Heebo font loaded (check for woff2 request)
- Tailwind classes `bg-brand-primary`, `text-sidebar-text` etc. resolve correctly
- `proxy.ts` exists at project root (not `middleware.ts`)
- No `pl-`, `pr-`, `ml-`, `mr-`, `left-`, `right-` in any component file
</verification>

<success_criteria>
FOUND-01: Next.js configured with RTL Hebrew, Heebo font, Chemo Aharon brand colors
FOUND-05 (partial): Root layout + CSS foundation ready for sidebar layout (actual sidebar built in 01-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
