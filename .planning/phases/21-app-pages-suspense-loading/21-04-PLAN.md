---
phase: 21-app-pages-suspense-loading
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app/fleet/drivers/DriverCardSkeleton.tsx
  - src/app/(app)/app/fleet/driver-card/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Opening /app/fleet/driver-card/[id] shows a card skeleton immediately — never a blank screen"
    - "Skeleton mirrors driver card layout: breadcrumb + header with avatar/name/buttons + 5 tab strip + content area"
    - "Animated shimmer bar at top signals the system is loading"
  artifacts:
    - path: "src/components/app/fleet/drivers/DriverCardSkeleton.tsx"
      provides: "Skeleton for driver card detail page"
      contains: "shimmer"
    - path: "src/app/(app)/app/fleet/driver-card/[id]/page.tsx"
      provides: "Suspense wrapper with DriverCardSkeleton fallback"
      contains: "Suspense"
  key_links:
    - from: "src/app/(app)/app/fleet/driver-card/[id]/page.tsx"
      to: "src/components/app/fleet/drivers/DriverCardSkeleton.tsx"
      via: "Suspense fallback prop"
      pattern: "fallback=.*DriverCardSkeleton"
---

<objective>
DriverCard detail page gets Suspense boundary + custom DriverCardSkeleton.

Purpose: Eliminate blank screen on driver card load — user sees card skeleton with header outline + 5 tab strip instantly.
Output: DriverCardSkeleton.tsx + refactored [id]/page.tsx with Suspense.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/performance-standard.md
@src/app/(app)/app/fleet/driver-card/[id]/page.tsx
@src/components/app/fleet/drivers/DriverCard.tsx
@src/components/app/fleet/fuel/FuelPageSkeleton.tsx
@src/app/(app)/app/fleet/fuel/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DriverCardSkeleton component</name>
  <files>src/components/app/fleet/drivers/DriverCardSkeleton.tsx</files>
  <action>
Create DriverCardSkeleton mirroring the real DriverCard layout. This is a unique layout (tabbed card, not table), so a custom skeleton is needed.

Structure to mirror:
1. **Animated shimmer bar** at very top (same pattern as FuelPageSkeleton)
2. **Breadcrumb skeleton**: right-aligned row with 2 small Skeleton blocks (w-20 h-4 + w-24 h-4)
3. **Card header** (rounded-t-2xl, bg-white, border #E2EBF4):
   - Accent bar: div h-1 with teal gradient background (same as real card: linear-gradient(to right, #4ECDC4, #3BBFB6, #2DAAA1, #4ECDC4))
   - Inside px-5 py-4, flex row sm:items-center sm:justify-between:
     - Right side (action buttons): 4 skeleton button blocks (h-9 w-16 rounded-lg) for Back, PDF, SMS, Delete
     - Left side (identity): avatar skeleton (w-12 h-12 rounded-xl) + name skeleton (h-7 w-36) + subtitle skeleton (h-4 w-48)
4. **Tab strip** (bg-white, border-x border-b #E2EBF4):
   - 5 tab placeholder blocks in a horizontal flex row (h-10 w-24 each) for: פרטי הנהג, רשיון נהיגה, מסמכים, תרבות נהיגה, לוג נסיעות
5. **Tab content area** (bg-white, rounded-b-2xl, border #E2EBF4, min-h-[300px]):
   - A few Skeleton rows simulating form fields (4 rows of label+value placeholders)

Container: max-w-4xl mx-auto w-full, dir="rtl" (NOTE: DriverCard uses max-w-4xl)
Use shadcn Skeleton component.
Include inline style block for @keyframes shimmer.

NOTE: Tab loading indicators (LOAD-04) — DriverCard tabs currently don't lazy-load data on tab switch (all data fetched upfront via Promise.all in page.tsx). Therefore no tab-switch loading indicator is needed. If tabs are later refactored to lazy-load, add useTransition + LoadingIndicator at that point.
  </action>
  <verify>File exists and exports DriverCardSkeleton. TypeScript compiles: `npx tsc --noEmit --pretty 2>&1 | grep -i "DriverCardSkeleton" || echo "clean"`</verify>
  <done>DriverCardSkeleton renders shimmer bar + breadcrumb + header with avatar + 5-tab strip + content area skeleton</done>
</task>

<task type="auto">
  <name>Task 2: Refactor driver-card/[id]/page.tsx with Suspense boundary</name>
  <files>src/app/(app)/app/fleet/driver-card/[id]/page.tsx</files>
  <action>
Refactor the driver card detail page.tsx to follow the fuel page Suspense pattern:

1. **page.tsx stays synchronous** (aside from auth + params) — only auth check + Suspense wrapper
2. **Create inner async component `DriverCardContent`** that receives `id` as prop and moves ALL data fetching into it (getDriverById, getDriverLicense, getDriverDocuments, getDriverViolations, env reads for yellowDays/redDays/docYellowDays)
3. **Keep `notFound()`** inside DriverCardContent (if driver not found)
4. **Wrap with Suspense** using DriverCardSkeleton as fallback

The page component should look like:
```tsx
export default async function DriverCardDetailPage({ params }: Props) {
  await verifyAppUser()
  const { id } = await params

  return (
    <Suspense fallback={<DriverCardSkeleton />}>
      <DriverCardContent id={id} />
    </Suspense>
  )
}
```

The DriverCardContent async component contains all the existing Promise.all fetching + env reads, and returns `<DriverCard ...props />`.
  </action>
  <verify>
Run `npm run build 2>&1 | tail -20` — page compiles without errors.
Navigate to /app/fleet/driver-card/[any-id] — skeleton appears during load, then real card replaces it.
  </verify>
  <done>Driver card detail page shows DriverCardSkeleton during initial load, then displays real driver card</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. DriverCardSkeleton.tsx contains shimmer animation and 5-tab strip skeleton
3. [id]/page.tsx contains `<Suspense fallback={<DriverCardSkeleton />}>`
4. [id]/page.tsx has inner async `DriverCardContent` component with all data fetching
5. notFound() still works for invalid driver IDs
</verification>

<success_criteria>
- Opening /app/fleet/driver-card/[id] shows animated skeleton immediately (no blank screen)
- Skeleton layout mirrors real card: breadcrumb + header + 5 tabs + content area
- After data loads, real DriverCard replaces the skeleton
- SKEL-APP-04 and LOAD-04 requirements satisfied (LOAD-04: all tab data fetched upfront, no tab-switch loading needed)
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-pages-suspense-loading/21-04-SUMMARY.md`
</output>
