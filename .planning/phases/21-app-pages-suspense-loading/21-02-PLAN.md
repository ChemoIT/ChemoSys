---
phase: 21-app-pages-suspense-loading
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app/fleet/vehicles/VehicleCardSkeleton.tsx
  - src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Opening /app/fleet/vehicle-card/[id] shows a card skeleton immediately — never a blank screen"
    - "Skeleton mirrors vehicle card layout: breadcrumb + header with avatar/plate/buttons + 8 tab strip + content area"
    - "Animated shimmer bar at top signals the system is loading"
  artifacts:
    - path: "src/components/app/fleet/vehicles/VehicleCardSkeleton.tsx"
      provides: "Skeleton for vehicle card detail page"
      contains: "shimmer"
    - path: "src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx"
      provides: "Suspense wrapper with VehicleCardSkeleton fallback"
      contains: "Suspense"
  key_links:
    - from: "src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx"
      to: "src/components/app/fleet/vehicles/VehicleCardSkeleton.tsx"
      via: "Suspense fallback prop"
      pattern: "fallback=.*VehicleCardSkeleton"
---

<objective>
VehicleCard detail page gets Suspense boundary + custom VehicleCardSkeleton.

Purpose: Eliminate blank screen on vehicle card load — user sees card skeleton with header outline + 8 tab strip instantly.
Output: VehicleCardSkeleton.tsx + refactored [id]/page.tsx with Suspense.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/performance-standard.md
@src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx
@src/components/app/fleet/vehicles/VehicleCard.tsx
@src/components/app/fleet/fuel/FuelPageSkeleton.tsx
@src/app/(app)/app/fleet/fuel/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VehicleCardSkeleton component</name>
  <files>src/components/app/fleet/vehicles/VehicleCardSkeleton.tsx</files>
  <action>
Create VehicleCardSkeleton mirroring the real VehicleCard layout. This is a unique layout (not a table), so a custom skeleton is needed (PageSkeleton won't work here).

Structure to mirror:
1. **Animated shimmer bar** at very top of the component (same pattern as FuelPageSkeleton)
2. **Breadcrumb skeleton**: right-aligned row with 2 small Skeleton blocks (w-20 h-4 + w-16 h-4)
3. **Card header** (rounded-t-2xl, bg-white, border #E2EBF4):
   - Accent bar: div h-1 with teal gradient background (same as real card)
   - Inside px-5 py-4, flex row sm:items-center sm:justify-between:
     - Right side (action buttons): 2 skeleton button blocks (h-9 w-16 rounded-lg)
     - Left side (identity): avatar skeleton (w-12 h-12 rounded-xl) + title skeleton (h-8 w-40) + subtitle skeleton (h-4 w-56)
4. **Tab strip** (bg-white, border-x border-b #E2EBF4):
   - 8 tab placeholder blocks in a horizontal row (h-10 w-20 each, with gap-0, inside a flex container)
5. **Tab content area** (bg-white, rounded-b-2xl, border #E2EBF4, min-h-[400px]):
   - A few Skeleton rows simulating form fields (3 rows of label+value placeholders, each h-5 with varying widths)

Container: max-w-[calc(100%-6cm)] mx-auto w-full, dir="rtl"
Use shadcn Skeleton component.
Include inline style block for @keyframes shimmer.

NOTE: Tab loading indicators (LOAD-03) — VehicleCard tabs currently don't lazy-load data on tab switch (all data fetched upfront via Promise.all in page.tsx). Therefore no tab-switch loading indicator is needed. If tabs are later refactored to lazy-load, add useTransition + LoadingIndicator at that point.
  </action>
  <verify>File exists and exports VehicleCardSkeleton. TypeScript compiles: `npx tsc --noEmit --pretty 2>&1 | grep -i "VehicleCardSkeleton" || echo "clean"`</verify>
  <done>VehicleCardSkeleton renders shimmer bar + breadcrumb + header with avatar + 8-tab strip + content area skeleton</done>
</task>

<task type="auto">
  <name>Task 2: Refactor vehicle-card/[id]/page.tsx with Suspense boundary</name>
  <files>src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx</files>
  <action>
Refactor the vehicle card detail page.tsx to follow the fuel page Suspense pattern:

1. **page.tsx stays synchronous** (aside from auth + params) — only auth check + Suspense wrapper
2. **Create inner async component `VehicleCardContent`** that receives `id` as prop and moves ALL data fetching into it (getVehicleById, getVehicleTests, getVehicleInsurance, getVehicleDocuments, getVehicleDriverJournal, getVehicleProjectJournal, getVehicleMonthlyCosts, companies query, expiry computations, env reads)
3. **Keep `notFound()`** inside VehicleCardContent (if vehicle not found)
4. **Wrap with Suspense** using VehicleCardSkeleton as fallback

The page component should look like:
```tsx
export default async function VehicleCardDetailPage({ params }: Props) {
  await verifyAppUser()
  const { id } = await params

  return (
    <Suspense fallback={<VehicleCardSkeleton />}>
      <VehicleCardContent id={id} />
    </Suspense>
  )
}
```

The VehicleCardContent async component contains all the existing Promise.all fetching + expiry computations + companies query + env reads, and returns `<VehicleCard ...props />`.

IMPORTANT: Do NOT wrap in a `<div className="p-4 sm:p-6" dir="rtl">` — VehicleCard already handles its own max-width and the parent fleet layout provides padding. Match the existing page structure.
  </action>
  <verify>
Run `npm run build 2>&1 | tail -20` — page compiles without errors.
Navigate to /app/fleet/vehicle-card/[any-id] — skeleton appears during load, then real card replaces it.
  </verify>
  <done>Vehicle card detail page shows VehicleCardSkeleton during initial load, then displays real vehicle card</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. VehicleCardSkeleton.tsx contains shimmer animation and 8-tab strip skeleton
3. [id]/page.tsx contains `<Suspense fallback={<VehicleCardSkeleton />}>`
4. [id]/page.tsx has inner async `VehicleCardContent` component with all data fetching
5. notFound() still works for invalid vehicle IDs
</verification>

<success_criteria>
- Opening /app/fleet/vehicle-card/[id] shows animated skeleton immediately (no blank screen)
- Skeleton layout mirrors real card: breadcrumb + header + 8 tabs + content area
- After data loads, real VehicleCard replaces the skeleton
- SKEL-APP-02 and LOAD-03 requirements satisfied (LOAD-03: all tab data fetched upfront, no tab-switch loading needed)
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-pages-suspense-loading/21-02-SUMMARY.md`
</output>
