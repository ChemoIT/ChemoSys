---
phase: 09-fleet-home
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/(app)/app/fleet/page.tsx
  - src/components/app/fleet/ComingSoon.tsx
  - src/app/(app)/app/fleet/fuel/page.tsx
  - src/app/(app)/app/fleet/tolls/page.tsx
  - src/app/(app)/app/fleet/invoices/page.tsx
  - src/app/(app)/app/fleet/ev-charging/page.tsx
  - src/app/(app)/app/fleet/charging-stations/page.tsx
  - src/app/(app)/app/fleet/rentals/page.tsx
  - src/app/(app)/app/fleet/forms/page.tsx
  - src/app/(app)/app/fleet/exceptions/page.tsx
  - src/app/(app)/app/fleet/reports/page.tsx
autonomous: false

must_haves:
  truths:
    - "Fleet home page at /app/fleet shows dashboard placeholder and 2 CTA buttons (כרטיס נהג, כרטיס רכב)"
    - "Clicking an enabled sub-module in the sidebar navigates to its page showing 'בקרוב' placeholder"
    - "All 9 sub-module routes resolve to a page (no 404)"
    - "The design feels modern with animations, icons, and clean spacing — more modern than the admin interface"
  artifacts:
    - path: "src/app/(app)/app/fleet/page.tsx"
      provides: "Fleet home page with dashboard placeholder + 2 CTA buttons"
    - path: "src/components/app/fleet/ComingSoon.tsx"
      provides: "Reusable placeholder component for sub-module pages"
    - path: "src/app/(app)/app/fleet/fuel/page.tsx"
      provides: "Fuel sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/tolls/page.tsx"
      provides: "Tolls sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/invoices/page.tsx"
      provides: "Invoices sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/ev-charging/page.tsx"
      provides: "EV charging sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/charging-stations/page.tsx"
      provides: "Charging stations sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/rentals/page.tsx"
      provides: "Rentals sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/forms/page.tsx"
      provides: "Forms sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/exceptions/page.tsx"
      provides: "Exceptions sub-module placeholder page"
    - path: "src/app/(app)/app/fleet/reports/page.tsx"
      provides: "Reports sub-module placeholder page"
  key_links:
    - from: "src/app/(app)/app/fleet/fuel/page.tsx"
      to: "src/components/app/fleet/ComingSoon.tsx"
      via: "import ComingSoon"
      pattern: "import.*ComingSoon"
    - from: "src/app/(app)/app/fleet/page.tsx"
      to: "src/components/app/fleet/FleetSidebar.tsx"
      via: "Sidebar links point to sub-module routes; fleet home is the default content"
      pattern: "href.*app/fleet"
---

<objective>
Replace the fleet placeholder page with a modern fleet home page featuring a dashboard "בקרוב" banner and 2 CTA buttons (כרטיס נהג, כרטיס רכב), then create all 9 sub-module placeholder pages using a reusable ComingSoon component. End with a visual verification checkpoint.

Purpose: Complete the fleet module user experience — users can navigate the sidebar and see placeholder content on every sub-module page. This is the visible payoff of Phase 9.

Output: Fleet home page, ComingSoon component, 9 sub-module pages, visual verification.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fleet-home/09-RESEARCH.md
@.planning/phases/09-fleet-home/09-01-SUMMARY.md
@src/app/(app)/app/fleet/layout.tsx
@src/components/app/fleet/FleetSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fleet home page + ComingSoon component + 9 sub-module placeholder pages</name>
  <files>
    src/app/(app)/app/fleet/page.tsx
    src/components/app/fleet/ComingSoon.tsx
    src/app/(app)/app/fleet/fuel/page.tsx
    src/app/(app)/app/fleet/tolls/page.tsx
    src/app/(app)/app/fleet/invoices/page.tsx
    src/app/(app)/app/fleet/ev-charging/page.tsx
    src/app/(app)/app/fleet/charging-stations/page.tsx
    src/app/(app)/app/fleet/rentals/page.tsx
    src/app/(app)/app/fleet/forms/page.tsx
    src/app/(app)/app/fleet/exceptions/page.tsx
    src/app/(app)/app/fleet/reports/page.tsx
  </files>
  <action>
1. **Create `src/components/app/fleet/ComingSoon.tsx`** — reusable placeholder for sub-module pages:
   - Props: `{ label: string; icon?: React.ComponentType<{ className?: string }> }`
   - Renders a centered card with the icon (if provided), the label in bold, "בקרוב..." subtitle, and a subtle animation
   - **Style — modern 2026 feel (per Sharon's "הכי מודרנית שיש"):**
     - White card on the light content background with rounded-2xl, shadow-lg
     - Icon rendered large (w-12 h-12) in brand-primary (#4ECDC4) with a subtle pulse animation (animate-pulse or custom)
     - Label as text-xl font-bold, "בקרוב..." as text-muted-foreground text-sm
     - Card has `transition-all duration-300 hover:shadow-xl hover:scale-[1.01]` for subtle interactivity
   - This component is used by ALL 9 sub-module pages for consistency

2. **Replace `src/app/(app)/app/fleet/page.tsx`** — fleet home page:
   - Remove the old placeholder entirely
   - New content structure:
     ```
     <div className="space-y-8">
       {/* Dashboard placeholder banner */}
       <section className="bg-brand-card rounded-2xl p-8 shadow-lg border border-border/50">
         <div className="flex items-center gap-3 mb-4">
           <LayoutDashboard icon (from lucide-react) in brand-primary color
           <h2 className="text-xl font-bold">דשבורד צי רכב</h2>
         </div>
         <p className="text-muted-foreground">הדשבורד ייבנה בהמשך — כאן יוצגו נתוני סטטוס, התראות, וסיכומים.</p>
         <div className="mt-4 text-xs text-muted-foreground/60">בקרוב...</div>
       </section>

       {/* 2 CTA buttons section */}
       <section className="grid grid-cols-1 sm:grid-cols-2 gap-4">
         {/* כרטיס נהג CTA */}
         <div className="group bg-brand-card rounded-2xl p-6 shadow-lg border border-border/50
                         hover:shadow-xl hover:border-brand-primary/30
                         transition-all duration-300 cursor-default">
           <div className="flex items-center gap-4">
             <div className="w-12 h-12 rounded-xl bg-brand-primary/10 flex items-center justify-center
                             group-hover:bg-brand-primary/20 transition-colors duration-300">
               <UserCheck icon, h-6 w-6, text-brand-primary />
             </div>
             <div>
               <h3 className="font-bold text-lg">כרטיס נהג</h3>
               <p className="text-sm text-muted-foreground">בקרוב...</p>
             </div>
           </div>
         </div>

         {/* כרטיס רכב CTA */}
         <div className="group bg-brand-card rounded-2xl p-6 shadow-lg border border-border/50
                         hover:shadow-xl hover:border-brand-primary/30
                         transition-all duration-300 cursor-default">
           <div className="flex items-center gap-4">
             <div className="w-12 h-12 rounded-xl bg-brand-primary/10 flex items-center justify-center
                             group-hover:bg-brand-primary/20 transition-colors duration-300">
               <Car icon, h-6 w-6, text-brand-primary />
             </div>
             <div>
               <h3 className="font-bold text-lg">כרטיס רכב</h3>
               <p className="text-sm text-muted-foreground">בקרוב...</p>
             </div>
           </div>
         </div>
       </section>
     </div>
     ```

   **Design notes:**
   - Cards use rounded-2xl, shadow-lg, and group-hover transitions for modern feel
   - Brand colors from globals.css: bg-brand-primary/10, text-brand-primary
   - Spacing via space-y-8 gives clean, airy layout
   - CTA cards are cursor-default (not clickable — placeholder only)
   - Icons from lucide-react: LayoutDashboard, UserCheck, Car

3. **Create 9 sub-module placeholder pages** — each is a minimal page.tsx importing ComingSoon:

   Each file follows this pattern:
   ```tsx
   import { ComingSoon } from "@/components/app/fleet/ComingSoon";
   import { IconName } from "lucide-react";

   export default function FleetXxxPage() {
     return <ComingSoon label="Hebrew Label" icon={IconName} />;
   }
   ```

   The 9 pages with their file paths, labels, and icons:
   - `src/app/(app)/app/fleet/fuel/page.tsx` — label: "דלק", icon: Fuel
   - `src/app/(app)/app/fleet/tolls/page.tsx` — label: "כבישי אגרה", icon: SquareActivity
   - `src/app/(app)/app/fleet/invoices/page.tsx` — label: "חשבוניות ספקים", icon: Receipt
   - `src/app/(app)/app/fleet/ev-charging/page.tsx` — label: "טעינת רכב חשמלי", icon: Zap
   - `src/app/(app)/app/fleet/charging-stations/page.tsx` — label: "מעקב עמדות טעינה פרטיות", icon: MapPin
   - `src/app/(app)/app/fleet/rentals/page.tsx` — label: "הזמנת רכב שכור", icon: KeyRound
   - `src/app/(app)/app/fleet/forms/page.tsx` — label: "טפסים", icon: ClipboardList
   - `src/app/(app)/app/fleet/exceptions/page.tsx` — label: "טבלת חריגים", icon: AlertTriangle
   - `src/app/(app)/app/fleet/reports/page.tsx` — label: "דוחות", icon: BarChart2

4. Verify TypeScript compilation and run dev server to confirm routes:
   ```bash
   npx tsc --noEmit
   ```
  </action>
  <verify>
    - `npx tsc --noEmit` — no TypeScript errors
    - `ls src/app/(app)/app/fleet/*/page.tsx | wc -l` — should be 9
    - `grep "ComingSoon" src/app/(app)/app/fleet/fuel/page.tsx` — imports ComingSoon
    - `grep "כרטיס נהג" src/app/(app)/app/fleet/page.tsx` — CTA button present
    - `grep "כרטיס רכב" src/app/(app)/app/fleet/page.tsx` — CTA button present
    - `grep "דשבורד צי רכב" src/app/(app)/app/fleet/page.tsx` — dashboard section present
  </verify>
  <done>
    - Fleet home page shows dashboard placeholder + 2 CTA buttons (כרטיס נהג, כרטיס רכב)
    - ComingSoon component exists and is reused by all 9 sub-module pages
    - All 9 sub-module routes exist (fuel, tolls, invoices, ev-charging, charging-stations, rentals, forms, exceptions, reports)
    - TypeScript compiles without errors
    - Modern styling with transitions, shadows, brand colors, and rounded cards
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of fleet module</name>
  <files>none — verification only</files>
  <action>
    Start dev server and perform visual verification of the complete fleet module.

    What was built:
    Complete fleet module shell: sidebar navigation with 9 sub-modules + fleet home page with dashboard placeholder and 2 CTA buttons + 9 "בקרוב" placeholder pages.

    Steps to verify:
    1. Start dev server: `npm run dev`
    2. Log in to ChemoSys at `http://localhost:3000/chemosys` with a user that has app_fleet permission
    3. Navigate to fleet module — you should see:
       - Sidebar on the RIGHT (RTL) collapsed to icons only
       - 9 sub-module icons in the sidebar
       - Hover/click the sidebar to expand and see Hebrew labels
    4. On the fleet home page (`/app/fleet`), verify:
       - Dashboard placeholder banner at top ("דשבורד צי רכב" + "בקרוב...")
       - 2 CTA cards below: "כרטיס נהג" and "כרטיס רכב" — both with "בקרוב..."
    5. Click on any enabled sub-module in sidebar (e.g., דלק):
       - Should navigate to `/app/fleet/fuel`
       - Should show ComingSoon placeholder with icon + label + "בקרוב..."
    6. Check disabled sub-modules (if your test user has limited permissions):
       - Should be grayed out (opacity-40)
       - Should NOT be clickable or keyboard-focusable
    7. Check mobile (resize to ~375px width):
       - Sidebar should become a hamburger/overlay (Sheet)
       - Content should remain readable
    8. Overall: design should feel modern — rounded cards, smooth transitions, brand colors
  </action>
  <verify>Sharon visually approves the fleet module at http://localhost:3000/app/fleet</verify>
  <done>Sharon types "approved" confirming fleet module looks correct, or describes issues to fix</done>
</task>

</tasks>

<verification>
1. All 9 sub-module routes return valid pages (no 404)
2. Fleet home page has dashboard placeholder + 2 CTA buttons
3. ComingSoon component renders consistently across all sub-module pages
4. Modern design with brand colors, transitions, shadows
5. TypeScript compiles without errors
6. Sharon approves the visual output
</verification>

<success_criteria>
- Fleet home page at /app/fleet shows dashboard placeholder and 2 primary CTA buttons
- All 9 sub-module pages render ComingSoon placeholder with correct icon and Hebrew label
- Design uses rounded-2xl cards, shadow-lg, brand colors, and hover transitions
- No 404 errors on any sub-module route
- Mobile responsive (sidebar becomes overlay on small screens)
- Sharon visually approves the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/09-fleet-home/09-02-SUMMARY.md`
</output>
