---
phase: 11-phase-10a-vehicle-card-database-storage-vehicle-suppliers-tables
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - supabase/migrations/00026_fleet_vehicles_storage_policies.sql
autonomous: false

must_haves:
  truths:
    - "Storage bucket fleet-vehicle-documents exists in Supabase (Private visibility)"
    - "Storage RLS policies allow authenticated users to INSERT/SELECT/UPDATE/DELETE in fleet-vehicle-documents bucket"
    - "Migration 00025 ran successfully — all 6 tables exist in Supabase"
    - "Migration 00026 ran successfully — storage policies active"
    - "vehicle_computed_status view returns rows"
    - "driver_computed_status view still works correctly (no regression)"
  artifacts:
    - path: "supabase/migrations/00026_fleet_vehicles_storage_policies.sql"
      provides: "Storage RLS policies for fleet-vehicle-documents bucket"
      contains: "fleet-vehicle-documents"
  key_links:
    - from: "vehicle_documents.file_url"
      to: "fleet-vehicle-documents bucket"
      via: "Supabase Storage signed URLs"
      pattern: "fleet-vehicle-documents"
    - from: "vehicle_tests.file_url"
      to: "fleet-vehicle-documents bucket"
      via: "Supabase Storage signed URLs"
      pattern: "fleet-vehicle-documents"
---

<objective>
Create the storage policies migration `00026_fleet_vehicles_storage_policies.sql` and verify both migrations (00025 + 00026) run successfully in Supabase.

Purpose: Complete the database foundation by adding storage access and verifying the entire vehicle schema is operational.

Output: `supabase/migrations/00026_fleet_vehicles_storage_policies.sql` + verified migrations in Supabase.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-phase-10a-vehicle-card-database-storage-vehicle-suppliers-tables/11-RESEARCH.md
@.planning/phases/11-phase-10a-vehicle-card-database-storage-vehicle-suppliers-tables/11-01-SUMMARY.md
@supabase/migrations/00019_fleet_storage_policies.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create storage policies migration file</name>
  <files>supabase/migrations/00026_fleet_vehicles_storage_policies.sql</files>
  <action>
Create `supabase/migrations/00026_fleet_vehicles_storage_policies.sql` following the exact pattern from `00019_fleet_storage_policies.sql`.

Header comment:
```sql
-- Migration: 00026_fleet_vehicles_storage_policies.sql
-- Purpose: Storage RLS policies for fleet-vehicle-documents bucket
-- PREREQUISITE: Create bucket "fleet-vehicle-documents" (Private) in Supabase Dashboard -> Storage BEFORE running this migration
-- Depends on: 00025_fleet_vehicles (tables that reference this bucket)
```

4 storage policies using `DROP POLICY IF EXISTS` + `CREATE POLICY` pattern:
1. `authenticated_insert_fleet_vehicle_documents` ON storage.objects FOR INSERT TO authenticated WITH CHECK (bucket_id = 'fleet-vehicle-documents')
2. `authenticated_select_fleet_vehicle_documents` ON storage.objects FOR SELECT TO authenticated USING (bucket_id = 'fleet-vehicle-documents')
3. `authenticated_update_fleet_vehicle_documents` ON storage.objects FOR UPDATE TO authenticated USING (bucket_id = 'fleet-vehicle-documents')
4. `authenticated_delete_fleet_vehicle_documents` ON storage.objects FOR DELETE TO authenticated USING (bucket_id = 'fleet-vehicle-documents')

Keep the file minimal — storage policies only, no table definitions.
  </action>
  <verify>File exists with 4 DROP+CREATE policy pairs. All reference bucket_id = 'fleet-vehicle-documents'. Policy names follow established naming convention.</verify>
  <done>Storage policies migration file complete, idempotent, ready to run after bucket creation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run migrations in Supabase and verify</name>
  <files>supabase/migrations/00025_fleet_vehicles.sql, supabase/migrations/00026_fleet_vehicles_storage_policies.sql</files>
  <action>
Sharon runs both migrations in Supabase SQL Editor and verifies the results. This is a human-verify checkpoint — Claude cannot access Supabase Dashboard.

**What was built:** Two migration files:
1. `00025_fleet_vehicles.sql` — 6 tables, 2 views, 9 RPCs, 18+ RLS policies, indexes, triggers
2. `00026_fleet_vehicles_storage_policies.sql` — 4 storage bucket policies

**Steps for Sharon:**

**Step 1: Create storage bucket**
1. Go to Supabase Dashboard -> Storage
2. Click "New Bucket"
3. Name: `fleet-vehicle-documents`
4. Visibility: Private (toggle OFF for public)
5. Click "Create"

**Step 2: Run migration 00025**
1. Go to Supabase Dashboard -> SQL Editor
2. Open `supabase/migrations/00025_fleet_vehicles.sql`
3. Copy entire contents and paste into SQL Editor
4. Click "Run" — should complete without errors

**Step 3: Run migration 00026**
1. Open `supabase/migrations/00026_fleet_vehicles_storage_policies.sql`
2. Copy and paste into SQL Editor
3. Click "Run" — should complete without errors

**Step 4: Verify tables exist**
1. Go to Table Editor
2. Confirm these tables appear: `vehicle_suppliers`, `vehicles`, `vehicle_tests`, `vehicle_insurance`, `vehicle_documents`, `vehicle_document_names`
3. Each table should have 0 rows (empty, ready for data)

**Step 5: Verify views**
1. In SQL Editor, run: `SELECT * FROM vehicle_computed_status LIMIT 1;` — should return 0 rows (no vehicles yet), no error
2. Run: `SELECT * FROM driver_computed_status LIMIT 5;` — should return existing driver statuses (regression check)

**Step 6: Verify storage**
1. Go to Storage -> fleet-vehicle-documents
2. Bucket should be visible and empty
  </action>
  <verify>Sharon confirms all 6 steps pass without errors.</verify>
  <done>Both migrations run successfully in Supabase. All 6 tables, 2 views, 9 RPCs, storage bucket, and storage policies are operational. Type "approved" or describe any errors.</done>
</task>

</tasks>

<verification>
1. `supabase/migrations/00026_fleet_vehicles_storage_policies.sql` exists with 4 storage policies
2. Both migrations (00025 + 00026) run without errors in Supabase SQL Editor
3. All 6 tables visible in Table Editor
4. Both views return results without errors
5. Storage bucket fleet-vehicle-documents exists and is accessible
</verification>

<success_criteria>
- Storage policies migration written and idempotent
- Both migrations verified running in Supabase by Sharon
- Vehicle module database foundation is complete and operational
</success_criteria>

<output>
After completion, create `.planning/phases/11-phase-10a-vehicle-card-database-storage-vehicle-suppliers-tables/11-02-SUMMARY.md`
</output>
