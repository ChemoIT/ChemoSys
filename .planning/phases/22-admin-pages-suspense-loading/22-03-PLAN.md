---
phase: 22-admin-pages-suspense-loading
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/audit-log/page.tsx
  - src/components/admin/audit-log/AuditLogSkeleton.tsx
  - src/components/admin/audit-log/AuditLogFilters.tsx
  - src/components/admin/audit-log/AuditLogTable.tsx
autonomous: true

must_haves:
  truths:
    - "Audit Log מציג skeleton עם placeholders לפילטרים + שורות טבלה בזמן טעינה ראשונית"
    - "שינוי filter או pagination ב-audit log מציג spinner + טקסט 'מעדכן נתונים...' — לא refresh שקט"
    - "Animated shimmer bar מופיע בראש ה-skeleton בזמן טעינה"
  artifacts:
    - path: "src/components/admin/audit-log/AuditLogSkeleton.tsx"
      provides: "Audit log skeleton with filter bar + table rows"
      contains: "AuditLogSkeleton"
    - path: "src/app/(admin)/admin/audit-log/page.tsx"
      provides: "Audit log page with Suspense boundary"
      contains: "Suspense"
    - path: "src/components/admin/audit-log/AuditLogFilters.tsx"
      provides: "Filters with useTransition loading state"
      contains: "useTransition"
    - path: "src/components/admin/audit-log/AuditLogTable.tsx"
      provides: "Table with useTransition for pagination"
      contains: "useTransition"
  key_links:
    - from: "src/app/(admin)/admin/audit-log/page.tsx"
      to: "src/components/admin/audit-log/AuditLogSkeleton.tsx"
      via: "Suspense fallback"
      pattern: "<Suspense fallback=.*AuditLogSkeleton"
    - from: "src/components/admin/audit-log/AuditLogFilters.tsx"
      to: "src/components/shared/LoadingIndicator.tsx"
      via: "isPending prop"
      pattern: "LoadingIndicator"
---

<objective>
Audit Log page gets Suspense boundary + custom skeleton + loading indicators on filter/pagination changes.

Purpose: Audit Log is the only admin page with server-side filtering and pagination via URL search params (router.push). It needs both initial-load skeleton AND in-page loading indicators when filters change. This is the LOAD-05 requirement.
Output: AuditLogSkeleton component, refactored page with Suspense, useTransition in filters and table pagination.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(admin)/admin/audit-log/page.tsx
@src/components/admin/audit-log/AuditLogTable.tsx
@src/components/admin/audit-log/AuditLogFilters.tsx
@src/components/shared/LoadingIndicator.tsx
@src/components/shared/PageSkeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditLogSkeleton + Suspense boundary on page</name>
  <files>src/components/admin/audit-log/AuditLogSkeleton.tsx, src/app/(admin)/admin/audit-log/page.tsx</files>
  <action>
**Part A — Create AuditLogSkeleton:**

Create `src/components/admin/audit-log/AuditLogSkeleton.tsx` server component:

1. Animated shimmer progress bar at top (same pattern as PageSkeleton)
2. Header: title placeholder (h-7 w-28) + refresh button placeholder (h-8 w-8)
3. Filter bar: 5 filter placeholders in a flex-wrap row — entity type dropdown (w-36 h-9), action dropdown (w-36 h-9), date from (w-32 h-9), date to (w-32 h-9), search (w-48 h-9) — all rounded-full
4. Table skeleton: 7 columns matching audit log layout [70, 80, 80, 80, 120, 60, 60], 10 rows, with pagination footer
5. Use `Skeleton` from `@/components/ui/skeleton`
6. Include inline `<style>` for shimmer keyframes
7. Wrap in `<div className="space-y-5">` — no maxWidth constraint (admin is full-width)

**Part B — Wrap audit-log page with Suspense:**

1. Extract ALL async logic from `AuditLogPage` into `async function AuditLogContent({ searchParams })`. The Content function receives the `searchParams` prop (Promise<SearchParams>) and contains everything after verifySession().
2. Main export becomes:
```tsx
export default async function AuditLogPage({
  searchParams,
}: {
  searchParams: Promise<SearchParams>
}) {
  await verifySession()
  return (
    <Suspense fallback={<AuditLogSkeleton />}>
      <AuditLogContent searchParams={searchParams} />
    </Suspense>
  )
}
```
3. Add imports: `Suspense` from react, `AuditLogSkeleton` from the new file.
4. IMPORTANT: Pass `searchParams` through to `AuditLogContent` — the content function needs it for server-side filtering.
  </action>
  <verify>`npx tsc --noEmit` passes. AuditLogSkeleton file exists. Page has Suspense boundary. searchParams correctly passed to content.</verify>
  <done>Audit log shows skeleton with filter placeholders + table rows during initial load and during filter-triggered re-renders.</done>
</task>

<task type="auto">
  <name>Task 2: Loading indicators on audit log filter and pagination changes</name>
  <files>src/components/admin/audit-log/AuditLogFilters.tsx, src/components/admin/audit-log/AuditLogTable.tsx</files>
  <action>
**Part A — AuditLogFilters.tsx — add useTransition + LoadingIndicator:**

1. Import `useTransition` from react and `LoadingIndicator` from `@/components/shared/LoadingIndicator`.
2. Add `const [isPending, startTransition] = useTransition()` at the top of the component.
3. Wrap ALL `router.push()` calls in `startTransition(() => { ... })`. There are calls in:
   - The apply filters handler (the function that builds URL params and pushes)
   - The reset/clear filters handler
4. Add `<LoadingIndicator isLoading={isPending} />` below the filter bar (after the flex-wrap container of filter inputs, before the table).

**Part B — AuditLogTable.tsx — add useTransition on pagination:**

1. Import `useTransition` from react and `LoadingIndicator` from `@/components/shared/LoadingIndicator`.
2. Add `const [isPending, startTransition] = useTransition()` in the component.
3. Wrap the pagination `router.push()` call in `startTransition(() => { ... })`.
4. Add `<LoadingIndicator isLoading={isPending} size="sm" />` near the pagination controls area.
5. Optionally add `className={isPending ? 'opacity-60 pointer-events-none' : ''}` on the table container for visual feedback during page transitions.
  </action>
  <verify>`npx tsc --noEmit` passes. Both files import useTransition and LoadingIndicator. All router.push calls are wrapped in startTransition.</verify>
  <done>Changing filters or clicking pagination in audit log shows spinner + "מעדכן נתונים..." text — user gets clear feedback during server re-render.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors
2. Audit log page has `<Suspense fallback={<AuditLogSkeleton />}>` wrapping async content
3. AuditLogSkeleton has shimmer bar + filter placeholders + table rows
4. AuditLogFilters.tsx wraps router.push in startTransition and shows LoadingIndicator
5. AuditLogTable.tsx wraps pagination router.push in startTransition and shows LoadingIndicator
</verification>

<success_criteria>
Audit log page shows skeleton on initial load. Filter/pagination changes show spinner + Hebrew loading text. No silent page refreshes.
</success_criteria>

<output>
After completion, create `.planning/phases/22-admin-pages-suspense-loading/22-03-SUMMARY.md`
</output>
