---
phase: 22-admin-pages-suspense-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/dashboard/page.tsx
  - src/components/admin/dashboard/DashboardSkeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard מציג skeleton של 6 stat cards + activity feed בזמן טעינה — לא מסך ריק"
    - "Animated shimmer progress bar מופיע בראש הדף בזמן טעינה"
  artifacts:
    - path: "src/components/admin/dashboard/DashboardSkeleton.tsx"
      provides: "Dashboard skeleton with 6 stat card placeholders + activity feed skeleton"
      contains: "DashboardSkeleton"
    - path: "src/app/(admin)/admin/dashboard/page.tsx"
      provides: "Dashboard page with Suspense boundary"
      contains: "Suspense"
  key_links:
    - from: "src/app/(admin)/admin/dashboard/page.tsx"
      to: "src/components/admin/dashboard/DashboardSkeleton.tsx"
      via: "Suspense fallback"
      pattern: "<Suspense fallback=.*DashboardSkeleton"
---

<objective>
Dashboard page gets Suspense boundary + custom DashboardSkeleton with 6 stat cards placeholders and activity feed skeleton.

Purpose: Dashboard has unique layout (6 cards grid + activity feed list) — needs a custom skeleton, not PageSkeleton. This is the heaviest admin page (7+ DB queries) so Suspense gives the biggest UX win here.
Output: DashboardSkeleton component + refactored dashboard page with Suspense wrapping.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(admin)/admin/dashboard/page.tsx
@src/components/shared/PageSkeleton.tsx
@src/app/(app)/app/fleet/fuel/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DashboardSkeleton component</name>
  <files>src/components/admin/dashboard/DashboardSkeleton.tsx</files>
  <action>
Create `DashboardSkeleton` server component that mirrors the real dashboard layout:

1. Animated shimmer progress bar at top (same pattern as PageSkeleton — `w-1/3 bg-sky-500/70 rounded-full animate-[shimmer_1.5s_ease-in-out_infinite]`)
2. Header: skeleton title "דשבורד" placeholder (h-7 w-24) + refresh button placeholder (h-8 w-8)
3. Stats cards grid: 6 skeleton cards in `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` — each card is a `rounded-2xl bg-white border` box with:
   - Top: icon placeholder (h-10 w-10 rounded-full) + label placeholder (h-4 w-20)
   - Bottom: number placeholder (h-8 w-16)
   - Height: ~120px each
4. Activity feed section: skeleton title "פעילות אחרונה" placeholder (h-6 w-32), then 8 rows of:
   - Avatar circle (h-8 w-8 rounded-full) + action text (h-4 w-48) + timestamp (h-3 w-20)
   - Separated by light borders

Include the inline `<style>` tag with the shimmer keyframes (same as PageSkeleton).
Do NOT use `'use client'` — this is a server component.
Import `Skeleton` from `@/components/ui/skeleton`.
  </action>
  <verify>File exists at `src/components/admin/dashboard/DashboardSkeleton.tsx`, exports `DashboardSkeleton`, and imports `Skeleton`.</verify>
  <done>DashboardSkeleton component renders 6 card placeholders + 8 activity feed rows + shimmer bar, matching real dashboard layout.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap Dashboard page with Suspense boundary</name>
  <files>src/app/(admin)/admin/dashboard/page.tsx</files>
  <action>
Refactor the dashboard page following the fuel page Suspense pattern:

1. Extract the async data-fetching logic into a separate `async function DashboardContent()` component (same pattern as `FuelContent` in fuel/page.tsx). Move ALL code from `DashboardPage` into `DashboardContent` EXCEPT `verifySession()`.

2. The main `DashboardPage` export becomes:
```tsx
export default async function DashboardPage() {
  await verifySession()
  return (
    <Suspense fallback={<DashboardSkeleton />}>
      <DashboardContent />
    </Suspense>
  )
}
```

3. Add imports: `import { Suspense } from 'react'` and `import { DashboardSkeleton } from '@/components/admin/dashboard/DashboardSkeleton'`

4. Keep the `verifySession()` call OUTSIDE the Suspense boundary (in DashboardPage) so auth runs immediately, not deferred.

5. `DashboardContent` keeps all existing logic unchanged — it's just moved into its own function.
  </action>
  <verify>File compiles without TypeScript errors. `DashboardPage` has Suspense with DashboardSkeleton fallback. `verifySession()` is outside Suspense.</verify>
  <done>Dashboard page wrapped in Suspense — skeleton shows during data fetch, auth guard runs immediately.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors
2. Dashboard page has `<Suspense fallback={<DashboardSkeleton />}>` wrapping async content
3. `verifySession()` is called before Suspense boundary
4. DashboardSkeleton has shimmer bar + 6 card placeholders + activity feed skeleton
</verification>

<success_criteria>
Dashboard displays skeleton with 6 stat cards + activity feed placeholders while data loads. No blank screen during fetch.
</success_criteria>

<output>
After completion, create `.planning/phases/22-admin-pages-suspense-loading/22-01-SUMMARY.md`
</output>
