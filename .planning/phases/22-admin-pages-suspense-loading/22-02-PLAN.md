---
phase: 22-admin-pages-suspense-loading
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/projects/page.tsx
  - src/app/(admin)/admin/users/page.tsx
  - src/app/(admin)/admin/templates/page.tsx
  - src/app/(admin)/admin/vehicle-suppliers/page.tsx
autonomous: true

must_haves:
  truths:
    - "דפי Projects, Users, Templates, VehicleSuppliers מציגים skeleton table בזמן טעינה — לא מסך ריק"
    - "כל 4 הדפים עטופים ב-Suspense עם PageSkeleton fallback"
    - "verifySession() רץ מחוץ ל-Suspense boundary בכל דף"
  artifacts:
    - path: "src/app/(admin)/admin/projects/page.tsx"
      provides: "Projects page with Suspense boundary"
      contains: "Suspense"
    - path: "src/app/(admin)/admin/users/page.tsx"
      provides: "Users page with Suspense boundary"
      contains: "Suspense"
    - path: "src/app/(admin)/admin/templates/page.tsx"
      provides: "Templates page with Suspense boundary"
      contains: "Suspense"
    - path: "src/app/(admin)/admin/vehicle-suppliers/page.tsx"
      provides: "Vehicle Suppliers page with Suspense boundary"
      contains: "Suspense"
  key_links:
    - from: "src/app/(admin)/admin/projects/page.tsx"
      to: "src/components/shared/PageSkeleton.tsx"
      via: "Suspense fallback"
      pattern: "<Suspense fallback=.*PageSkeleton"
---

<objective>
Four admin table pages (Projects, Users, Templates, VehicleSuppliers) get Suspense boundaries with PageSkeleton fallbacks.

Purpose: These pages all follow the same pattern — async server component fetching data then rendering a table. They can all use the shared PageSkeleton component with appropriate configs. Grouping them saves context budget since the transformation is identical.
Output: 4 refactored page.tsx files, each with Suspense + PageSkeleton.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/shared/PageSkeleton.tsx
@src/app/(app)/app/fleet/fuel/page.tsx
@src/app/(admin)/admin/projects/page.tsx
@src/app/(admin)/admin/users/page.tsx
@src/app/(admin)/admin/templates/page.tsx
@src/app/(admin)/admin/vehicle-suppliers/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Projects + Users pages — Suspense + PageSkeleton</name>
  <files>src/app/(admin)/admin/projects/page.tsx, src/app/(admin)/admin/users/page.tsx</files>
  <action>
For EACH page (projects, users), apply the same Suspense refactor pattern:

**Projects page (`projects/page.tsx`):**
1. Extract all async logic from `ProjectsPage` into `async function ProjectsContent()` — move everything after `verifySession()` into it.
2. Main export becomes:
```tsx
export default async function ProjectsPage() {
  await verifySession()
  return (
    <Suspense fallback={<PageSkeleton config={{
      titleWidth: 120,
      table: { columns: [100, 120, 80, 80, 80, 80, 60], rows: 8 }
    }} />}>
      <ProjectsContent />
    </Suspense>
  )
}
```
3. Add imports: `Suspense` from react, `PageSkeleton` from `@/components/shared/PageSkeleton`.
4. Remove `maxWidth` from PageSkeleton config (admin pages are full-width, no `max-w-[calc(100%-6cm)]`). Pass `maxWidth: "max-w-full"`.

**Users page (`users/page.tsx`):**
1. Same pattern — extract into `async function UsersContent()`.
2. PageSkeleton config:
```tsx
<PageSkeleton config={{
  titleWidth: 110,
  table: { columns: [100, 80, 100, 80, 60, 80], rows: 8 },
  maxWidth: "max-w-full"
}} />
```
3. Add same imports.

IMPORTANT for both: `verifySession()` stays OUTSIDE Suspense. The `createClient()` and all DB queries go inside the Content function.
  </action>
  <verify>`npx tsc --noEmit` passes. Both pages have Suspense wrapping async content with PageSkeleton fallback.</verify>
  <done>Projects and Users pages display table skeleton during data load.</done>
</task>

<task type="auto">
  <name>Task 2: Templates + VehicleSuppliers pages — Suspense + PageSkeleton</name>
  <files>src/app/(admin)/admin/templates/page.tsx, src/app/(admin)/admin/vehicle-suppliers/page.tsx</files>
  <action>
Same Suspense refactor for both simpler pages:

**Templates page (`templates/page.tsx`):**
1. Extract async logic into `async function TemplatesContent()`.
2. Main export:
```tsx
export default async function TemplatesPage() {
  await verifySession()
  return (
    <Suspense fallback={<PageSkeleton config={{
      titleWidth: 110,
      table: { columns: [120, 200, 60], rows: 6, pagination: false },
      maxWidth: "max-w-full"
    }} />}>
      <TemplatesContent />
    </Suspense>
  )
}
```
3. Add imports.

**Vehicle Suppliers page (`vehicle-suppliers/page.tsx`):**
1. Extract into `async function VehicleSuppliersContent()`.
2. PageSkeleton config:
```tsx
<PageSkeleton config={{
  titleWidth: 100,
  table: { columns: [80, 120, 80, 80, 60], rows: 8, pagination: false },
  maxWidth: "max-w-full"
}} />
```
3. Add imports.

Both pages: `verifySession()` outside Suspense, all async work inside Content function.
  </action>
  <verify>`npx tsc --noEmit` passes. Both pages have Suspense wrapping.</verify>
  <done>Templates and VehicleSuppliers pages display skeleton during data load.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors
2. All 4 pages have `<Suspense fallback={<PageSkeleton config={...} />}>` wrapping async content
3. `verifySession()` is called before Suspense boundary in each page
4. PageSkeleton configs match the general column structure of each table
</verification>

<success_criteria>
Projects, Users, Templates, and VehicleSuppliers pages all display table skeletons during data fetch. No blank screens.
</success_criteria>

<output>
After completion, create `.planning/phases/22-admin-pages-suspense-loading/22-02-SUMMARY.md`
</output>
