---
phase: 15-phase-10f-vehiclelist-addvehicledialog-mot-api-auto-fill-pages-integration
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/app/fleet/vehicles/VehicleList.tsx
  - src/app/(app)/app/fleet/vehicle-card/page.tsx
  - src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx
autonomous: false

must_haves:
  truths:
    - "User navigating to /app/fleet/vehicle-card sees a list of all vehicles with fitness lights"
    - "User can filter vehicles by status (all/active/inactive) and fitness (all/red/yellow/green)"
    - "User can search vehicles by plate, make, model, company, or driver name"
    - "User can click a vehicle row to navigate to /app/fleet/vehicle-card/[id]"
    - "User can open AddVehicleDialog from the list page via 'רכב חדש' button"
    - "/app/fleet/vehicle-card/[id] shows ComingSoon placeholder (Phase 14 builds VehicleCard)"
  artifacts:
    - path: "src/components/app/fleet/vehicles/VehicleList.tsx"
      provides: "Vehicle list client component with filters, search, table"
      min_lines: 150
    - path: "src/app/(app)/app/fleet/vehicle-card/page.tsx"
      provides: "Server page fetching vehicles and rendering VehicleList"
      exports: ["default"]
    - path: "src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx"
      provides: "Detail route shell with ComingSoon placeholder"
      exports: ["default"]
  key_links:
    - from: "src/app/(app)/app/fleet/vehicle-card/page.tsx"
      to: "src/actions/fleet/vehicles.ts"
      via: "getVehiclesList server call"
      pattern: "getVehiclesList"
    - from: "src/app/(app)/app/fleet/vehicle-card/page.tsx"
      to: "src/components/app/fleet/vehicles/VehicleList.tsx"
      via: "renders VehicleList with vehicles prop"
      pattern: "VehicleList"
    - from: "src/components/app/fleet/vehicles/VehicleList.tsx"
      to: "src/components/app/fleet/vehicles/AddVehicleDialog.tsx"
      via: "renders AddVehicleDialog (from Plan 01)"
      pattern: "AddVehicleDialog"
    - from: "src/components/app/fleet/vehicles/VehicleList.tsx"
      to: "src/components/app/fleet/shared/VehicleFitnessLight.tsx"
      via: "renders VehicleFitnessLight per row"
      pattern: "VehicleFitnessLight"
---

<objective>
VehicleList component and page routing for the fleet vehicle module.

Purpose: Build the vehicle list UI (mirroring DriverList pattern) with fitness lights, filters, search, and the add button, then wire it into the page routes. Replace the existing ComingSoon placeholder at /app/fleet/vehicle-card with the real list, and create the [id] route shell for Phase 14.

Output: VehicleList.tsx component, updated vehicle-card/page.tsx server page, new vehicle-card/[id]/page.tsx route shell.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-phase-10f-vehiclelist-addvehicledialog-mot-api-auto-fill-pages-integration/15-01-SUMMARY.md

@src/components/app/fleet/drivers/DriverList.tsx
@src/components/app/fleet/shared/VehicleFitnessLight.tsx
@src/app/(app)/app/fleet/driver-card/page.tsx
@src/app/(app)/app/fleet/driver-card/[id]/page.tsx
@src/lib/fleet/vehicle-types.ts
@src/lib/format.ts
@src/components/app/fleet/ComingSoon.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build VehicleList component + page routes</name>
  <files>src/components/app/fleet/vehicles/VehicleList.tsx, src/app/(app)/app/fleet/vehicle-card/page.tsx, src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx</files>
  <action>
**1. Create `src/components/app/fleet/vehicles/VehicleList.tsx`** — 'use client' component mirroring DriverList.tsx pattern exactly.

**Props:** `{ vehicles: VehicleListItem[]; yellowDays: number }`

Import `VehicleListItem` from `@/lib/fleet/vehicle-types`.

**State:**
- `addOpen: boolean` (controls AddVehicleDialog)
- `statusFilter: StatusFilter` ('all' | 'active' | 'inactive'), default 'active'
- `fitnessFilter: FitnessFilter` ('all' | 'red' | 'yellow' | 'green'), default 'all'
- `search: string`

**Filtering logic:**
- Status filter: match `vehicle.computedStatus`
- Fitness filter: use `computeVehicleFitnessStatus(vehicle.testExpiryDate, vehicle.insuranceMinExpiry, vehicle.documentMinExpiry, yellowDays)` from VehicleFitnessLight.tsx
- Search: match against `vehicle.licensePlate`, `vehicle.tozeret`, `vehicle.degem`, `vehicle.companyName`, `vehicle.assignedDriverName` (all lowercased, null-safe)

**Counts:** total, active, inactive — same pattern as DriverList.

**Layout (mirror DriverList exactly):**
1. Header: title "כרטיסי רכב", stat chips (total/active/inactive), "רכב חדש" button (Car + Plus icon, teal gradient like DriverList)
2. Filters: search input (placeholder "לוחית / יצרן / דגם / חברה..."), status segment control, fitness segment control — exact same styling as DriverList
3. Table with columns:
   - **כשירות** (w-14): `<VehicleFitnessLight>` with testExpiryDate, insuranceMinExpiry, documentMinExpiry, yellowDays props
   - **מספר רישוי** (font-mono font-semibold): `formatLicensePlate(vehicle.licensePlate)` from `@/lib/format`
   - **יצרן / דגם**: combine `vehicle.tozeret` + `vehicle.degem` (show dash if both null)
   - **חברה** (hidden md:table-cell): vehicle.companyName with Building2 icon
   - **שנה** (hidden lg:table-cell, w-16): vehicle.shnatYitzur
   - **נהג** (hidden lg:table-cell): vehicle.assignedDriverName or "--"
   - **סטטוס** (w-20): active/inactive badge — same styling as DriverList
   - Chevron arrow (w-10): ChevronLeft icon
4. Row click: `router.push(\`/app/fleet/vehicle-card/${vehicle.id}\`)`
5. Footer: "מציג X מתוך Y רכבים"
6. Empty state: Car icon, "לא נמצאו רכבים", "נסה לשנות את הפילטרים"

**Responsive (IRON RULE):**
- Plate + fitness + status always visible
- Make/model always visible (primary identifier after plate)
- Company: `hidden md:table-cell`
- Year + driver: `hidden lg:table-cell`
- Search input: `w-full sm:w-52`
- Touch targets: all buttons/rows min 44px height

**At bottom of component:** Render `<AddVehicleDialog open={addOpen} onClose={() => setAddOpen(false)} />` — imported from `@/components/app/fleet/vehicles/AddVehicleDialog` (built in Plan 01).

**Imports:**
- VehicleFitnessLight, computeVehicleFitnessStatus from shared/VehicleFitnessLight
- AddVehicleDialog from vehicles/AddVehicleDialog
- formatLicensePlate from @/lib/format
- VehicleListItem from @/lib/fleet/vehicle-types
- shadcn: Button, Badge
- lucide-react: Car, Plus, ChevronLeft, Building2, Search, CheckCircle2
- React: useState; next/navigation: useRouter

**2. Replace `src/app/(app)/app/fleet/vehicle-card/page.tsx`** — server component mirroring driver-card/page.tsx:
- `await verifyAppUser()`
- `const vehicles = await getVehiclesList()`
- `const yellowDays = parseInt(process.env['FLEET_LICENSE_YELLOW_DAYS'] ?? '60', 10)`
- Render `<VehicleList vehicles={vehicles} yellowDays={yellowDays} />`
- Import verifyAppUser from @/lib/dal, getVehiclesList from @/actions/fleet/vehicles, VehicleList from @/components/app/fleet/vehicles/VehicleList

**3. Create `src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx`** — route shell for Phase 14:
- `await verifyAppUser()`
- `const { id } = await params` (consume params)
- Render `<ComingSoon label="כרטיס רכב" icon={Car} />`
- This route exists so VehicleList row clicks and AddVehicleDialog redirects work
- Phase 14 will replace ComingSoon with VehicleCard component
- Type for params: `{ params: Promise<{ id: string }> }`
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. VehicleList.tsx exists with >150 lines
4. vehicle-card/page.tsx imports VehicleList (not ComingSoon)
5. vehicle-card/[id]/page.tsx exists with ComingSoon placeholder
6. Grep confirms VehicleFitnessLight used: `grep "VehicleFitnessLight" src/components/app/fleet/vehicles/VehicleList.tsx`
7. Grep confirms formatLicensePlate used: `grep "formatLicensePlate" src/components/app/fleet/vehicles/VehicleList.tsx`
  </verify>
  <done>
VehicleList displays all vehicles in a table with fitness lights, formatted plates, filters, search, and add button. vehicle-card/page.tsx is a real server page (not placeholder). [id]/page.tsx exists as route shell for Phase 14.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of VehicleList + AddVehicleDialog</name>
  <files>src/components/app/fleet/vehicles/VehicleList.tsx, src/components/app/fleet/vehicles/AddVehicleDialog.tsx</files>
  <action>
Human verification of the complete vehicle list page and add dialog. Start dev server if not running: `npm run dev`.

Verify the following at http://localhost:3000/app/fleet/vehicle-card:
1. Vehicle list renders with fitness lights, formatted plates, make/model, status badges
2. Status and fitness filters work correctly
3. Search filters by plate, make, model, company, driver name
4. "רכב חדש" button opens AddVehicleDialog with two-step MOT flow
5. MOT preview shows vehicle data (manufacturer, model, year, color)
6. Creating a vehicle redirects to [id] page (ComingSoon)
7. Mobile responsive — non-critical columns hidden at narrow widths
8. RTL layout correct — text right-aligned, chevron arrows point left
  </action>
  <verify>
1. Login to ChemoSys at /chemosys with a user that has app_fleet permission
2. Navigate to /app/fleet/vehicle-card via the sidebar
3. Verify vehicle list shows with fitness lights and formatted plates
4. Test all filters (status + fitness) and search
5. Click "רכב חדש" — enter plate, select company, click "בדוק ב-MOT" — see preview
6. Click "פתח כרטיס רכב" — vehicle created, redirect to [id] page
7. Resize to 375px width — verify responsive layout, no overflow
  </verify>
  <done>
Vehicle list page and AddVehicleDialog are visually correct, filters work, MOT two-step flow functions, responsive layout confirmed, RTL layout verified.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `npm run build` — build succeeds
3. /app/fleet/vehicle-card renders VehicleList (not ComingSoon placeholder)
4. /app/fleet/vehicle-card/[id] renders ComingSoon (Phase 14 builds VehicleCard)
5. VehicleList shows fitness lights via VehicleFitnessLight component
6. VehicleList formats plates via formatLicensePlate
7. AddVehicleDialog creates vehicle via two-step MOT flow
8. All pages protected by verifyAppUser
</verification>

<success_criteria>
- Vehicle list page is fully functional with filters, search, fitness lights
- AddVehicleDialog two-step flow works: plate -> MOT preview -> create
- Table is responsive — hides non-critical columns on mobile
- [id] route exists as shell for Phase 14
- All pages use verifyAppUser guard
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-phase-10f-vehiclelist-addvehicledialog-mot-api-auto-fill-pages-integration/15-02-SUMMARY.md`
</output>
