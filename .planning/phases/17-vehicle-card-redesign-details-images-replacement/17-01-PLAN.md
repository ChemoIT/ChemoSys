---
phase: 17-vehicle-card-redesign-details-images-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/fleet/vehicle-types.ts
  - src/actions/fleet/vehicles.ts
  - src/actions/fleet/vehicle-replacement.ts
  - src/components/app/fleet/vehicles/AddVehicleDialog.tsx
  - src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
  - src/components/app/fleet/vehicles/VehicleImageGallery.tsx
autonomous: true

must_haves:
  truths:
    - "טאב 'פרטי הרכב' מציג גלריית 5 חריצי תמונות — ריקים/מלאים — ומאפשר העלאה, צפייה בלייטבוקס ומחיקה"
    - "שדות סוג רכב (פרטי/מסחרי/משאית/ניגרר) וסטטוס רכב (5 ערכים) מוצגים ועובדים"
    - "כרטיס נעול (הוחזר/נמכר/מושבת) — כל שדות הטופס read-only מלבד dropdown הסטטוס"
    - "מעבר לסטטוס נועל ללא תאריך יציאה מהצי — חסימת שמירה עם הודעת שגיאה"
    - "AddVehicleDialog פועל ללא בחירת חברה — רק לוחית רישוי + MOT"
    - "createVehicle() מקבל licensePlate בלבד — ללא companyId"
  artifacts:
    - path: "src/lib/fleet/vehicle-types.ts"
      provides: "VehicleImage, VehicleReplacementRecord, VehicleFuelCard types + VEHICLE_STATUS_LABELS + VEHICLE_TYPE_LABELS (מעודכן) + REPLACEMENT_REASON_LABELS"
    - path: "src/actions/fleet/vehicles.ts"
      provides: "createVehicle (ללא companyId), updateVehicleDetails (עם vehicleStatus + fleetExitDate + is_active sync), getVehicleById (עם vehicle_status + fleet_exit_date), addVehicleImage, deleteVehicleImage, getVehicleImages"
    - path: "src/actions/fleet/vehicle-replacement.ts"
      provides: "getVehicleReplacementRecords, addVehicleReplacementRecord, updateVehicleReplacementRecord, deleteVehicleReplacementRecord, addVehicleFuelCard, deleteVehicleFuelCard"
    - path: "src/components/app/fleet/vehicles/VehicleImageGallery.tsx"
      provides: "רכיב גלריה עם 5 חריצים, העלאה client-side, לייטבוקס, מחיקה"
    - path: "src/components/app/fleet/vehicles/VehicleDetailsSection.tsx"
      provides: "טופס מחודש עם גלריה, vehicle_status, fleet_exit_date, נעילת כרטיס"
    - path: "src/components/app/fleet/vehicles/AddVehicleDialog.tsx"
      provides: "דיאלוג ללא company selector"
  key_links:
    - from: "VehicleImageGallery.tsx"
      to: "supabase storage vehicle-images bucket"
      via: "createClient() browser + upload({ upsert: true })"
      pattern: "from\\('vehicle-images'\\)"
    - from: "VehicleImageGallery.tsx"
      to: "addVehicleImage / deleteVehicleImage"
      via: "Server Action calls after storage upload/delete"
      pattern: "addVehicleImage|deleteVehicleImage"
    - from: "VehicleDetailsSection.tsx"
      to: "updateVehicleDetails"
      via: "handleSave() with vehicleStatus + fleetExitDate"
      pattern: "vehicleStatus.*fleetExitDate|updateVehicleDetails"
    - from: "AddVehicleDialog.tsx"
      to: "createVehicle"
      via: "handleCreate() — plate only, no companyId"
      pattern: "createVehicle\\(plate\\)"
---

<objective>
תשתית סרבר + רכיבי UI לפאזה 17: סוגי TypeScript חדשים, Server Actions לתמונות ורכב חלופי, תיקון AddVehicleDialog, עיצוב מחדש של VehicleDetailsSection עם גלריית תמונות ונעילת כרטיס.

Purpose: מבסס את כל שכבת הנתונים והרכיבים שבהם תלוי Plan 17-02 (ReplacementVehicleDialog).
Output: vehicle-types.ts מעודכן, vehicles.ts עם 3 פונקציות חדשות + 2 מעודכנות, vehicle-replacement.ts חדש, VehicleImageGallery חדש, VehicleDetailsSection מחודש, AddVehicleDialog מתוקן.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vehicle-card-redesign-details-images-replacement/17-RESEARCH.md
@src/lib/fleet/vehicle-types.ts
@src/actions/fleet/vehicles.ts
@src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
@src/components/app/fleet/vehicles/AddVehicleDialog.tsx
@src/components/app/fleet/shared/FleetDateInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: עדכון vehicle-types.ts + יצירת vehicle-replacement.ts</name>
  <files>
    src/lib/fleet/vehicle-types.ts
    src/actions/fleet/vehicle-replacement.ts
  </files>
  <action>
**A. עדכון `src/lib/fleet/vehicle-types.ts`:**

1. החלף את `VEHICLE_TYPE_LABELS` הישן (7 ערכים ישנים: minibus, light_commercial, heavy, forklift, equipment, other) בקבוע חדש:
```typescript
export const VEHICLE_TYPE_LABELS: Record<string, string> = {
  private:    'פרטי',
  commercial: 'מסחרי',
  truck:      'משאית',
  trailer:    'ניגרר',
}
```
(מותאם ל-CHECK constraint של migration 00027)

2. הוסף קבועים חדשים אחרי `OWNERSHIP_TYPE_LABELS`:
```typescript
export const VEHICLE_STATUS_LABELS: Record<string, string> = {
  active:         'פעיל',
  suspended:      'מושבת זמני',
  returned:       'הוחזר',
  sold:           'נמכר',
  decommissioned: 'מושבת',
}

export const REPLACEMENT_REASON_LABELS: Record<string, string> = {
  maintenance: 'טיפול',
  test:        'טסט',
  accident:    'תאונה',
  other:       'אחר',
}
```

3. הוסף טיפוסים חדשים בסוף הקובץ (לפני DriverOptionForAssignment):
```typescript
export type VehicleImage = {
  id: string
  vehicleId: string
  storagePath: string
  position: number           // 1-5
  signedUrl: string | null
  createdAt: string
}

export type VehicleFuelCard = {
  id: string
  replacementRecordId: string
  cardNumber: string
  createdAt: string
}

export type VehicleReplacementRecord = {
  id: string
  vehicleId: string
  licensePlate: string
  motData: Record<string, unknown> | null
  entryDate: string
  entryKm: number | null
  returnDate: string | null
  returnKm: number | null
  reason: 'maintenance' | 'test' | 'accident' | 'other'
  reasonOther: string | null
  status: 'active' | 'returned'
  notes: string | null
  fuelCards: VehicleFuelCard[]
  createdAt: string
}
```

4. הרחב את `VehicleFull` — הוסף לסוף ה-type (לפני הסגר):
```typescript
  vehicleStatus: string        // 'active'|'suspended'|'returned'|'sold'|'decommissioned'
  fleetExitDate: string | null // yyyy-mm-dd
```

**B. יצירת `src/actions/fleet/vehicle-replacement.ts`:**

קובץ חדש עם 'use server' בראשו. פונקציות:

1. `getVehicleReplacementRecords(vehicleId: string): Promise<VehicleReplacementRecord[]>`
   - שאילתה: `from('vehicle_replacement_records').select('*, vehicle_fuel_cards(*)').eq('vehicle_id', vehicleId).is('deleted_at', null).order('entry_date', { ascending: false })`
   - מיפוי לטיפוס: camelCase מ-snake_case, fuelCards = vehicle_fuel_cards array
   - status מחושב מ-return_date: `return_date IS NULL ? 'active' : 'returned'`

2. `addVehicleReplacementRecord(input: {...}): Promise<ActionResult & { id?: string }>`
   - input: `{ vehicleId, licensePlate, entryDate, entryKm?, reason, reasonOther?, notes? }`
   - בדיקה: אם כבר קיים רשומה פעילה → `return { success: false, error: 'קיים כבר רכב חלופי פעיל עבור רכב זה' }`
   - בדיקה: `reason === 'other' && !reasonOther` → `return { success: false, error: 'נדרש הסבר לסיבה "אחר"' }`
   - INSERT: `vehicle_replacement_records` עם כל השדות + `created_by: userId`
   - לאחר INSERT: `await supabase.from('vehicles').update({ vehicle_status: 'suspended', is_active: false, updated_by: userId }).eq('id', input.vehicleId).is('deleted_at', null)`
   - `revalidatePath('/app/fleet/vehicle-card/' + input.vehicleId)`
   - החזר `{ success: true, id: data.id }`

3. `updateVehicleReplacementRecord(input: {...}): Promise<ActionResult>`
   - input: `{ recordId, vehicleId, licensePlate, entryDate, entryKm?, returnDate?, returnKm?, reason, reasonOther?, notes? }`
   - status מחושב: `const status = input.returnDate ? 'returned' : 'active'`
   - בדיקה: `reason === 'other' && !reasonOther` → error
   - UPDATE: `vehicle_replacement_records SET (...) WHERE id = recordId AND deleted_at IS NULL`
   - אם `status === 'returned'`: בדוק אם עדיין קיימת רשומה פעילה אחרת; אם לא — `await supabase.from('vehicles').update({ vehicle_status: 'active', is_active: true, updated_by: userId }).eq('id', input.vehicleId).is('deleted_at', null)`
   - `revalidatePath('/app/fleet/vehicle-card/' + input.vehicleId)`

4. `deleteVehicleReplacementRecord(recordId: string, vehicleId: string): Promise<ActionResult>`
   - שימוש ב-RPC: `supabase.rpc('soft_delete_vehicle_replacement_record', { p_id: recordId, p_user_id: userId })`
   - `revalidatePath('/app/fleet/vehicle-card/' + vehicleId)`

5. `addVehicleFuelCard(replacementRecordId: string, cardNumber: string): Promise<ActionResult & { id?: string }>`
   - ולידציה: `if (!/^\d+$/.test(cardNumber.trim())) return { success: false, error: 'מספר כרטיס חייב להכיל ספרות בלבד' }`
   - INSERT: `vehicle_fuel_cards` עם `replacement_record_id, card_number: cardNumber.trim(), created_by: userId`
   - החזר `{ success: true, id: data.id }`

6. `deleteVehicleFuelCard(cardId: string): Promise<ActionResult>`
   - DELETE (hard delete — ללא soft delete לפי decision [16-01]): `supabase.from('vehicle_fuel_cards').delete().eq('id', cardId)`
   - החזר `{ success: true }`

כל פונקציה מתחילה ב-`const { userId } = await verifyAppUser()` ו-`const supabase = await createClient()`.
  </action>
  <verify>
```bash
npx tsc --noEmit 2>&1 | grep -E "vehicle-types|vehicle-replacement" | head -20
```
אם אין שגיאות TypeScript בקבצים אלו — בסדר. בדוק גם שהקובץ vehicle-replacement.ts נוצר:
```bash
ls C:/Sharon_ClaudeCode/Apps/ChemoSystem/src/actions/fleet/vehicle-replacement.ts
```
  </verify>
  <done>
- `vehicle-types.ts` מייצא: VEHICLE_TYPE_LABELS (4 ערכים חדשים), VEHICLE_STATUS_LABELS, REPLACEMENT_REASON_LABELS, VehicleImage, VehicleFuelCard, VehicleReplacementRecord
- `VehicleFull` כולל `vehicleStatus` ו-`fleetExitDate`
- `vehicle-replacement.ts` קיים עם 6 פונקציות מיוצאות
- אין שגיאות TypeScript בשני הקבצים
  </done>
</task>

<task type="auto">
  <name>Task 2: עדכון vehicles.ts — createVehicle + updateVehicleDetails + image actions + getVehicleById</name>
  <files>
    src/actions/fleet/vehicles.ts
  </files>
  <action>
**שינויים בקובץ הקיים `src/actions/fleet/vehicles.ts`:**

**A. עדכון import טיפוסים:**
הוסף לרשימת ה-import מ-vehicle-types:
```typescript
import type {
  VehicleListItem,
  VehicleFull,
  VehicleTest,
  VehicleInsurance,
  VehicleDocument,
  DriverOptionForAssignment,
  VehicleImage,       // חדש
} from '@/lib/fleet/vehicle-types'
```

**B. עדכון `createVehicle()` — הסרת companyId:**
החלף את החתימה הנוכחית:
```typescript
// לפני:
export async function createVehicle(
  licensePlate: string,
  companyId: string
): Promise<ActionResult & { vehicleId?: string }>

// אחרי:
export async function createVehicle(
  licensePlate: string
): Promise<ActionResult & { vehicleId?: string }>
```
הסר את השורות:
- `if (!companyId) return { success: false, error: 'חברה נדרשת' }`
- `company_id: companyId,` מתוך ה-INSERT

**C. עדכון `getVehicleById()` — הוספת שדות חדשים:**
בשאילתת ה-SELECT הוסף אחרי `is_active`:
```
vehicle_status,
fleet_exit_date,
```
בה-return object הוסף לסוף (לפני הסגר):
```typescript
vehicleStatus: data.vehicle_status ?? 'active',
fleetExitDate: data.fleet_exit_date ?? null,
```

**D. עדכון `UpdateVehicleInput` type + `updateVehicleDetails()`:**
הוסף לטיפוס:
```typescript
vehicleStatus?: string       // 'active'|'suspended'|'returned'|'sold'|'decommissioned'
fleetExitDate?: string | null
```
בפונקציה `updateVehicleDetails`, הוסף לה-.update():
```typescript
vehicle_status: input.vehicleStatus ?? 'active',
fleet_exit_date: input.fleetExitDate ?? null,
// is_active נגזר מ-vehicle_status:
is_active: (input.vehicleStatus ?? 'active') === 'active',
```
הסר את שורת `is_active: input.isActive ?? true` הישנה (כי is_active עכשיו נגזר מ-vehicle_status).
הסר גם את `isActive?: boolean` מ-`UpdateVehicleInput`.

**E. הוסף פונקציות חדשות לתמונות — בסוף הקובץ לפני הפונקציות האחרות:**

```typescript
// ─────────────────────────────────────────────────────────────
// VEHICLE IMAGES
// ─────────────────────────────────────────────────────────────

/**
 * Returns all images for a vehicle with signed URLs (1-year TTL).
 * Called client-side to avoid caching stale signed URLs.
 */
export async function getVehicleImages(vehicleId: string): Promise<VehicleImage[]> {
  await verifyAppUser()
  const supabase = await createClient()

  const { data, error } = await supabase
    .from('vehicle_images')
    .select('id, vehicle_id, storage_path, position, created_at')
    .eq('vehicle_id', vehicleId)
    .order('position', { ascending: true })

  if (error || !data) return []

  return Promise.all(
    data.map(async (img) => {
      const { data: signed } = await supabase.storage
        .from('vehicle-images')
        .createSignedUrl(img.storage_path, 60 * 60 * 24 * 365)
      return {
        id: img.id,
        vehicleId: img.vehicle_id,
        storagePath: img.storage_path,
        position: img.position,
        signedUrl: signed?.signedUrl ?? null,
        createdAt: img.created_at,
      }
    })
  )
}

/**
 * Saves image metadata after client-side upload to storage.
 * If slot already occupied — deletes old storage file + DB row first.
 */
export async function addVehicleImage(
  vehicleId: string,
  storagePath: string,
  position: number
): Promise<ActionResult & { id?: string }> {
  const { userId } = await verifyAppUser()
  const supabase = await createClient()

  // Handle position conflict: delete old DB row + storage file
  const { data: existing } = await supabase
    .from('vehicle_images')
    .select('id, storage_path')
    .eq('vehicle_id', vehicleId)
    .eq('position', position)
    .maybeSingle()

  if (existing) {
    await supabase.storage.from('vehicle-images').remove([existing.storage_path])
    await supabase.from('vehicle_images').delete().eq('id', existing.id)
  }

  const { data, error } = await supabase
    .from('vehicle_images')
    .insert({ vehicle_id: vehicleId, storage_path: storagePath, position, created_by: userId })
    .select('id')
    .single()

  if (error) return { success: false, error: 'שגיאה בשמירת התמונה' }

  revalidatePath(`/app/fleet/vehicle-card/${vehicleId}`)
  return { success: true, id: data.id }
}

/**
 * Hard-deletes an image from storage + DB (no soft-delete — decision [16-01]).
 */
export async function deleteVehicleImage(
  imageId: string,
  storagePath: string,
  vehicleId: string
): Promise<ActionResult> {
  await verifyAppUser()
  const supabase = await createClient()

  await supabase.storage.from('vehicle-images').remove([storagePath])
  const { error } = await supabase.from('vehicle_images').delete().eq('id', imageId)

  if (error) return { success: false, error: 'שגיאה במחיקת התמונה' }

  revalidatePath(`/app/fleet/vehicle-card/${vehicleId}`)
  return { success: true }
}
```
  </action>
  <verify>
```bash
npx tsc --noEmit 2>&1 | grep "vehicles.ts" | head -20
```
בדוק גם שה-createVehicle החדש מייצא את החתימה הנכונה:
```bash
grep -n "export async function createVehicle" C:/Sharon_ClaudeCode/Apps/ChemoSystem/src/actions/fleet/vehicles.ts
```
  </verify>
  <done>
- `createVehicle(licensePlate)` — ללא companyId, ללא בדיקת `if (!companyId)`
- `getVehicleById` מחזיר `vehicleStatus` ו-`fleetExitDate`
- `UpdateVehicleInput` כולל `vehicleStatus` ו-`fleetExitDate`, ללא `isActive`
- `updateVehicleDetails` כותב `vehicle_status`, `fleet_exit_date`, ו-`is_active` (נגזר)
- 3 פונקציות תמונה חדשות: `getVehicleImages`, `addVehicleImage`, `deleteVehicleImage`
- אין שגיאות TypeScript בקובץ
  </done>
</task>

<task type="auto">
  <name>Task 3: AddVehicleDialog + VehicleImageGallery + VehicleDetailsSection מחודש</name>
  <files>
    src/components/app/fleet/vehicles/AddVehicleDialog.tsx
    src/components/app/fleet/vehicles/VehicleImageGallery.tsx
    src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
  </files>
  <action>
**A. תיקון `AddVehicleDialog.tsx`:**

הסר מהדיאלוג את כל מה שקשור ל-company:
1. הסר `const [companyId, setCompanyId] = useState('')`
2. הסר `const [companies, setCompanies] = useState<...>([])`
3. הסר את ה-`useEffect` שמטעין `getCompaniesForSelect().then(setCompanies)`
4. הסר את import של `getCompaniesForSelect` מ-vehicles
5. עדכן `handleLookup()`: הסר `!companyId` מהבדיקה, השאר `if (!plate.trim()) return`
6. עדכן `handleCreate()`: הסר `!companyId`, עדכן קריאה ל-`createVehicle(plate)` ללא companyId
7. הסר את בלוק ה-JSX של `{/* Company selector */}`
8. עדכן את הכפתור "בדוק ב-MOT" — הסר `!companyId` מ-`disabled`
9. עדכן `DialogDescription` צעד 1: `'הזן מספר רישוי, לאחר מכן בדוק נתונים ב-MOT.'`
10. עדכן `onKeyDown` בשדה הלוחית: `if (e.key === 'Enter' && plate.trim()) handleLookup()`

**B. יצירת `VehicleImageGallery.tsx` — רכיב חדש:**

```typescript
'use client'

/**
 * VehicleImageGallery — 5-slot image gallery for vehicle card.
 *
 * Each slot shows thumbnail (if filled) or empty placeholder.
 * Click filled slot → lightbox dialog.
 * Click empty slot → file picker (image/*).
 * Filled slots have a delete button (×).
 * Upload: client-side to Supabase storage → Server Action to save metadata.
 * Auto-save on upload/delete — no dirty tracking.
 * Locked: when isLocked=true — no upload/delete available.
 */

import { useState, useEffect, useRef } from 'react'
import { createClient } from '@/lib/supabase/browser'
import { toast } from 'sonner'
import { X, ImagePlus, Loader2, ZoomIn } from 'lucide-react'
import { Dialog, DialogContent } from '@/components/ui/dialog'
import { getVehicleImages, addVehicleImage, deleteVehicleImage } from '@/actions/fleet/vehicles'
import type { VehicleImage } from '@/lib/fleet/vehicle-types'

type Props = {
  vehicleId: string
  isLocked: boolean
}

export function VehicleImageGallery({ vehicleId, isLocked }: Props) {
  const [images, setImages] = useState<VehicleImage[]>([])
  const [uploading, setUploading] = useState<number | null>(null)
  const [lightboxImage, setLightboxImage] = useState<VehicleImage | null>(null)
  const fileInputRef = useRef<HTMLInputElement>(null)
  const [pendingPosition, setPendingPosition] = useState<number | null>(null)

  // טעינת תמונות בהרצת הרכיב
  useEffect(() => {
    void getVehicleImages(vehicleId).then(setImages)
  }, [vehicleId])

  // 5 חריצים תמיד — ממולאים לפי position
  const slots = Array.from({ length: 5 }, (_, i) => ({
    position: i + 1,
    image: images.find((img) => img.position === i + 1) ?? null,
  }))

  async function handleFileSelect(position: number, file: File) {
    setUploading(position)
    const supabase = createClient()
    const ext = file.name.split('.').pop() ?? 'jpg'
    const path = `${vehicleId}/${position}.${ext}`

    const { error: uploadError } = await supabase.storage
      .from('vehicle-images')
      .upload(path, file, { upsert: true })  // upsert=true: מחליף קובץ קיים

    if (uploadError) {
      toast.error('שגיאה בהעלאת התמונה')
      setUploading(null)
      return
    }

    const result = await addVehicleImage(vehicleId, path, position)
    if (!result.success) {
      toast.error(result.error ?? 'שגיאה בשמירת התמונה')
      setUploading(null)
      return
    }

    // רענון תמונות עם signed URLs מעודכנים
    const updated = await getVehicleImages(vehicleId)
    setImages(updated)
    setUploading(null)
    toast.success('התמונה הועלתה')
  }

  async function handleDelete(image: VehicleImage) {
    const result = await deleteVehicleImage(image.id, image.storagePath, vehicleId)
    if (!result.success) {
      toast.error(result.error ?? 'שגיאה במחיקת התמונה')
      return
    }
    setImages((prev) => prev.filter((img) => img.id !== image.id))
    toast.success('התמונה נמחקה')
  }

  function handleSlotClick(position: number, hasImage: boolean) {
    if (isLocked) return
    if (hasImage) return  // לחיצה על תמונה מפתחת לייטבוקס (handler נפרד)
    // פתח file picker לחריץ ריק
    setPendingPosition(position)
    fileInputRef.current?.click()
  }

  return (
    <div className="space-y-2">
      <p className="text-xs text-muted-foreground font-semibold">תמונות רכב (עד 5)</p>

      {/* Hidden file input */}
      <input
        ref={fileInputRef}
        type="file"
        accept="image/*"
        className="hidden"
        onChange={(e) => {
          const file = e.target.files?.[0]
          if (file && pendingPosition !== null) {
            void handleFileSelect(pendingPosition, file)
            setPendingPosition(null)
          }
          e.target.value = ''  // reset input לאפשר בחירה חוזרת
        }}
      />

      {/* Grid of 5 slots */}
      <div className="grid grid-cols-3 sm:grid-cols-5 gap-2">
        {slots.map(({ position, image }) => (
          <div key={position} className="relative aspect-square">
            {image ? (
              // חריץ מלא
              <div className="relative w-full h-full group">
                <img
                  src={image.signedUrl ?? ''}
                  alt={`תמונת רכב ${position}`}
                  className="w-full h-full object-cover rounded-lg border border-border cursor-pointer"
                  onClick={() => setLightboxImage(image)}
                />
                {/* אייקון זום */}
                <div className="absolute inset-0 flex items-center justify-center bg-black/20 opacity-0 group-hover:opacity-100 transition-opacity rounded-lg cursor-pointer pointer-events-none">
                  <ZoomIn className="h-5 w-5 text-white" />
                </div>
                {/* כפתור מחיקה */}
                {!isLocked && (
                  <button
                    onClick={(e) => { e.stopPropagation(); void handleDelete(image) }}
                    className="absolute top-1 left-1 bg-red-500 text-white rounded-full w-5 h-5 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity hover:bg-red-600"
                    title="מחק תמונה"
                  >
                    <X className="h-3 w-3" />
                  </button>
                )}
              </div>
            ) : uploading === position ? (
              // חריץ בטעינה
              <div className="w-full h-full rounded-lg border-2 border-dashed border-primary/30 flex items-center justify-center bg-primary/5">
                <Loader2 className="h-6 w-6 text-primary animate-spin" />
              </div>
            ) : (
              // חריץ ריק
              <button
                onClick={() => handleSlotClick(position, false)}
                disabled={isLocked}
                className="w-full h-full rounded-lg border-2 border-dashed border-border hover:border-primary/50 hover:bg-primary/5 transition-colors flex items-center justify-center disabled:opacity-40 disabled:cursor-not-allowed"
                title={isLocked ? 'הכרטיס נעול' : 'הוסף תמונה'}
              >
                <ImagePlus className="h-6 w-6 text-muted-foreground" />
              </button>
            )}
          </div>
        ))}
      </div>

      {/* Lightbox */}
      <Dialog open={!!lightboxImage} onOpenChange={() => setLightboxImage(null)}>
        <DialogContent className="max-w-3xl p-2" dir="rtl">
          {lightboxImage && (
            <img
              src={lightboxImage.signedUrl ?? ''}
              alt={`תמונת רכב ${lightboxImage.position}`}
              className="w-full h-auto rounded-lg object-contain max-h-[80dvh]"
            />
          )}
        </DialogContent>
      </Dialog>
    </div>
  )
}
```

**C. עיצוב מחדש של `VehicleDetailsSection.tsx`:**

החלף את הרכיב הקיים לגמרי. שמור על:
- אותם imports בסיסיים (useState, useEffect, useTransition, toast, Label, formatLicensePlate, formatDate, FleetDateInput)
- אותה מבנה InfoRow
- אותן dropdown ספקים (leasing, insurance, fuel_card, garage)
- אותו עמוד שמאל/ימין
- MOT panel זהה

**הוסף:**

1. imports חדשים:
```typescript
import { Lock } from 'lucide-react'
import { VehicleImageGallery } from './VehicleImageGallery'
import { VEHICLE_STATUS_LABELS, VEHICLE_TYPE_LABELS } from '@/lib/fleet/vehicle-types'
import FleetDateInput from '@/components/app/fleet/shared/FleetDateInput'
```

2. state חדש:
```typescript
const [vehicleStatus, setVehicleStatus] = useState(vehicle.vehicleStatus ?? 'active')
const [fleetExitDate, setFleetExitDate] = useState(vehicle.fleetExitDate ?? '')
```

3. הסר `isActive` state לגמרי (is_active נגזר מ-vehicle_status בשרת).

4. קבוע נעילה — מיד אחרי הגדרת ה-state:
```typescript
const LOCKED_STATUSES = ['returned', 'sold', 'decommissioned'] as const
const isLocked = LOCKED_STATUSES.includes(vehicleStatus as typeof LOCKED_STATUSES[number])
```

5. עדכן `isDirty` — הוסף:
```typescript
vehicleStatus !== (vehicle.vehicleStatus ?? 'active') ||
fleetExitDate !== (vehicle.fleetExitDate ?? '')
```
הסר את ה-isActive מה-isDirty.

6. עדכן `handleSave()`:
```typescript
function handleSave() {
  // בדיקה: סטטוס נועל חייב תאריך יציאה
  if (LOCKED_STATUSES.includes(vehicleStatus as typeof LOCKED_STATUSES[number]) && !fleetExitDate) {
    toast.error('יש להזין תאריך יציאה מהצי לפני שינוי הסטטוס')
    return
  }
  startSaveTransition(async () => {
    const result = await updateVehicleDetails({
      vehicleId: vehicle.id,
      vehicleType: vehicleType || null,
      ownershipType: ownershipType || null,
      companyId: companyId || null,
      vehicleStatus,
      fleetExitDate: fleetExitDate || null,
      leasingCompanyId: leasingCompanyId || null,
      insuranceCompanyId: insuranceCompanyId || null,
      fuelCardSupplierId: fuelCardSupplierId || null,
      garageId: garageId || null,
    })
    if (result.success) {
      toast.success('פרטי הרכב נשמרו בהצלחה')
    } else {
      toast.error(result.error ?? 'שגיאה בשמירה')
    }
  })
}
```

7. בעמודה השמאלית (שדות עריכה) — בנה מחדש:

**לפני "סוג רכב"** — הוסף גלריה:
```jsx
{/* גלריית תמונות */}
<div className="space-y-1.5">
  <VehicleImageGallery vehicleId={vehicle.id} isLocked={isLocked} />
</div>
```

**שנה "סוג רכב"** — ה-select נשאר אבל מקבל `disabled={isLocked}`.

**הוסף אחרי "סוג רכב"** — "סטטוס רכב" (תמיד enabled, גם בנעילה):
```jsx
<div className="space-y-1.5">
  <Label>סטטוס רכב</Label>
  <select
    value={vehicleStatus}
    onChange={(e) => setVehicleStatus(e.target.value)}
    className={selectClass}
    {/* אין disabled={isLocked} — סטטוס תמיד עריך */}
  >
    {Object.entries(VEHICLE_STATUS_LABELS).map(([key, label]) => (
      <option key={key} value={key}>{label}</option>
    ))}
  </select>
</div>
```

**הוסף אחרי "סטטוס רכב"** — "תאריך יציאה מהצי":
```jsx
<div className="space-y-1.5">
  <Label>
    תאריך יציאה מהצי
    {LOCKED_STATUSES.includes(vehicleStatus as typeof LOCKED_STATUSES[number]) && (
      <span className="text-red-500 mr-1">*</span>
    )}
  </Label>
  <FleetDateInput
    value={fleetExitDate}
    onChange={setFleetExitDate}
    disabled={isLocked}
  />
</div>
```

**הסר** את הswitch "רכב פעיל" (is_active נגזר מ-vehicle_status).

**הוסף לפני כפתור "שמור"** — כפתור "רכב חלופי":
```jsx
<div className="space-y-1.5">
  <button
    type="button"
    onClick={() => {/* יפתח דיאלוג — יחובר ב-Plan 17-02 */}}
    className="w-full flex items-center justify-center gap-2 px-4 py-2 rounded-lg text-sm font-medium border border-border hover:bg-muted/60 transition-colors"
  >
    <Car className="h-4 w-4" />
    ניהול רכב חלופי
  </button>
</div>
```
(הוסף `Car` לרשימת imports מ-lucide-react)

**כל select ו-FleetDateInput** בעמודה השמאלית (מלבד vehicleStatus select) מקבל `disabled={isLocked}`.

**כפתור "שמור שינויים"** — הסר `|| !isDirty` מה-disabled (כדי שאפשר לשמור שינוי סטטוס גם כשהכרטיס נעול):
```jsx
disabled={isSaving || (!isDirty)}
```
(ממשיך לקבל disabled כשאין שינויים — זה תקין)

**הוסף תג "נעול"** בראש עמודת העריכה, כאשר isLocked:
```jsx
{isLocked && (
  <div className="flex items-center gap-1.5 text-xs text-amber-600 bg-amber-50 border border-amber-200 rounded-lg px-3 py-2 mb-2">
    <Lock className="h-3.5 w-3.5" />
    הכרטיס נעול — שנה סטטוס לפעיל/מושבת זמני כדי לערוך
  </div>
)}
```
  </action>
  <verify>
```bash
npx tsc --noEmit 2>&1 | grep -E "AddVehicleDialog|VehicleImageGallery|VehicleDetailsSection" | head -20
```
בדוק שה-build עובר:
```bash
cd C:/Sharon_ClaudeCode/Apps/ChemoSystem && npm run build 2>&1 | tail -20
```
  </verify>
  <done>
- `AddVehicleDialog.tsx` — ללא company selector, handleLookup/handleCreate עם plate בלבד
- `VehicleImageGallery.tsx` קיים — 5 חריצים, העלאה ל-storage, לייטבוקס, מחיקה, נעילה
- `VehicleDetailsSection.tsx` — כולל: גלריה, vehicle_status select, fleet_exit_date FleetDateInput, תג נעילה, validation ב-handleSave, כפתור "ניהול רכב חלופי" (placeholder), כל שדות disabled בנעילה מלבד vehicle_status
- `npm run build` עובר ללא שגיאות
  </done>
</task>

</tasks>

<verification>
לאחר השלמת כל המשימות:

1. `npx tsc --noEmit` — אפס שגיאות TypeScript
2. `npm run build` — build מצליח
3. בדוק ב-browser: דף `/app/fleet/vehicle-card` — אין שגיאות console
4. בדוק AddVehicleDialog: לחיצה על "הוסף רכב" — הדיאלוג לא מציג שדה חברה
5. בדוק VehicleDetailsSection: dropdown "סטטוס רכב" מציג 5 אפשרויות בעברית
</verification>

<success_criteria>
- vehicle-types.ts מייצא סוגים וקבועים חדשים ללא שגיאות TypeScript
- vehicle-replacement.ts קיים עם 6 פונקציות server action
- vehicles.ts: createVehicle ללא companyId, updateVehicleDetails עם vehicleStatus, 3 image actions חדשות
- VehicleImageGallery.tsx קיים ומוטמע ב-VehicleDetailsSection
- AddVehicleDialog.tsx ללא company selector
- VehicleDetailsSection.tsx עם נעילת כרטיס, vehicle_status, fleet_exit_date
- npm run build עובר
</success_criteria>

<output>
לאחר השלמה, צור `.planning/phases/17-vehicle-card-redesign-details-images-replacement/17-01-SUMMARY.md`
</output>
