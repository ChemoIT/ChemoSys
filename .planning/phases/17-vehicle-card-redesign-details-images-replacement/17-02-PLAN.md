---
phase: 17-vehicle-card-redesign-details-images-replacement
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/app/fleet/vehicles/ReplacementVehicleDialog.tsx
  - src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
autonomous: false

must_haves:
  truths:
    - "לחיצה על 'ניהול רכב חלופי' בטאב פרטי הרכב פותחת דיאלוג עם היסטוריית רכבים חלופיים"
    - "ניתן להוסיף רשומת רכב חלופי חדשה עם לוחית, תאריך כניסה, סיבה (טיפול/טסט/תאונה/אחר)"
    - "סיבה 'אחר' דורשת שדה הסבר חובה — שמירה ללא הסבר נחסמת"
    - "אחרי שמירת רשומה חדשה — עובר אוטומטית למצב עריכה כדי לאפשר הוספת כרטיסי דלק"
    - "כרטיסי דלק ניתן להוסיף/להסיר inline — ספרות בלבד, הסרה hard-delete"
    - "רשומה פעילה (return_date IS NULL) מוצגת עם תג 'פעיל', רשומה שהוחזרה עם תאריך החזרה"
    - "תקופת שהייה וסה\"כ קמ' מחושבים client-side ומוצגים אוטומטית"
    - "שמירת רשומה חדשה ← vehicle_status → 'מושבת זמני' ← כרטיס מתעדכן"
  artifacts:
    - path: "src/components/app/fleet/vehicles/ReplacementVehicleDialog.tsx"
      provides: "דיאלוג ניהול רכב חלופי — list view + form view + fuel cards sub-list"
      min_lines: 300
    - path: "src/components/app/fleet/vehicles/VehicleDetailsSection.tsx"
      provides: "כפתור 'ניהול רכב חלופי' מחובר ל-ReplacementVehicleDialog + router.refresh() אחרי שמירה"
  key_links:
    - from: "ReplacementVehicleDialog.tsx"
      to: "vehicle-replacement.ts server actions"
      via: "getVehicleReplacementRecords, addVehicleReplacementRecord, updateVehicleReplacementRecord"
      pattern: "addVehicleReplacementRecord|updateVehicleReplacementRecord"
    - from: "ReplacementVehicleDialog.tsx"
      to: "addVehicleFuelCard / deleteVehicleFuelCard"
      via: "FuelCardsList inline add/remove"
      pattern: "addVehicleFuelCard|deleteVehicleFuelCard"
    - from: "VehicleDetailsSection.tsx"
      to: "ReplacementVehicleDialog"
      via: "state showReplacementDialog + router.refresh() on close"
      pattern: "showReplacementDialog|router\\.refresh"
---

<objective>
ReplacementVehicleDialog — דיאלוג מלא לניהול רכב חלופי עם list view, form view וכרטיסי דלק inline. חיבור הדיאלוג לכפתור ב-VehicleDetailsSection.

Purpose: משלים את כרטיס הרכב המחודש — מאפשר תיעוד מלא של כל אירוע החלפת רכב.
Output: ReplacementVehicleDialog.tsx חדש, VehicleDetailsSection.tsx מחובר.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-vehicle-card-redesign-details-images-replacement/17-RESEARCH.md
@.planning/phases/17-vehicle-card-redesign-details-images-replacement/17-01-SUMMARY.md
@src/actions/fleet/vehicle-replacement.ts
@src/lib/fleet/vehicle-types.ts
@src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
@src/components/app/fleet/shared/FleetDateInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: יצירת ReplacementVehicleDialog.tsx</name>
  <files>
    src/components/app/fleet/vehicles/ReplacementVehicleDialog.tsx
  </files>
  <action>
יצור רכיב חדש `'use client'` עם שתי תצוגות: רשימה (list) וטופס (add/edit).

**מבנה כללי:**

```typescript
'use client'

import { useState, useEffect, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { toast } from 'sonner'
import { Plus, ChevronRight, Loader2, X, Car, RefreshCw } from 'lucide-react'
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Label } from '@/components/ui/label'
import { Input } from '@/components/ui/input'
import FleetDateInput from '@/components/app/fleet/shared/FleetDateInput'
import {
  getVehicleReplacementRecords,
  addVehicleReplacementRecord,
  updateVehicleReplacementRecord,
  deleteVehicleReplacementRecord,
  addVehicleFuelCard,
  deleteVehicleFuelCard,
} from '@/actions/fleet/vehicle-replacement'
import {
  REPLACEMENT_REASON_LABELS,
  type VehicleReplacementRecord,
  type VehicleFuelCard,
} from '@/lib/fleet/vehicle-types'
import { lookupVehicleFromMot } from '@/actions/fleet/mot-sync'
import { formatDate } from '@/lib/format'

type DialogView = 'list' | 'add' | 'edit'

type Props = {
  vehicleId: string
  open: boolean
  onClose: () => void
}
```

**רכיב `FuelCardsList` פנימי (לא מיוצא):**

```typescript
function FuelCardsList({
  recordId,
  cards,
  onCardsChange,
}: {
  recordId: string
  cards: VehicleFuelCard[]
  onCardsChange: () => void
}) {
  const [newCard, setNewCard] = useState('')
  const [isAdding, setIsAdding] = useState(false)

  async function handleAdd() {
    const trimmed = newCard.trim()
    if (!/^\d+$/.test(trimmed)) {
      toast.error('מספר כרטיס חייב להכיל ספרות בלבד')
      return
    }
    setIsAdding(true)
    const result = await addVehicleFuelCard(recordId, trimmed)
    if (result.success) {
      setNewCard('')
      onCardsChange()
    } else {
      toast.error(result.error ?? 'שגיאה בהוספת הכרטיס')
    }
    setIsAdding(false)
  }

  async function handleRemove(cardId: string) {
    const result = await deleteVehicleFuelCard(cardId)
    if (result.success) {
      onCardsChange()
    } else {
      toast.error(result.error ?? 'שגיאה בהסרת הכרטיס')
    }
  }

  return (
    <div className="space-y-2">
      <Label>כרטיסי דלק</Label>
      {/* רשימת כרטיסים קיימים */}
      {cards.length > 0 ? (
        <div className="space-y-1">
          {cards.map((card) => (
            <div key={card.id} className="flex items-center justify-between px-3 py-1.5 bg-muted/40 rounded-lg text-sm">
              <span className="font-mono" dir="ltr">{card.cardNumber}</span>
              <button
                onClick={() => void handleRemove(card.id)}
                className="text-muted-foreground hover:text-red-500 transition-colors"
                title="הסר כרטיס"
              >
                <X className="h-4 w-4" />
              </button>
            </div>
          ))}
        </div>
      ) : (
        <p className="text-xs text-muted-foreground">אין כרטיסי דלק</p>
      )}
      {/* הוספת כרטיס חדש */}
      <div className="flex gap-2">
        <Input
          value={newCard}
          onChange={(e) => setNewCard(e.target.value.replace(/\D/g, ''))}
          placeholder="מספר כרטיס (ספרות בלבד)"
          dir="ltr"
          className="font-mono text-sm flex-1"
          onKeyDown={(e) => { if (e.key === 'Enter') void handleAdd() }}
        />
        <Button
          type="button"
          size="sm"
          variant="outline"
          onClick={() => void handleAdd()}
          disabled={!newCard || isAdding}
        >
          {isAdding ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : <Plus className="h-3.5 w-3.5" />}
        </Button>
      </div>
    </div>
  )
}
```

**רכיב `ReplacementRecordForm` פנימי:**

```typescript
function ReplacementRecordForm({
  vehicleId,
  record,  // null = add mode, VehicleReplacementRecord = edit mode
  onSave,
  onCancel,
}: {
  vehicleId: string
  record: VehicleReplacementRecord | null
  onSave: (newRecordId?: string) => void
  onCancel: () => void
}) {
  const isEdit = !!record

  // Form state — pre-filled from record in edit mode
  const [plate, setPlate] = useState(record?.licensePlate ?? '')
  const [entryDate, setEntryDate] = useState(record?.entryDate ?? '')
  const [entryKm, setEntryKm] = useState(record?.entryKm?.toString() ?? '')
  const [returnDate, setReturnDate] = useState(record?.returnDate ?? '')
  const [returnKm, setReturnKm] = useState(record?.returnKm?.toString() ?? '')
  const [reason, setReason] = useState<'maintenance' | 'test' | 'accident' | 'other'>(
    record?.reason ?? 'maintenance'
  )
  const [reasonOther, setReasonOther] = useState(record?.reasonOther ?? '')
  const [notes, setNotes] = useState(record?.notes ?? '')
  const [motInfo, setMotInfo] = useState<string | null>(null)  // תצוגה מקדימה של MOT
  const [isSaving, startSaving] = useTransition()
  const [isLooking, startLookup] = useTransition()

  // כרטיסי דלק — רק בmode עריכה אחרי שיש record.id
  const [fuelCards, setFuelCards] = useState<VehicleFuelCard[]>(record?.fuelCards ?? [])

  // חישוב תקופת שהייה
  const stayDays = entryDate && returnDate
    ? Math.round((new Date(returnDate).getTime() - new Date(entryDate).getTime()) / 86400000)
    : null

  // חישוב ק"מ
  const kmTotal = entryKm && returnKm
    ? Math.max(0, parseInt(returnKm) - parseInt(entryKm))
    : null

  // MOT lookup לרכב חלופי
  function handleMotLookup() {
    if (!plate.trim()) return
    startLookup(async () => {
      const result = await lookupVehicleFromMot(plate)
      if (result.success && result.data) {
        setMotInfo(`${result.data.tozeret_nm ?? ''} ${result.data.degem_nm ?? ''} (${result.data.shnat_yitzur ?? ''})`)
      } else {
        toast.warning('לא נמצאו נתוני MOT לרכב זה')
        setMotInfo(null)
      }
    })
  }

  function handleSave() {
    if (!plate.trim()) { toast.error('נדרש מספר רישוי לרכב החלופי'); return }
    if (!entryDate) { toast.error('נדרש תאריך כניסה'); return }
    if (reason === 'other' && !reasonOther.trim()) { toast.error('נדרש הסבר לסיבה "אחר"'); return }

    startSaving(async () => {
      if (isEdit && record) {
        // עדכון רשומה קיימת
        const result = await updateVehicleReplacementRecord({
          recordId: record.id,
          vehicleId,
          licensePlate: plate.trim(),
          entryDate,
          entryKm: entryKm ? parseInt(entryKm) : null,
          returnDate: returnDate || null,
          returnKm: returnKm ? parseInt(returnKm) : null,
          reason,
          reasonOther: reason === 'other' ? reasonOther.trim() : null,
          notes: notes.trim() || null,
        })
        if (result.success) {
          toast.success('הרשומה עודכנה')
          onSave()
        } else {
          toast.error(result.error ?? 'שגיאה בשמירה')
        }
      } else {
        // הוספת רשומה חדשה
        const result = await addVehicleReplacementRecord({
          vehicleId,
          licensePlate: plate.trim(),
          entryDate,
          entryKm: entryKm ? parseInt(entryKm) : null,
          reason,
          reasonOther: reason === 'other' ? reasonOther.trim() : null,
          notes: notes.trim() || null,
        })
        if (result.success && result.id) {
          toast.success('רכב חלופי נוסף — כעת ניתן להוסיף כרטיסי דלק')
          onSave(result.id)  // מעביר recordId החדש לדיאלוג לעבור למצב עריכה
        } else {
          toast.error(result.error ?? 'שגיאה בשמירה')
        }
      }
    })
  }

  const selectClass = 'w-full border border-border rounded-lg px-3 py-2 text-base bg-background focus:outline-none focus:ring-2 focus:ring-primary/30 text-right appearance-none cursor-pointer'

  return (
    <div className="space-y-4">
      {/* כותרת */}
      <div className="flex items-center gap-2">
        <button onClick={onCancel} className="text-muted-foreground hover:text-foreground transition-colors">
          <ChevronRight className="h-5 w-5" />
        </button>
        <h3 className="font-semibold text-sm">{isEdit ? 'עריכת רכב חלופי' : 'הוספת רכב חלופי'}</h3>
      </div>

      {/* לוחית רישוי + MOT */}
      <div className="space-y-1.5">
        <Label>מספר רישוי רכב חלופי</Label>
        <div className="flex gap-2">
          <Input
            value={plate}
            onChange={(e) => setPlate(e.target.value)}
            placeholder="לדוגמה: 12-345-67"
            dir="ltr"
            className="font-mono flex-1"
          />
          <Button type="button" size="sm" variant="outline" onClick={handleMotLookup} disabled={!plate.trim() || isLooking}>
            {isLooking ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : <RefreshCw className="h-3.5 w-3.5" />}
          </Button>
        </div>
        {motInfo && (
          <p className="text-xs text-muted-foreground bg-muted/40 px-2 py-1 rounded">{motInfo}</p>
        )}
      </div>

      {/* שורת תאריכים וק"מ — 2 עמודות */}
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div className="space-y-1.5">
          <Label>תאריך כניסה <span className="text-red-500">*</span></Label>
          <FleetDateInput value={entryDate} onChange={setEntryDate} />
        </div>
        <div className="space-y-1.5">
          <Label>ק"מ כניסה</Label>
          <Input
            value={entryKm}
            onChange={(e) => setEntryKm(e.target.value.replace(/\D/g, ''))}
            placeholder="קילומטרז' בכניסה"
            dir="ltr"
            type="text"
            inputMode="numeric"
          />
        </div>
        <div className="space-y-1.5">
          <Label>תאריך החזרה</Label>
          <FleetDateInput value={returnDate} onChange={setReturnDate} />
        </div>
        <div className="space-y-1.5">
          <Label>ק"מ החזרה</Label>
          <Input
            value={returnKm}
            onChange={(e) => setReturnKm(e.target.value.replace(/\D/g, ''))}
            placeholder="קילומטרז' בהחזרה"
            dir="ltr"
            type="text"
            inputMode="numeric"
          />
        </div>
      </div>

      {/* חישובים אוטומטיים */}
      {(stayDays !== null || kmTotal !== null) && (
        <div className="flex gap-4 text-xs text-muted-foreground bg-muted/30 px-3 py-2 rounded-lg">
          {stayDays !== null && <span>תקופת שהייה: <strong>{stayDays} ימים</strong></span>}
          {kmTotal !== null && <span>סה"כ ק"מ: <strong>{kmTotal.toLocaleString()}</strong></span>}
        </div>
      )}

      {/* סיבה */}
      <div className="space-y-1.5">
        <Label>סיבה <span className="text-red-500">*</span></Label>
        <select
          value={reason}
          onChange={(e) => setReason(e.target.value as typeof reason)}
          className={selectClass}
        >
          {Object.entries(REPLACEMENT_REASON_LABELS).map(([key, label]) => (
            <option key={key} value={key}>{label}</option>
          ))}
        </select>
      </div>

      {/* הסבר לאחר */}
      {reason === 'other' && (
        <div className="space-y-1.5">
          <Label>הסבר <span className="text-red-500">*</span></Label>
          <Input
            value={reasonOther}
            onChange={(e) => setReasonOther(e.target.value)}
            placeholder="פרט את הסיבה..."
          />
        </div>
      )}

      {/* הערות */}
      <div className="space-y-1.5">
        <Label>הערות</Label>
        <textarea
          value={notes}
          onChange={(e) => setNotes(e.target.value)}
          placeholder="הערות נוספות..."
          rows={2}
          className="w-full border border-border rounded-lg px-3 py-2 text-base bg-background focus:outline-none focus:ring-2 focus:ring-primary/30 resize-none"
        />
      </div>

      {/* כרטיסי דלק — רק במצב עריכה (כשיש record.id) */}
      {isEdit && record && (
        <FuelCardsList
          recordId={record.id}
          cards={fuelCards}
          onCardsChange={async () => {
            // רענן כרטיסים
            const records = await getVehicleReplacementRecords(vehicleId)
            const current = records.find((r) => r.id === record.id)
            setFuelCards(current?.fuelCards ?? [])
          }}
        />
      )}
      {!isEdit && (
        <p className="text-xs text-muted-foreground">שמור קודם כדי להוסיף כרטיסי דלק</p>
      )}

      {/* כפתורים */}
      <div className="flex justify-between pt-2">
        <Button type="button" variant="outline" onClick={onCancel} disabled={isSaving}>
          ביטול
        </Button>
        <Button type="button" onClick={handleSave} disabled={isSaving}>
          {isSaving && <Loader2 className="h-4 w-4 ms-2 animate-spin" />}
          {isEdit ? 'שמור שינויים' : 'הוסף רכב חלופי'}
        </Button>
      </div>
    </div>
  )
}
```

**הרכיב הראשי `ReplacementVehicleDialog`:**

```typescript
export function ReplacementVehicleDialog({ vehicleId, open, onClose }: Props) {
  const router = useRouter()
  const [view, setView] = useState<DialogView>('list')
  const [editingRecord, setEditingRecord] = useState<VehicleReplacementRecord | null>(null)
  const [records, setRecords] = useState<VehicleReplacementRecord[]>([])
  const [isLoading, setIsLoading] = useState(false)

  async function loadRecords() {
    setIsLoading(true)
    const data = await getVehicleReplacementRecords(vehicleId)
    setRecords(data)
    setIsLoading(false)
  }

  // טעינה בפתיחת הדיאלוג
  useEffect(() => {
    if (open) {
      setView('list')
      setEditingRecord(null)
      void loadRecords()
    }
  }, [open, vehicleId])

  function handleSaveRecord(newRecordId?: string) {
    void loadRecords()
    if (newRecordId) {
      // לאחר הוספת רשומה חדשה — עבור למצב עריכה כדי לאפשר כרטיסי דלק
      void getVehicleReplacementRecords(vehicleId).then((data) => {
        const newRecord = data.find((r) => r.id === newRecordId) ?? null
        setEditingRecord(newRecord)
        setView('edit')
        setRecords(data)
      })
    } else {
      setView('list')
      router.refresh()  // מרענן VehicleDetailsSection עם vehicleStatus מעודכן
    }
  }

  function handleClose() {
    router.refresh()  // תמיד רענן בסגירה (vehicle_status עשוי להשתנות)
    onClose()
  }

  return (
    <Dialog open={open} onOpenChange={(v) => !v && handleClose()}>
      <DialogContent className="max-w-lg max-h-[90dvh] overflow-y-auto" dir="rtl">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2">
            <Car className="h-5 w-5" />
            ניהול רכב חלופי
          </DialogTitle>
        </DialogHeader>

        {/* תצוגת רשימה */}
        {(view === 'list') && (
          <div className="space-y-3">
            {isLoading ? (
              <div className="flex justify-center py-8">
                <Loader2 className="h-6 w-6 animate-spin text-muted-foreground" />
              </div>
            ) : records.length === 0 ? (
              <p className="text-sm text-muted-foreground text-center py-6">
                אין רכבים חלופיים מתועדים
              </p>
            ) : (
              <div className="space-y-2">
                {records.map((rec) => (
                  <button
                    key={rec.id}
                    onClick={() => { setEditingRecord(rec); setView('edit') }}
                    className="w-full text-right border border-border rounded-lg px-4 py-3 hover:bg-muted/50 transition-colors space-y-1"
                  >
                    <div className="flex items-center justify-between">
                      <span
                        className={`text-xs px-2 py-0.5 rounded-full font-medium ${
                          rec.status === 'active'
                            ? 'bg-green-100 text-green-700'
                            : 'bg-muted text-muted-foreground'
                        }`}
                      >
                        {rec.status === 'active' ? 'פעיל' : 'הוחזר'}
                      </span>
                      <span className="font-mono font-bold text-sm" dir="ltr">
                        {rec.licensePlate}
                      </span>
                    </div>
                    <div className="flex items-center justify-between text-xs text-muted-foreground">
                      <span>{REPLACEMENT_REASON_LABELS[rec.reason]}</span>
                      <span>
                        {formatDate(rec.entryDate)}
                        {rec.returnDate && ` — ${formatDate(rec.returnDate)}`}
                      </span>
                    </div>
                    {rec.fuelCards.length > 0 && (
                      <p className="text-xs text-muted-foreground">
                        {rec.fuelCards.length} כרטיסי דלק
                      </p>
                    )}
                  </button>
                ))}
              </div>
            )}

            {/* כפתור הוסף */}
            <Button
              type="button"
              variant="outline"
              className="w-full"
              onClick={() => { setEditingRecord(null); setView('add') }}
            >
              <Plus className="h-4 w-4 ms-2" />
              הוסף רכב חלופי
            </Button>
          </div>
        )}

        {/* תצוגת טופס הוספה/עריכה */}
        {(view === 'add' || view === 'edit') && (
          <ReplacementRecordForm
            vehicleId={vehicleId}
            record={view === 'edit' ? editingRecord : null}
            onSave={handleSaveRecord}
            onCancel={() => setView('list')}
          />
        )}
      </DialogContent>
    </Dialog>
  )
}
```
  </action>
  <verify>
```bash
npx tsc --noEmit 2>&1 | grep "ReplacementVehicleDialog" | head -10
```
בדוק שהקובץ נוצר ואינו ריק:
```bash
wc -l C:/Sharon_ClaudeCode/Apps/ChemoSystem/src/components/app/fleet/vehicles/ReplacementVehicleDialog.tsx
```
  </verify>
  <done>
- `ReplacementVehicleDialog.tsx` קיים, מעל 300 שורות
- מייצא `ReplacementVehicleDialog` component
- מכיל `FuelCardsList` ו-`ReplacementRecordForm` כרכיבים פנימיים
- אין שגיאות TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: חיבור ReplacementVehicleDialog ל-VehicleDetailsSection + Build בדיקה</name>
  <files>
    src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
  </files>
  <action>
עדכן את `VehicleDetailsSection.tsx` לחבר את הדיאלוג שנוצר:

1. הוסף import בראש הקובץ:
```typescript
import { ReplacementVehicleDialog } from './ReplacementVehicleDialog'
import { useRouter } from 'next/navigation'
```

2. הוסף state להצגת הדיאלוג (בתוך הרכיב, אחרי שאר ה-state):
```typescript
const [showReplacementDialog, setShowReplacementDialog] = useState(false)
const router = useRouter()
```

3. החלף את כפתור "ניהול רכב חלופי" הplaceholder שנוצר ב-Plan 17-01 עם:
```jsx
<div className="space-y-1.5">
  <button
    type="button"
    onClick={() => setShowReplacementDialog(true)}
    className="w-full flex items-center justify-center gap-2 px-4 py-2 rounded-lg text-sm font-medium border border-border hover:bg-muted/60 transition-colors"
  >
    <Car className="h-4 w-4" />
    ניהול רכב חלופי
  </button>
</div>
```

4. הוסף את הדיאלוג בסוף ה-JSX (לפני הסגר הראשי של ה-return):
```jsx
{/* Replacement Vehicle Dialog */}
<ReplacementVehicleDialog
  vehicleId={vehicle.id}
  open={showReplacementDialog}
  onClose={() => {
    setShowReplacementDialog(false)
    router.refresh()  // מרענן כדי שהheader badge ו-VehicleDetailsSection יציגו vehicleStatus מעודכן
  }}
/>
```

5. הרץ build מלא לאימות:
```bash
cd C:/Sharon_ClaudeCode/Apps/ChemoSystem && npm run build
```
תקן כל שגיאת TypeScript שתתגלה.

**שגיאות נפוצות לצפות:**
- ייבוא FleetDateInput — בדוק אם default export או named export:
  `import FleetDateInput from '@/components/app/fleet/shared/FleetDateInput'`
  או `import { FleetDateInput } from '@/components/app/fleet/shared/FleetDateInput'`
- `lookupVehicleFromMot` מ-mot-sync.ts — בדוק שה-import נכון
- אם `useRouter` נדרש — ודא `'use client'` בראש הקובץ (כבר קיים)
  </action>
  <verify>
```bash
cd C:/Sharon_ClaudeCode/Apps/ChemoSystem && npm run build 2>&1 | tail -30
```
  </verify>
  <done>
- `VehicleDetailsSection.tsx` מייבא ומשתמש ב-`ReplacementVehicleDialog`
- כפתור "ניהול רכב חלופי" פותח את הדיאלוג
- סגירת הדיאלוג קוראת ל-`router.refresh()`
- `npm run build` עובר ללא שגיאות
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: אימות ויזואלי ופונקציונלי — פאזה 17</name>
  <action>אין פעולת אוטומציה — המשתמש מבצע אימות ידני לפי ההוראות בסעיף how-to-verify</action>
  <what-built>
    - Plan 17-01: vehicle-types.ts (טיפוסים + קבועים חדשים), vehicle-replacement.ts (6 server actions), vehicles.ts (createVehicle ללא companyId + image actions), VehicleImageGallery, VehicleDetailsSection המחודש, AddVehicleDialog ללא company selector
    - Plan 17-02: ReplacementVehicleDialog (list + form + fuel cards), חיבור ל-VehicleDetailsSection
  </what-built>
  <how-to-verify>
    **1. AddVehicleDialog — הסרת company selector:**
    - עבור ל-`/app/fleet/vehicle-card`
    - לחץ על כפתור הוספת רכב — הדיאלוג חייב להציג רק שדה לוחית רישוי, ללא dropdown חברה
    - הכנס לוחית רישוי ולחץ "בדוק ב-MOT" — אמור לעבוד

    **2. טאב פרטי הרכב — שדות חדשים:**
    - פתח כרטיס רכב קיים (או צור חדש)
    - עבור לטאב "פרטי הרכב"
    - ודא שמוצגים:
      a. גלריית 5 חריצי תמונות בחלק העליון של עמודה שמאלית
      b. dropdown "סטטוס רכב" עם 5 ערכים בעברית (פעיל / מושבת זמני / הוחזר / נמכר / מושבת)
      c. שדה "תאריך יציאה מהצי" (FleetDateInput)
      d. כפתור "ניהול רכב חלופי"
      e. dropdown "סוג רכב" מציג 4 ערכים בלבד: פרטי / מסחרי / משאית / ניגרר

    **3. נעילת כרטיס:**
    - שנה "סטטוס רכב" ל-"הוחזר"
    - לחץ שמור — הודעת שגיאה "יש להזין תאריך יציאה מהצי"
    - הכנס תאריך יציאה ולחץ שמור — השמירה אמורה לעבוד
    - רענן הדף — כל השדות (מלבד "סטטוס רכב") אמורים להיות read-only עם תג "הכרטיס נעול"
    - שנה חזרה ל-"פעיל" ושמור — הנעילה מוסרת

    **4. גלריית תמונות:**
    - לחץ על חריץ תמונה ריק — אמור לפתוח file picker
    - העלה תמונה — אמורה להופיע בחריץ תוך מספר שניות
    - לחץ על התמונה — לייטבוקס נפתח
    - מחק תמונה (X) — התמונה נעלמת

    **5. דיאלוג רכב חלופי:**
    - לחץ "ניהול רכב חלופי" — דיאלוג נפתח עם רשימה ריקה
    - לחץ "הוסף רכב חלופי" — טופס מוצג
    - הכנס: לוחית רישוי, תאריך כניסה, סיבה "טיפול"
    - לחץ "הוסף רכב חלופי" — עובר לעריכה (ניתן להוסיף כרטיסי דלק)
    - סגור דיאלוג — כרטיס מתרענן, "סטטוס רכב" מוצג כ-"מושבת זמני"
  </how-to-verify>
  <resume-signal>הקלד "approved" אם הכל עובד, או תאר בעיות שנמצאו</resume-signal>
</task>

</tasks>

<verification>
לאחר checkpoint:
1. בדוק בבסיס הנתונים (Supabase Dashboard): טבלת `vehicle_replacement_records` מכילה רשומה חדשה
2. בדוק: טבלת `vehicles` — השדה `vehicle_status` עודכן ל-'suspended' לאחר הוספת רכב חלופי
3. בדוק: טבלת `vehicle_images` — תמונה שהועלתה מופיעה עם storage_path נכון
</verification>

<success_criteria>
- ReplacementVehicleDialog פועל — רשימה, הוספה, עריכה, כרטיסי דלק
- גלריית תמונות פועלת — העלאה, לייטבוקס, מחיקה
- נעילת כרטיס פועלת — status נועל → שדות read-only → status תמיד עריך
- AddVehicleDialog ללא company selector — יצירת רכב עובדת
- vehicle_status מסונכרן בין עדכוני DB לרענון ה-UI
- npm run build עובר
- checkpoint:human-verify אושר
</success_criteria>

<output>
לאחר השלמה, צור `.planning/phases/17-vehicle-card-redesign-details-images-replacement/17-02-SUMMARY.md`
</output>
