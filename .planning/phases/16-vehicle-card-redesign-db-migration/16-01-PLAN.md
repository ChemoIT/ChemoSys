---
phase: 16-vehicle-card-redesign-db-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00027_vehicle_card_redesign.sql
autonomous: true

must_haves:
  truths:
    - "vehicles table has 9 new columns (vehicle_status, fleet_exit_date, vehicle_category, camp_responsible_type, camp_responsible_name, camp_responsible_phone, ownership_supplier_id, contract_number, vehicle_group)"
    - "vehicles.vehicle_type CHECK constraint allows only private/commercial/truck/trailer"
    - "vehicles.ownership_type CHECK constraint allows only company/rental/operational_leasing/mini_leasing"
    - "vehicle_suppliers.supplier_type CHECK constraint includes ownership value"
    - "6 new tables exist: vehicle_images, vehicle_replacement_records, vehicle_fuel_cards, vehicle_driver_journal, vehicle_project_journal, vehicle_monthly_costs"
    - "vehicle_computed_status view uses vehicle_status instead of is_active"
    - "driver_computed_status view filters vehicles by vehicle_status in addition to is_active"
    - "RLS policies exist on all 6 new tables"
    - "soft_delete_vehicle_replacement_record RPC exists"
  artifacts:
    - path: "supabase/migrations/00027_vehicle_card_redesign.sql"
      provides: "Complete schema migration for vehicle card redesign"
      contains: "vehicle_images"
  key_links:
    - from: "vehicles.ownership_supplier_id"
      to: "vehicle_suppliers.id"
      via: "FK reference"
      pattern: "REFERENCES public.vehicle_suppliers"
    - from: "vehicle_replacement_records"
      to: "vehicles.id"
      via: "FK on vehicle_id"
      pattern: "REFERENCES public.vehicles"
    - from: "vehicle_fuel_cards"
      to: "vehicle_replacement_records.id"
      via: "FK on replacement_record_id"
      pattern: "REFERENCES public.vehicle_replacement_records"
---

<objective>
Write the main migration file `00027_vehicle_card_redesign.sql` that extends the existing vehicles schema for the full Vehicle Card Redesign.

Purpose: This migration provides all the database objects (columns, tables, views, RPCs, RLS, indexes) needed by Phases 17-19 (UI phases). Without it, no redesigned UI component can read or write data.

Output: `supabase/migrations/00027_vehicle_card_redesign.sql` — ready to run in Supabase SQL Editor.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/vehicle-card-redesign-requirements.md
@.planning/phases/16-vehicle-card-redesign-db-migration/16-RESEARCH.md
@supabase/migrations/00025_fleet_vehicles.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 00027 — ALTER TABLE vehicles + vehicle_suppliers</name>
  <files>supabase/migrations/00027_vehicle_card_redesign.sql</files>
  <action>
Create `supabase/migrations/00027_vehicle_card_redesign.sql` with a clear header comment block (migration name, purpose, depends on 00025, run in Supabase SQL Editor).

**Section 1: ALTER TABLE vehicles — clear incompatible values**

```sql
UPDATE public.vehicles SET vehicle_type = NULL
  WHERE vehicle_type NOT IN ('private','commercial','truck','trailer') AND vehicle_type IS NOT NULL;
UPDATE public.vehicles SET ownership_type = NULL
  WHERE ownership_type NOT IN ('company','rental','operational_leasing','mini_leasing') AND ownership_type IS NOT NULL;
```

**Section 2: ALTER TABLE vehicles — drop old CHECK constraints**

```sql
ALTER TABLE public.vehicles DROP CONSTRAINT IF EXISTS vehicles_vehicle_type_check;
ALTER TABLE public.vehicles DROP CONSTRAINT IF EXISTS vehicles_ownership_type_check;
```

**Section 3: ALTER TABLE vehicles — add 9 new columns (IF NOT EXISTS)**

All columns must be nullable OR have a DEFAULT for existing rows:
- `vehicle_status TEXT NOT NULL DEFAULT 'active'` (existing rows get 'active')
- `fleet_exit_date DATE`
- `vehicle_category TEXT`
- `camp_responsible_type TEXT`
- `camp_responsible_name TEXT`
- `camp_responsible_phone TEXT`
- `ownership_supplier_id UUID REFERENCES public.vehicle_suppliers(id)`
- `contract_number TEXT`
- `vehicle_group INT`

Use a single `ALTER TABLE` statement with `ADD COLUMN IF NOT EXISTS` for each column.

**Section 4: ALTER TABLE vehicles — add new CHECK constraints**

Add 6 constraints (vehicle_type, ownership_type, vehicle_status, vehicle_category, camp_responsible_type, vehicle_group). Each must use `DROP CONSTRAINT IF EXISTS` before `ADD CONSTRAINT` for idempotency. The vehicle_status constraint: `CHECK (vehicle_status IN ('active','suspended','returned','sold','decommissioned'))`. The vehicle_group constraint: `CHECK (vehicle_group BETWEEN 1 AND 7)`.

**IMPORTANT:** The ADD CONSTRAINT for vehicle_status must NOT have `IF NOT EXISTS` (PostgreSQL doesn't support it). Instead, use `DROP CONSTRAINT IF EXISTS vehicles_vehicle_status_check` before `ADD CONSTRAINT`.

**Section 5: ALTER TABLE vehicle_suppliers — add 'ownership' type**

```sql
ALTER TABLE public.vehicle_suppliers DROP CONSTRAINT IF EXISTS vehicle_suppliers_supplier_type_check;
ALTER TABLE public.vehicle_suppliers ADD CONSTRAINT vehicle_suppliers_supplier_type_check
  CHECK (supplier_type IN ('leasing','insurance','fuel_card','garage','other','ownership'));
```

**Section 6: CREATE TABLE vehicle_images**

Fields: id UUID PK, vehicle_id UUID NOT NULL FK vehicles ON DELETE CASCADE, storage_path TEXT NOT NULL, position INT NOT NULL CHECK (1-5), created_at TIMESTAMPTZ, created_by UUID FK auth.users.
No soft-delete, no updated_at. Hard-delete pattern (images are replaced, not versioned).
Add UNIQUE INDEX on (vehicle_id, position).

**Section 7: CREATE TABLE vehicle_replacement_records**

Fields: id UUID PK, vehicle_id UUID NOT NULL FK vehicles ON DELETE CASCADE, license_plate TEXT NOT NULL, mot_data JSONB, entry_date DATE NOT NULL, entry_km INT, return_date DATE, return_km INT, reason TEXT NOT NULL CHECK ('maintenance','test','accident','other'), reason_other TEXT, status TEXT NOT NULL DEFAULT 'active' CHECK ('active','returned'), notes TEXT, created_at, updated_at, created_by, updated_by, deleted_at (soft-delete).
Add updated_at trigger (`DROP TRIGGER IF EXISTS` + `CREATE TRIGGER`).

**Section 8: CREATE TABLE vehicle_fuel_cards**

Fields: id UUID PK, replacement_record_id UUID NOT NULL FK vehicle_replacement_records ON DELETE CASCADE, card_number TEXT NOT NULL, created_at, created_by.
No soft-delete (hard-delete with parent cascade). Add index on replacement_record_id.

**Section 9: CREATE TABLE vehicle_driver_journal**

Fields: id UUID PK, vehicle_id UUID NOT NULL FK vehicles ON DELETE CASCADE, driver_id UUID NOT NULL FK drivers(id), start_date DATE NOT NULL, end_date DATE (NULL = current), created_at, created_by.
No soft-delete (permanent history). Add partial index on vehicle_id WHERE end_date IS NULL.

**Section 10: CREATE TABLE vehicle_project_journal**

Fields: id UUID PK, vehicle_id UUID NOT NULL FK vehicles ON DELETE CASCADE, project_id UUID NOT NULL FK projects(id), start_date DATE NOT NULL, end_date DATE (NULL = current), created_at, created_by.
No soft-delete. Add partial index on vehicle_id WHERE end_date IS NULL.

**Section 11: CREATE TABLE vehicle_monthly_costs**

Fields: id UUID PK, vehicle_id UUID NOT NULL FK vehicles ON DELETE CASCADE, start_date DATE NOT NULL, end_date DATE, amount NUMERIC NOT NULL, created_at, updated_at, created_by UUID FK auth.users, updated_by UUID FK auth.users.
No soft-delete. Add updated_at trigger.

**Section 12: RLS policies for all 6 new tables**

Follow exact pattern from 00025:
- Enable RLS on each table.
- For tables WITH soft-delete (vehicle_replacement_records): SELECT USING(deleted_at IS NULL), INSERT WITH CHECK(true), UPDATE USING(true) WITH CHECK(true).
- For tables WITHOUT soft-delete (vehicle_images, vehicle_fuel_cards, journal tables, vehicle_monthly_costs): SELECT USING(true), INSERT WITH CHECK(true), UPDATE USING(true) WITH CHECK(true).
- For vehicle_images and vehicle_fuel_cards: also add DELETE policy USING(true) (hard-delete allowed).
- All policies use `DROP POLICY IF EXISTS` before `CREATE POLICY`.

**Section 13: Updated views**

`CREATE OR REPLACE VIEW public.vehicle_computed_status WITH (security_invoker = true)`:
- deleted_at IS NOT NULL -> 'inactive'
- vehicle_status IN ('returned','sold','decommissioned') -> 'inactive'
- vehicle_status = 'suspended' -> 'suspended'
- ELSE -> 'active'

`CREATE OR REPLACE VIEW public.driver_computed_status WITH (security_invoker = true)`:
- Same structure as in 00025, but the vehicle subquery adds: `AND veh.vehicle_status NOT IN ('returned','sold','decommissioned')` alongside existing `AND veh.is_active = TRUE`.

**Section 14: Soft-delete RPC**

`soft_delete_vehicle_replacement_record(p_id UUID, p_user_id UUID)` — SECURITY DEFINER, SET search_path = public. Same pattern as soft_delete_vehicle in 00025.

**Idempotency rules (CRITICAL):**
- All `CREATE TABLE` use `IF NOT EXISTS`
- All `CREATE INDEX` use `IF NOT EXISTS`
- All triggers: `DROP TRIGGER IF EXISTS` before `CREATE TRIGGER`
- All policies: `DROP POLICY IF EXISTS` before `CREATE POLICY`
- All functions: `CREATE OR REPLACE FUNCTION`
- All views: `CREATE OR REPLACE VIEW`
- All constraints: `DROP CONSTRAINT IF EXISTS` before `ADD CONSTRAINT` (PostgreSQL has no `ADD CONSTRAINT IF NOT EXISTS`)
  </action>
  <verify>
1. File exists at `supabase/migrations/00027_vehicle_card_redesign.sql`
2. File contains all 6 CREATE TABLE statements (grep for each table name)
3. File contains DROP CONSTRAINT IF EXISTS for vehicles_vehicle_type_check, vehicles_ownership_type_check, vehicle_suppliers_supplier_type_check
4. File contains CREATE OR REPLACE VIEW for both vehicle_computed_status and driver_computed_status
5. File contains soft_delete_vehicle_replacement_record function
6. File contains RLS ENABLE + policies for all 6 new tables
7. File contains updated_at triggers for vehicle_replacement_records and vehicle_monthly_costs
8. All CHECK constraint values match requirements exactly
  </verify>
  <done>
Migration file 00027 exists, is syntactically complete, idempotent, and contains all schema changes specified in the vehicle card redesign requirements: 9 new vehicles columns (with updated CHECK constraints), 1 updated vehicle_suppliers constraint, 6 new tables with RLS + indexes + triggers, 2 updated views, and 1 new soft-delete RPC.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'CREATE TABLE IF NOT EXISTS' supabase/migrations/00027_vehicle_card_redesign.sql` returns 6
2. `grep -c 'ENABLE ROW LEVEL SECURITY' supabase/migrations/00027_vehicle_card_redesign.sql` returns 6
3. `grep 'vehicle_status' supabase/migrations/00027_vehicle_card_redesign.sql` shows column definition + CHECK constraint + view usage
4. `grep 'soft_delete_vehicle_replacement_record' supabase/migrations/00027_vehicle_card_redesign.sql` shows function definition
5. `grep 'driver_computed_status' supabase/migrations/00027_vehicle_card_redesign.sql` shows updated view with vehicle_status filter
6. No syntax errors visible in SQL structure (balanced parentheses, semicolons after statements)
</verification>

<success_criteria>
- 00027_vehicle_card_redesign.sql exists in supabase/migrations/
- File follows project idempotent migration pattern (DROP IF EXISTS + CREATE)
- All 9 new vehicles columns defined with correct types, defaults, and CHECK constraints
- All 6 new tables match the research specification exactly
- Both computed status views updated for vehicle_status
- RLS policies cover all 6 new tables
- Soft-delete RPC exists for vehicle_replacement_records
- File is ready to paste into Supabase SQL Editor (no syntax issues)
</success_criteria>

<output>
After completion, create `.planning/phases/16-vehicle-card-redesign-db-migration/16-01-SUMMARY.md`
</output>
