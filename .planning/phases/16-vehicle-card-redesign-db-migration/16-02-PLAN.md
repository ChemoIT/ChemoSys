---
phase: 16-vehicle-card-redesign-db-migration
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - supabase/migrations/00028_vehicle_images_storage_policies.sql
autonomous: false

must_haves:
  truths:
    - "Storage policies for vehicle-images bucket exist (INSERT/SELECT/UPDATE/DELETE)"
    - "Migration 00027 runs without errors in Supabase SQL Editor"
    - "Migration 00028 runs without errors in Supabase SQL Editor"
    - "All 6 new tables are visible in Supabase Table Editor"
    - "vehicle_images bucket exists in Supabase Storage (Private)"
  artifacts:
    - path: "supabase/migrations/00028_vehicle_images_storage_policies.sql"
      provides: "Storage RLS policies for vehicle-images bucket"
      contains: "vehicle-images"
  key_links:
    - from: "storage.objects policies"
      to: "vehicle-images bucket"
      via: "bucket_id filter"
      pattern: "bucket_id = 'vehicle-images'"
---

<objective>
Write the storage policies migration file `00028_vehicle_images_storage_policies.sql` and verify both migrations (00027 + 00028) run successfully in Supabase.

Purpose: The vehicle-images bucket needs RLS policies for authenticated users to upload/view/delete vehicle photos. Human verification ensures both migrations ran cleanly before UI phases begin.

Output: `supabase/migrations/00028_vehicle_images_storage_policies.sql` + verified DB state.
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-vehicle-card-redesign-db-migration/16-01-SUMMARY.md
@supabase/migrations/00026_fleet_vehicles_storage_policies.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 00028 — vehicle-images storage policies</name>
  <files>supabase/migrations/00028_vehicle_images_storage_policies.sql</files>
  <action>
Create `supabase/migrations/00028_vehicle_images_storage_policies.sql` following the exact pattern from `00026_fleet_vehicles_storage_policies.sql`.

Header comment:
```sql
-- Migration: 00028_vehicle_images_storage_policies.sql
-- Purpose: Storage RLS policies for vehicle-images bucket (vehicle photos, up to 5 per vehicle)
-- PREREQUISITE: Create bucket "vehicle-images" (Private) in Supabase Dashboard -> Storage BEFORE running this migration
-- Depends on: 00027_vehicle_card_redesign (vehicle_images table)
```

4 policies — all use `DROP POLICY IF EXISTS` before `CREATE POLICY`:

1. `authenticated_insert_vehicle_images` — FOR INSERT TO authenticated WITH CHECK (bucket_id = 'vehicle-images')
2. `authenticated_select_vehicle_images` — FOR SELECT TO authenticated USING (bucket_id = 'vehicle-images')
3. `authenticated_update_vehicle_images` — FOR UPDATE TO authenticated USING (bucket_id = 'vehicle-images')
4. `authenticated_delete_vehicle_images` — FOR DELETE TO authenticated USING (bucket_id = 'vehicle-images')

Verbatim copy of 00026 pattern, just replacing bucket name from 'fleet-vehicle-documents' to 'vehicle-images'.
  </action>
  <verify>
1. File exists at `supabase/migrations/00028_vehicle_images_storage_policies.sql`
2. File contains exactly 4 policy definitions (INSERT/SELECT/UPDATE/DELETE)
3. All policies reference bucket_id = 'vehicle-images'
4. All policies use DROP POLICY IF EXISTS before CREATE POLICY
  </verify>
  <done>
00028_vehicle_images_storage_policies.sql exists with 4 storage policies for the vehicle-images bucket, following the established pattern from 00026.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify migrations in Supabase</name>
  <files>supabase/migrations/00027_vehicle_card_redesign.sql, supabase/migrations/00028_vehicle_images_storage_policies.sql</files>
  <action>
Sharon runs both migrations in Supabase SQL Editor and verifies the results. This is a human-verify checkpoint — Claude cannot access Supabase Dashboard.

**What was built:** Two migration files:
1. `00027_vehicle_card_redesign.sql` — ALTER TABLE vehicles (9 new columns + updated constraints), ALTER TABLE vehicle_suppliers (ownership type), 6 new tables, updated views, RLS policies, soft-delete RPC
2. `00028_vehicle_images_storage_policies.sql` — 4 storage policies for vehicle-images bucket

**Steps for Sharon:**

**Step 1: Create storage bucket**
1. Go to Supabase Dashboard -> Storage
2. Create new bucket: Name = `vehicle-images`, Public = OFF (Private)

**Step 2: Run migration 00027**
1. Go to Supabase Dashboard -> SQL Editor
2. Paste contents of `supabase/migrations/00027_vehicle_card_redesign.sql`
3. Click Run — should complete without errors

**Step 3: Run migration 00028**
1. Paste contents of `supabase/migrations/00028_vehicle_images_storage_policies.sql`
2. Click Run — should complete without errors

**Step 4: Verify schema**
1. Go to Table Editor — confirm these 6 new tables exist:
   - vehicle_images
   - vehicle_replacement_records
   - vehicle_fuel_cards
   - vehicle_driver_journal
   - vehicle_project_journal
   - vehicle_monthly_costs
2. Click on `vehicles` table — confirm new columns: vehicle_status, fleet_exit_date, vehicle_category, camp_responsible_type, camp_responsible_name, camp_responsible_phone, ownership_supplier_id, contract_number, vehicle_group
3. Check vehicle_status column has default value 'active' on existing rows

**Step 5: Verify storage**
1. Go to Storage — confirm `vehicle-images` bucket exists and is Private

Type "approved" if all migrations ran successfully and tables/columns are visible, or describe any errors encountered.
  </action>
  <verify>Human confirmed migrations ran without errors and all tables/columns/bucket are visible in Supabase Dashboard.</verify>
  <done>Both migrations (00027 + 00028) executed successfully. 6 new tables, 9 new vehicles columns, updated views, RLS policies, soft-delete RPC, and vehicle-images storage bucket all verified in production Supabase.</done>
</task>

</tasks>

<verification>
1. `supabase/migrations/00028_vehicle_images_storage_policies.sql` exists with 4 policies
2. Human confirmed both migrations ran without errors
3. Human confirmed 6 new tables visible in Supabase Table Editor
4. Human confirmed vehicles table has 9 new columns
5. Human confirmed vehicle-images bucket exists in Storage
</verification>

<success_criteria>
- 00028 migration file exists and follows project pattern
- Both 00027 and 00028 executed successfully in Supabase (human verified)
- All 6 new tables visible in Supabase Table Editor
- vehicles table has all 9 new columns with correct defaults
- vehicle_suppliers accepts 'ownership' supplier type
- vehicle-images storage bucket exists (Private)
- Database is ready for Phases 17-19 (UI development)
</success_criteria>

<output>
After completion, create `.planning/phases/16-vehicle-card-redesign-db-migration/16-02-SUMMARY.md`
</output>
