---
phase: 14-phase-10e-vehiclecard-tabs-4-8-assignment-costs-documents-notes-km-placeholder
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/app/fleet/vehicles/VehicleCard.tsx
  - src/components/app/fleet/vehicles/VehicleAssignmentSection.tsx
  - src/components/app/fleet/vehicles/VehicleCostsSection.tsx
  - src/components/app/fleet/vehicles/VehicleDocumentsSection.tsx
  - src/components/app/fleet/vehicles/VehicleNotesSection.tsx
  - src/app/(app)/app/fleet/vehicle-card/page.tsx
autonomous: true

must_haves:
  truths:
    - "Tab 4 (שיוך נהג) shows current assigned driver and allows assigning/removing a driver"
    - "Tab 5 (עלויות) shows a Coming Soon placeholder with DollarSign icon"
    - "Tab 6 (מסמכים) supports full document CRUD with autocomplete names, file upload, expiry tracking, and alerts"
    - "Tab 7 (הערות) shows a textarea for notes with save functionality and dirty tracking"
    - "Tab 8 (ק״מ) shows a Coming Soon placeholder with Gauge icon"
    - "/app/fleet/vehicle-card page shows a basic vehicle list table with links to individual vehicle cards"
  artifacts:
    - path: "src/components/app/fleet/vehicles/VehicleAssignmentSection.tsx"
      provides: "Tab 4 — current driver display + assign/remove driver"
    - path: "src/components/app/fleet/vehicles/VehicleCostsSection.tsx"
      provides: "Tab 5 — Coming Soon placeholder"
    - path: "src/components/app/fleet/vehicles/VehicleDocumentsSection.tsx"
      provides: "Tab 6 — vehicle documents CRUD with autocomplete, upload, alerts"
    - path: "src/components/app/fleet/vehicles/VehicleNotesSection.tsx"
      provides: "Tab 7 — notes textarea with save"
    - path: "src/app/(app)/app/fleet/vehicle-card/page.tsx"
      provides: "Minimal vehicle list page with table linking to vehicle cards"
  key_links:
    - from: "src/components/app/fleet/vehicles/VehicleAssignmentSection.tsx"
      to: "src/actions/fleet/vehicles.ts"
      via: "getActiveDriversForAssignment, assignDriverToVehicle"
    - from: "src/components/app/fleet/vehicles/VehicleDocumentsSection.tsx"
      to: "src/actions/fleet/vehicles.ts"
      via: "addVehicleDocument, updateVehicleDocument, deleteVehicleDocument, getVehicleDocumentNameSuggestions"
    - from: "src/app/(app)/app/fleet/vehicle-card/page.tsx"
      to: "src/actions/fleet/vehicles.ts"
      via: "getVehiclesList"
---

<objective>
Build the remaining 5 tabs (Assignment, Costs placeholder, Documents, Notes, KM placeholder) and replace the ComingSoon page with a minimal vehicle list.

Purpose: Completes the VehicleCard with all 8 functional/placeholder tabs and provides a navigable list page so the vehicle card flow is end-to-end testable.

Output:
- VehicleAssignmentSection.tsx — driver assignment with searchable dropdown
- VehicleCostsSection.tsx — Coming Soon placeholder
- VehicleDocumentsSection.tsx — full document CRUD (mirror of DriverDocumentsSection)
- VehicleNotesSection.tsx — notes textarea with save
- KM tab placeholder inline in VehicleCard.tsx
- Updated vehicle-card/page.tsx with minimal vehicle list table
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Plan 14-01 SUMMARY (for VehicleCard.tsx structure)
@.planning/phases/14-phase-10e-vehiclecard-tabs-4-8-assignment-costs-documents-notes-km-placeholder/14-01-SUMMARY.md

# Reference implementations
@src/components/app/fleet/drivers/DriverDocumentsSection.tsx
@src/components/app/fleet/drivers/DriverCard.tsx

# Server actions and types
@src/actions/fleet/vehicles.ts
@src/lib/fleet/vehicle-types.ts

# Shared components
@src/components/app/fleet/shared/FleetDateInput.tsx
@src/components/app/fleet/shared/AlertToggle.tsx
@src/components/app/fleet/shared/ExpiryIndicator.tsx
@src/components/app/fleet/shared/FleetFilePreview.tsx
@src/components/app/fleet/shared/FleetUploadZone.tsx
@src/components/app/fleet/shared/VehicleFitnessLight.tsx

# VehicleCard shell from Plan 14-01
@src/components/app/fleet/vehicles/VehicleCard.tsx

# Research
@.planning/phases/14-phase-10e-vehiclecard-tabs-4-8-assignment-costs-documents-notes-km-placeholder/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build VehicleAssignmentSection (Tab 4), VehicleCostsSection (Tab 5 placeholder), and VehicleNotesSection (Tab 7)</name>
  <files>
    src/components/app/fleet/vehicles/VehicleAssignmentSection.tsx
    src/components/app/fleet/vehicles/VehicleCostsSection.tsx
    src/components/app/fleet/vehicles/VehicleNotesSection.tsx
  </files>
  <action>
**1. VehicleAssignmentSection.tsx (Tab 4):**

Simple section showing current driver assignment + ability to change it.

**Layout:**
- Current assignment display: if `assignedDriverId` is not null, show driver name in a styled card (name + "נהג משויך" label). If null, show "לא משויך לנהג" in muted text.
- Below: searchable dropdown of active drivers for assignment
  - Fetch drivers via `getActiveDriversForAssignment()` on component mount (useEffect)
  - Dropdown/Select showing driver full name
  - "שייך נהג" button → calls `assignDriverToVehicle(vehicleId, selectedDriverId)`
  - If already assigned: "הסר שיוך" button → calls `assignDriverToVehicle(vehicleId, null)`
- Toast on success ("נהג שויך בהצלחה" / "שיוך הוסר")
- After assign/remove: call `router.refresh()` to update server data

**Props:**
```typescript
type Props = {
  vehicleId: string
  assignedDriverId: string | null
  assignedDriverName: string | null
}
```

**No dirty tracking needed** — assignment is auto-save on button click (not form-based).

**2. VehicleCostsSection.tsx (Tab 5 — Coming Soon placeholder):**

Simple placeholder matching DriverCard's "לוג נסיעות" Coming Soon pattern:

```tsx
<div className="bg-white border-x border-b rounded-b-2xl py-12 text-center" style={{ borderColor: '#E2EBF4' }}>
  <div className="w-12 h-12 rounded-xl flex items-center justify-center mx-auto mb-3"
    style={{ background: '#F0F5FB', border: '1px solid #E2EBF4' }}>
    <DollarSign className="h-6 w-6 text-muted-foreground/35" />
  </div>
  <p className="text-sm font-semibold text-muted-foreground">פיתוח עתידי</p>
  <p className="text-xs text-muted-foreground/50 mt-0.5">ניהול עלויות חודשיות ייפתח בגרסה הבאה</p>
</div>
```

No props needed beyond basic container. `'use client'` not required if purely static — but add it for consistency.

**3. VehicleNotesSection.tsx (Tab 7):**

Simple textarea bound to `vehicle.notes`:

- textarea (full width, 6 rows minimum) with current notes value
- "שמור" button → calls `updateVehicleDetails({ vehicleId, notes: currentNotes })`
- `isDirty` = local notes state !== original vehicle.notes
- `onEditingChange(isDirty)` callback to parent VehicleCard for dirty tracking
- Toast on success: "הערות נשמרו בהצלחה"
- Toast on error: error message from server action

**Props:**
```typescript
type Props = {
  vehicleId: string
  notes: string | null
  onEditingChange: (dirty: boolean) => void
}
```

`'use client'` directive. Use `useEffect` to call `onEditingChange` whenever dirty state changes.
  </action>
  <verify>
- `npm run build` passes
- Tab 4: Shows current driver or "לא משויך", can assign/remove driver
- Tab 5: Shows Coming Soon placeholder with DollarSign icon
- Tab 7: Notes textarea saves correctly, dirty tracking works
  </verify>
  <done>
- VehicleAssignmentSection shows assigned driver, supports assign/remove with driver dropdown
- VehicleCostsSection displays Coming Soon placeholder
- VehicleNotesSection saves notes with dirty tracking integration
  </done>
</task>

<task type="auto">
  <name>Task 2: Build VehicleDocumentsSection (Tab 6) + KM placeholder (Tab 8) + update VehicleCard imports + minimal VehicleList page</name>
  <files>
    src/components/app/fleet/vehicles/VehicleDocumentsSection.tsx
    src/components/app/fleet/vehicles/VehicleCard.tsx
    src/app/(app)/app/fleet/vehicle-card/page.tsx
  </files>
  <action>
**1. VehicleDocumentsSection.tsx (Tab 6) — near-exact mirror of DriverDocumentsSection.tsx (489 lines):**

Copy DriverDocumentsSection.tsx and make these substitutions:

| DriverDocumentsSection | VehicleDocumentsSection |
|------------------------|------------------------|
| `driverId` prop | `vehicleId` prop |
| `addDriverDocument` | `addVehicleDocument` |
| `updateDriverDocument` | `updateVehicleDocument` |
| `deleteDriverDocument` | `deleteVehicleDocument` |
| `getDocumentNameSuggestions` | `getVehicleDocumentNameSuggestions` |
| `fleet-documents` bucket | `fleet-vehicle-documents` bucket |
| `DriverDocument` type | `VehicleDocument` type |
| `${driverId}_doc_` upload path | `${vehicleId}_doc_` upload path |
| import from drivers actions | import from `@/actions/fleet/vehicles` |

All shared components plug in identically: FleetDateInput, AlertToggle, ExpiryIndicator, FleetUploadZone, FleetFilePreview.

**Features (same as DriverDocumentsSection):**
- Document list with expiry indicator, file preview, alert bell icon
- Add document form with:
  - Document name with autocomplete (fetches suggestions via `getVehicleDocumentNameSuggestions()`)
  - Expiry date (FleetDateInput) + ExpiryIndicator + quick expiry buttons (3m/1y/2y)
  - File upload (FleetUploadZone, bucket: `fleet-vehicle-documents`)
  - Alert enabled toggle (AlertToggle)
  - Notes textarea
- Edit existing document (same form, pre-populated)
- Delete document (soft-delete via `deleteVehicleDocument`)
- `onEditingChange` callback for dirty tracking
- Signed URLs: `createSignedUrl(fileName, 31_536_000)` (1 year)

**Props:**
```typescript
type Props = {
  vehicleId: string
  documents: VehicleDocument[]
  docYellowDays: number
  onEditingChange: (dirty: boolean) => void
}
```

**2. Update VehicleCard.tsx — integrate all tab sections + KM placeholder:**

Wire up all 5 new tab sections into VehicleCard.tsx tabs 4-8:
- Tab 4 (`assignment`): `<VehicleAssignmentSection vehicleId={vehicle.id} assignedDriverId={vehicle.assignedDriverId} assignedDriverName={vehicle.assignedDriverName} />`
- Tab 5 (`costs`): `<VehicleCostsSection />`
- Tab 6 (`documents`): `<VehicleDocumentsSection vehicleId={vehicle.id} documents={documents} docYellowDays={docYellowDays} onEditingChange={onDocumentsEditingChange} />`
- Tab 7 (`notes`): `<VehicleNotesSection vehicleId={vehicle.id} notes={vehicle.notes} onEditingChange={onNotesEditingChange} />`
- Tab 8 (`km`): Inline Coming Soon placeholder (no separate component):

```tsx
<TabsContent value="km" className="mt-0">
  <div dir="rtl">
    <div className="bg-white border-x border-b rounded-b-2xl py-12 text-center" style={{ borderColor: '#E2EBF4' }}>
      <div className="w-12 h-12 rounded-xl flex items-center justify-center mx-auto mb-3"
        style={{ background: '#F0F5FB', border: '1px solid #E2EBF4' }}>
        <Gauge className="h-6 w-6 text-muted-foreground/35" />
      </div>
      <p className="text-sm font-semibold text-muted-foreground">פיתוח עתידי</p>
      <p className="text-xs text-muted-foreground/50 mt-0.5">מעקב קילומטראז׳ ייפתח בגרסה הבאה</p>
    </div>
  </div>
</TabsContent>
```

Add all imports for the new section components.

**3. Replace ComingSoon in vehicle-card/page.tsx with minimal VehicleList:**

Replace the current ComingSoon placeholder at `/app/fleet/vehicle-card/page.tsx` with a minimal vehicle list:

- Server component: fetches vehicles via `getVehiclesList()`
- Simple table: license plate (formatted, clickable link to /app/fleet/vehicle-card/{id}), tozeret/degem, company name, vehicle type label, status badge (active/inactive), VehicleFitnessLight
- Sort by license plate
- "כרטיסי רכב" page title
- Responsive: on mobile, show as card layout (`sm:table-cell hidden` pattern or simpler card-based list)
- NO AddVehicleDialog (that's Phase 15)
- NO search/filter (Phase 15)

Import VehicleFitnessLight from shared components. Use formatLicensePlate from format.ts. Link to vehicle cards via Next.js `<Link>`.
  </action>
  <verify>
- `npm run build` passes with no TypeScript errors
- Tab 6 (מסמכים): document list renders, add/edit/delete works with file upload to fleet-vehicle-documents bucket
- Tab 8 (ק"מ): shows Coming Soon placeholder
- VehicleCard: all 8 tabs render their content correctly
- `/app/fleet/vehicle-card` page shows vehicle list table with clickable links to individual cards
- End-to-end: list page → click vehicle → card loads → all tabs work → back button returns to list
  </verify>
  <done>
- VehicleDocumentsSection fully mirrors DriverDocumentsSection with vehicle-specific bucket and actions
- VehicleCard.tsx integrates all 8 tabs with proper section components
- KM tab shows Coming Soon placeholder inline
- Vehicle list page replaces ComingSoon with navigable table
- Full end-to-end flow: list → card → tabs → back to list works
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — zero TypeScript errors
2. `/app/fleet/vehicle-card` — vehicle list displays with license plates, fitness lights, clickable links
3. Click vehicle → card loads with all 8 tabs
4. Tab 4 (שיוך נהג): assign/remove driver works with dropdown
5. Tab 5 (עלויות): Coming Soon placeholder visible
6. Tab 6 (מסמכים): add/edit/delete documents with file upload, autocomplete names, expiry tracking
7. Tab 7 (הערות): notes save correctly, dirty tracking triggers Dialog on tab switch
8. Tab 8 (ק"מ): Coming Soon placeholder visible
9. Dirty tracking: switching from documents or notes tab with changes → Dialog appears
10. Back button from card → returns to vehicle list
</verification>

<success_criteria>
- All 8 VehicleCard tabs fully functional or showing Coming Soon placeholders
- VehicleDocumentsSection is a working mirror of DriverDocumentsSection (fleet-vehicle-documents bucket)
- Driver assignment works end-to-end (assign, remove, refresh)
- Notes save with dirty tracking
- Minimal vehicle list page provides navigation to individual vehicle cards
- No TypeScript errors, no console errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-phase-10e-vehiclecard-tabs-4-8-assignment-costs-documents-notes-km-placeholder/14-02-SUMMARY.md`
</output>
