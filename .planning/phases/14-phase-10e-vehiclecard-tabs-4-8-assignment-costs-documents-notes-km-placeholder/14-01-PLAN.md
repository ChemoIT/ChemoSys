---
phase: 14-phase-10e-vehiclecard-tabs-4-8-assignment-costs-documents-notes-km-placeholder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/fleet/vehicles.ts
  - src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx
  - src/components/app/fleet/vehicles/VehicleCard.tsx
  - src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
  - src/components/app/fleet/vehicles/VehicleTestsSection.tsx
  - src/components/app/fleet/vehicles/VehicleInsuranceSection.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /app/fleet/vehicle-card/{id} displays a full vehicle card with header, fitness light, and 8 tabs"
    - "Tab 1 (פרטי הרכב) shows MOT data (read-only) and operational fields (editable) with save functionality"
    - "Tab 2 (טסטים) shows test history list, allows adding new tests with file upload, editing and soft-deleting existing tests"
    - "Tab 3 (ביטוח) shows insurance policies list, allows adding new policies with supplier dropdown, editing and soft-deleting"
    - "Switching tabs with unsaved changes shows a confirmation Dialog (not browser confirm)"
    - "VehicleFitnessLight in header reflects min expiry across tests, insurance, and documents"
  artifacts:
    - path: "src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx"
      provides: "Server page that fetches vehicle + tests + insurance + documents and passes as props"
    - path: "src/components/app/fleet/vehicles/VehicleCard.tsx"
      provides: "Main card component with header, 8 tabs, dirty tracking, unsaved changes Dialog"
    - path: "src/components/app/fleet/vehicles/VehicleDetailsSection.tsx"
      provides: "Tab 1 — MOT fields (read-only) + operational fields (editable) with save"
    - path: "src/components/app/fleet/vehicles/VehicleTestsSection.tsx"
      provides: "Tab 2 — test history list + add/edit/delete with file upload"
    - path: "src/components/app/fleet/vehicles/VehicleInsuranceSection.tsx"
      provides: "Tab 3 — insurance policies list + add/edit/delete with supplier dropdown"
  key_links:
    - from: "src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx"
      to: "src/actions/fleet/vehicles.ts"
      via: "getVehicleById, getVehicleTests, getVehicleInsurance, getVehicleDocuments"
    - from: "src/components/app/fleet/vehicles/VehicleCard.tsx"
      to: "src/components/app/fleet/shared/*"
      via: "VehicleFitnessLight import"
    - from: "src/components/app/fleet/vehicles/VehicleInsuranceSection.tsx"
      to: "src/actions/fleet/vehicles.ts"
      via: "getActiveSuppliersByType for supplier dropdown"
---

<objective>
Build the VehicleCard page infrastructure and first 3 tabs — mirroring the DriverCard pattern exactly.

Purpose: This creates the complete vehicle detail view with editable fields, test history management, and insurance policy management — the core of the vehicle card.

Output:
- Server page at /app/fleet/vehicle-card/[id]
- VehicleCard.tsx with header (avatar, license plate, fitness light, action buttons) + 8-tab shell + dirty tracking + unsaved changes Dialog
- VehicleDetailsSection.tsx (Tab 1) — MOT read-only fields + operational editable fields
- VehicleTestsSection.tsx (Tab 2) — test history CRUD with file upload
- VehicleInsuranceSection.tsx (Tab 3) — insurance policy CRUD with supplier dropdown
- getActiveSuppliersByType() server action added to vehicles.ts
</objective>

<execution_context>
@C:/Users/Alias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Alias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference implementation — THE blueprint
@src/components/app/fleet/drivers/DriverCard.tsx
@src/components/app/fleet/drivers/DriverDocumentsSection.tsx
@src/components/app/fleet/drivers/DriverLicenseSection.tsx

# Server actions and types
@src/actions/fleet/vehicles.ts
@src/lib/fleet/vehicle-types.ts

# Shared components to reuse
@src/components/app/fleet/shared/VehicleFitnessLight.tsx
@src/components/app/fleet/shared/FleetDateInput.tsx
@src/components/app/fleet/shared/AlertToggle.tsx
@src/components/app/fleet/shared/ExpiryIndicator.tsx
@src/components/app/fleet/shared/FleetUploadZone.tsx
@src/components/app/fleet/shared/FleetFilePreview.tsx

# Existing driver card server page for pattern reference
@src/app/(app)/app/fleet/driver-card/[id]/page.tsx

# Research
@.planning/phases/14-phase-10e-vehiclecard-tabs-4-8-assignment-costs-documents-notes-km-placeholder/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getActiveSuppliersByType server action + create VehicleCard server page + VehicleCard shell component</name>
  <files>
    src/actions/fleet/vehicles.ts
    src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx
    src/components/app/fleet/vehicles/VehicleCard.tsx
  </files>
  <action>
**1. Add `getActiveSuppliersByType()` to `src/actions/fleet/vehicles.ts`:**

NOTE: `vehicle-suppliers.ts` already exports `getVehicleSuppliers(type?)` but it returns ALL suppliers (active + inactive) and uses `verifySession()` (admin). We need a lighter version that filters `is_active = true` and uses `verifyAppUser()` (app context). Add to `vehicles.ts`:

```typescript
export async function getActiveSuppliersByType(
  supplierType: string
): Promise<{ id: string; name: string }[]> {
  await verifyAppUser()
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('vehicle_suppliers')
    .select('id, name')
    .eq('supplier_type', supplierType)
    .eq('is_active', true)
    .is('deleted_at', null)
    .order('name')
  if (error) throw new Error(error.message)
  return (data ?? []).map(row => ({ id: row.id, name: row.name }))
}
```

**2. Create `src/app/(app)/app/fleet/vehicle-card/[id]/page.tsx`:**

Server page that:
- Fetches vehicle via `getVehicleById(id)`, tests via `getVehicleTests(id)`, insurance via `getVehicleInsurance(id)`, documents via `getVehicleDocuments(id)` — all in parallel with `Promise.all`
- Calls `notFound()` if vehicle is null
- Reads env vars: `FLEET_LICENSE_YELLOW_DAYS` (default 60), `FLEET_DOCUMENT_YELLOW_DAYS` (default 60)
- Computes min expiry dates for fitness light: testExpiryDate (earliest from tests), insuranceMinExpiry (earliest from insurance), documentMinExpiry (earliest from documents) — same pattern as getVehiclesList
- Passes all data as props to `<VehicleCard>` client component
- Follow the exact pattern from `src/app/(app)/app/fleet/driver-card/[id]/page.tsx`
- IMPORTANT: In Next.js 16, params may need `await` — use `const { id } = await params` pattern

**3. Create `src/components/app/fleet/vehicles/VehicleCard.tsx`:**

Mirror DriverCard.tsx structure exactly (885 lines reference). Key sections:

**Header (mirror DriverCard lines 354-457):**
- Avatar: first 2 chars of license plate (not driver name initial)
- Primary identity: formatted license plate (bold, large via `formatLicensePlate`) + tozeret/degem + kinuy mishari
- Secondary info: company name + shnat yitzur + vehicle type label
- Status badge: active (ירוק) / inactive (אפור)
- VehicleFitnessLight component (pass testExpiryDate, insuranceMinExpiry, documentMinExpiry, yellowDays)
- Action buttons: Back (←) and Delete (with admin password Dialog — same pattern as DriverCard)
- NO PDF button, NO SMS button (vehicles don't have phone numbers)

**Delete Dialog:**
- Same admin password confirmation pattern as DriverCard
- Calls `deleteVehicleWithPassword(vehicleId, password)`
- On success: `router.push('/app/fleet/vehicle-card')` + toast.success

**8-tab shell with controlled Tabs:**
- Tab values: details, tests, insurance, assignment, costs, documents, notes, km
- Tab labels (Hebrew): פרטי הרכב | טסטים | ביטוח | שיוך נהג | עלויות | מסמכים | הערות | ק"מ
- Icons from lucide-react: Car, ClipboardCheck, Shield, User, DollarSign, Paperclip, FileText, Gauge
- CRITICAL: `dir="rtl"` on `<TabsList>` AND on every `<TabsContent>` wrapper div

**Dirty tracking (mirror DriverCard lines 160-170):**
- `useRef<Record<string, boolean>>({})` for dirty states
- `useCallback` handlers: onDetailsEditingChange, onTestsEditingChange, onInsuranceEditingChange, onDocumentsEditingChange, onNotesEditingChange
- Controlled Tabs: `value` + `onValueChange` that checks dirty state before switching
- Unsaved changes Dialog (shadcn Dialog, NOT browser confirm):
  - Hebrew text: "יש שינויים שלא נשמרו"
  - "חזור לשמור" button + "עבור בלי לשמור" button
  - Pending tab stored in state, applied on "עבור" click

**Tab content areas:**
- Tabs 1-3: render VehicleDetailsSection, VehicleTestsSection, VehicleInsuranceSection (built in this task + Task 2)
- Tabs 4-8: render placeholder sections (will be populated in Plan 14-02)
  - For now, tabs 4-8 content: simple `<div>` with "Section TBD" or pass-through to components from Plan 14-02

**Props interface:**
```typescript
type VehicleCardProps = {
  vehicle: VehicleFull
  tests: VehicleTest[]
  insurance: VehicleInsurance[]
  documents: VehicleDocument[]
  yellowDays: number
  docYellowDays: number
  testExpiryDate: string | null
  insuranceMinExpiry: string | null
  documentMinExpiry: string | null
}
```

Add `'use client'` directive. Import from `@/lib/supabase/browser` (NOT client). Import formatLicensePlate, formatDate from `@/lib/format`.
  </action>
  <verify>
- `npm run build` passes with no TypeScript errors
- Navigate to `/app/fleet/vehicle-card/{valid-id}` — page loads with vehicle header and 8 tab labels visible
- Tab switching works (clicking tabs changes content)
- Back button navigates to vehicle card list
  </verify>
  <done>
- VehicleCard page loads with header showing license plate, vehicle info, status badge, and fitness light
- 8 tabs render with correct Hebrew labels in RTL order
- Controlled tab switching works with dirty state tracking infrastructure in place
- getActiveSuppliersByType action available for insurance/details tabs
  </done>
</task>

<task type="auto">
  <name>Task 2: Build VehicleDetailsSection (Tab 1), VehicleTestsSection (Tab 2), and VehicleInsuranceSection (Tab 3)</name>
  <files>
    src/components/app/fleet/vehicles/VehicleDetailsSection.tsx
    src/components/app/fleet/vehicles/VehicleTestsSection.tsx
    src/components/app/fleet/vehicles/VehicleInsuranceSection.tsx
  </files>
  <action>
**1. VehicleDetailsSection.tsx (Tab 1) — mirror DriverCard Tab 1 pattern:**

Two-column RTL grid layout (`grid-cols-1 sm:grid-cols-2`):

**Right column (first in RTL) — MOT data (read-only, gray background):**
- License plate (formatted via formatLicensePlate)
- Manufacturer (tozeret_nm) + Model (degem_nm)
- Commercial name (kinuy_mishari)
- Year (shnat_yitzur)
- Color (tzeva_rechev)
- Fuel type (sug_delek_nm)
- Chassis (misgeret)
- Engine model (degem_manoa)
- Trim level (ramat_gimur)
- Emission group (kvutzat_zihum)
- Ownership (baalut)
- Registration date (moed_aliya_lakvish) — formatted via formatDate
- MOT last sync — formatted date + "עדכן ממשרד הרישוי" button that calls `syncVehicleFromMot()` from `mot-sync.ts`
- All MOT fields displayed with `readOnly` + muted background styling

**Left column — Operational fields (editable):**
- Vehicle type: select from `VEHICLE_TYPE_LABELS` (import from vehicle-types.ts)
- Ownership type: select from `OWNERSHIP_TYPE_LABELS`
- Company: select from companies list (passed as prop from server page — query companies in the server page)
- Is Active: toggle switch
- Leasing company: select from suppliers (fetch via `getActiveSuppliersByType('leasing')` on mount — useEffect)
- Insurance company: select from suppliers (fetch via `getActiveSuppliersByType('insurance')`)
- Fuel card supplier: select from suppliers (fetch via `getActiveSuppliersByType('fuel_card')`)
- Garage: select from suppliers (fetch via `getActiveSuppliersByType('garage')`)

**Save:** Button calls `updateVehicleDetails()` with all editable fields. Toast on success/error.
**Dirty tracking:** Compare current form state vs original vehicle data. Call `onEditingChange(isDirty)` prop.
**Supplier dropdowns:** Fetch all 4 supplier types on component mount via separate `getActiveSuppliersByType` calls. Store in local state.

NOTE: Also add companies fetch to the server page (`[id]/page.tsx`) — query active companies and pass as prop to VehicleCard → VehicleDetailsSection. There is NO existing `getCompanies()` action, so fetch directly via supabase in the server page:
```typescript
const supabase = await createClient()
const { data: companies } = await supabase
  .from('companies')
  .select('id, name')
  .is('deleted_at', null)
  .order('name')
```
Pass `companies ?? []` as a prop to VehicleCard → VehicleDetailsSection.

**2. VehicleTestsSection.tsx (Tab 2) — mirror DriverDocumentsSection pattern:**

**List view:**
- Shows all tests sorted by test_date DESC (newest first)
- Each row: test date (formatted), expiry date with ExpiryIndicator, passed ✓/✗ badge, station name, cost (₪), file preview (FleetFilePreview), alert bell if alertEnabled
- Edit button (pencil icon) and delete button (trash icon) per row
- "הוסף טסט" button at top

**Add/Edit form (inline, replaces list when active):**
- test_date: FleetDateInput (required)
- expiry_date: FleetDateInput (required) + ExpiryIndicator + quick expiry buttons (3m/1y/2y)
- passed: checkbox (default true)
- test_station: text input
- cost: number input (₪)
- file: FleetUploadZone (bucket: `fleet-vehicle-documents`, path: `${vehicleId}_test_${crypto.randomUUID()}.${ext}`)
- alert_enabled: AlertToggle
- notes: textarea

**Actions:**
- Add: call `addVehicleTest()` → refresh via router.refresh() + update local state
- Edit: call `updateVehicleTest()` → same refresh pattern
- Delete: call `deleteVehicleTest(testId)` → removes from list + toast

**Dirty tracking:** `onEditingChange` callback — dirty when form is open with changes.

**3. VehicleInsuranceSection.tsx (Tab 3) — similar to VehicleTestsSection:**

**List view:**
- Shows all insurance policies sorted by expiry_date DESC
- Each row: insurance type label (from INSURANCE_TYPE_LABELS), policy number, supplier name, expiry date with ExpiryIndicator, cost (₪), file preview, alert bell
- Edit + delete buttons per row
- "הוסף פוליסה" button at top

**Add/Edit form:**
- insurance_type: select ('mandatory' | 'comprehensive' | 'third_party') with Hebrew labels from INSURANCE_TYPE_LABELS
- policy_number: text input
- supplier_id: select populated from `getActiveSuppliersByType('insurance')` (fetch on component mount)
- start_date: FleetDateInput
- expiry_date: FleetDateInput (required) + ExpiryIndicator + quick expiry buttons
- cost: number input (₪)
- file: FleetUploadZone (bucket: `fleet-vehicle-documents`, path: `${vehicleId}_insurance_${crypto.randomUUID()}.${ext}`)
- alert_enabled: AlertToggle
- notes: textarea

**Actions:**
- Add: `addVehicleInsurance()` → refresh
- Edit: `updateVehicleInsurance()` → refresh
- Delete: `deleteVehicleInsurance(id)` → remove + toast

**Dirty tracking:** same onEditingChange pattern.

**For all 3 components:**
- `'use client'` directive
- Import from `@/lib/supabase/browser` (NOT client)
- Use `createSignedUrl(fileName, 31_536_000)` for 1-year signed URLs on upload
- All dates display via `formatDate()` from `@/lib/format`
- Toast via `sonner` — `toast.success()` / `toast.error()`
- RTL: inherit from parent TabsContent `dir="rtl"` wrapper
- Responsive: `grid-cols-1 sm:grid-cols-2` for form fields, touch targets 44px minimum
  </action>
  <verify>
- `npm run build` passes with no TypeScript errors
- Tab 1 shows MOT data (read-only) and operational fields (editable), save works
- Tab 2 shows test list, add/edit/delete test works with file upload
- Tab 3 shows insurance list, add/edit/delete insurance works with supplier dropdown
- Dirty tracking triggers unsaved changes Dialog on tab switch
  </verify>
  <done>
- VehicleDetailsSection displays all MOT fields read-only and all operational fields editable with save
- VehicleTestsSection shows test history, supports add/edit/delete with file upload and quick expiry buttons
- VehicleInsuranceSection shows insurance policies, supports add/edit/delete with supplier dropdown
- All three tabs integrate with VehicleCard dirty tracking and unsaved changes Dialog
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — zero TypeScript errors
2. Navigate to `/app/fleet/vehicle-card/{id}` — full card loads with header + 8 tabs
3. Tab 1: MOT fields visible (read-only), operational fields editable, save works
4. Tab 2: Test list renders, add new test with file upload, edit existing, delete with toast
5. Tab 3: Insurance list renders, supplier dropdown populated, add/edit/delete work
6. Switch tabs with unsaved changes → Dialog appears with Hebrew text
7. VehicleFitnessLight in header shows correct color based on test/insurance/doc expiry
8. Back button navigates to vehicle card list page
</verification>

<success_criteria>
- Vehicle card page fully renders at /app/fleet/vehicle-card/[id] with server-fetched data
- Header shows license plate, vehicle info, status, fitness light, back + delete buttons
- Tabs 1-3 fully functional with CRUD operations, file upload, and dirty tracking
- Tabs 4-8 visible with placeholder content (populated in Plan 14-02)
- No TypeScript errors, no console errors in browser
</success_criteria>

<output>
After completion, create `.planning/phases/14-phase-10e-vehiclecard-tabs-4-8-assignment-costs-documents-notes-km-placeholder/14-01-SUMMARY.md`
</output>
